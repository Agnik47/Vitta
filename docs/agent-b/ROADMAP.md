# Agent B — Roadmap

Timeline and milestones for Agent B's slice of the build, against the hard deadline in `docs/00-PRODUCT-BRIEF.md`: a working end-to-end demo by **31 July 2026 night**, live hackathon demo **1 August 2026**. Today is **28 July 2026** — roughly 3 days out.

This is Agent B's own pacing plan, not a shared commitment Agent A is bound by. It exists so a fresh session (or a status check from Agent A) can see at a glance whether Agent B's track is on schedule, without re-deriving it from the task list. Update the "Where we actually are" section every session; don't let it go stale.

## Milestones

| Milestone | Target | Depends on | Notes |
|---|---|---|---|
| M1 — Dodo test account confirmed, `.env` populated | ASAP, before any 1c code | Nothing — a manual/human step | Hard blocker until resolved; see `docs/common/04-BLOCKERS.md` |
| M2 — `webcmd doctor` passing | ✅ Done 2026-07-29 (later still) — root cause was a missing ~535MB Chromium binary (`cloakbrowser install`), plus a `PATH` issue blocking its extraction step | Nothing — resolved directly | See ADR-007 for the full diagnosis |
| M3 — Phase 1c done (Dodo ledger, real integration script run + pasted output) | ✅ Done 2026-07-29 (later same day) — implemented by this track per direct user instruction, ADR-006 | M1, Phase 0 scaffolding | `src/ledger/` — real, tested, idempotency confirmed |
| M4 — Phase 1d done (webcmd manifest + executor, real test script run + pasted output) | ✅ Done 2026-07-29 (later still) | M2, Phase 0 scaffolding | `src/webcmd/` — `execute()` now real-tested against a live browser command, 3 real bugs found+fixed along the way (ADR-007) |
| M5 — Dashboard app shell scaffolded | As soon as M3+M4 land | M3, M4 | Doesn't need Agent A's frozen schemas — start immediately, don't wait idle |
| M6 — Dashboard data routes (`/api/mandate`, `/api/events`, `/api/receipts`) | Day 2 (29-30 Jul) | Agent A's 1b + 1e pushed (Sync Point 4) | If Agent A is behind, keep building shell/static pages, don't block |
| M7 — Dashboard pages wired to real data, build+start verified, acceptance checklist passed | Day 2-3 (30-31 Jul) | M6 | `docs/06-DASHBOARD-SPEC.md` § Acceptance checklist is the actual gate |
| M8 — Phase 2-4 stub verification (filler, whenever idle) | Opportunistic | Phase 0 | Low effort, pick up during any wait on Agent A |
| M9 — Joint Phase 5 rehearsal | Day 3, before 1 Aug | Agent A's 1g + this track's 1h | Live joint session, not async |

## Where we actually are

_(Update every session — one line per milestone, most recent state.)_

- **2026-07-28:** Phase 0 scaffolding drafted locally (uncommitted) as a working baseline — package.json, tsconfig.json, full `src/` skeleton, `.env.example`, `dodopayments` installed. M1 and M2 are both open: no `.env` with real Dodo keys yet, no webcmd installed yet. Both are hard blockers for M3/M4 — cannot write real Phase 1c/1d code until resolved. Working on docs/task-tracking in the meantime; will pick up M3/M4 the moment M1/M2 clear, without waiting on Agent A's own phases.
- **2026-07-29:** Pulled Agent A's real Phase 0. M2 cleared partially (webcmd installed, but its browser-connectivity check fails — new blocker B-002) → M4 (webcmd) done except `execute()`'s live verification. M5-M7 (dashboard) pulled forward hard once Agent A shipped 1b+1e (Sync Point 4 opened same day) — built the whole dashboard in one pass rather than just the shell: Next.js app, 3 API routes, 3 pages, verified in an actual browser against real signed fixture data including a live tamper test. Later the same day: M3 (Phase 1c) reassigned to Agent A entirely (ADR-005) — no longer a milestone on this track. Agent A resolved M1 (B-001) via real account provisioning; the user then shared those same credentials with this session directly, letting M7's Dodo-balance work get real-account verification too (previously only SDK-type-level confidence) — found and fixed a real `environment: 'test_mode'` SDK bug along the way. Later still, same day: the user directly instructed this track to implement M3 after all (superseding the reassignment for this specific file, confirmed Agent A hadn't started it first) — `src/ledger/DodoCreditLedger.ts` written and real-tested, including confirming Dodo's `idempotency_key` genuinely prevents a double `draw()`. M3 now done. Later still, same day: the user directly asked to resolve B-002 (M2) — root cause was a missing Chromium binary `cloakbrowser` needs (never downloaded on either agent's machine) plus a `PATH` issue in its installer; fixed both, `webcmd doctor` now passes for real, and `execute()` itself is real-tested against two live browser commands (found+fixed 3 more real bugs along the way — unsafe Windows process spawning and a `runId`/`tracePath` shape mismatch that would have corrupted Agent A's real receipts). M2 and M4 now fully done. M7's acceptance checklist passes fully except the CLI-kill-mid-run item, which needs a real Phase 1g run to exist first. M8/M9 not started. **No open blockers remain anywhere on this track.**
- **2026-07-29 (later still):** Agent A independently reproduced B-002 on their own machine (same root cause) and then ran the real Phase 1g rehearsal solo: Beats 1-4 confirmed live (real Dodo checkout+payment, real blinkit login/search/add-to-cart, real cart read, real DENY), 4 more real bugs found+fixed, one touching this track's files (`Ledger.fund()`'s return type additively widened to `{ reserveRef, checkoutUrl? }`). Pulled it, re-verified clean (`tsc --noEmit`, 45/45 tests), fixed a stale `03-INTERFACES.md` row this track owns, synced the shared status tables. M9 (joint Phase 5 rehearsal) is now gated on one thing only: the user deciding whether/when to authorize Beats 5-8 — a real purchase on a real merchant site, i.e. actual money, distinct from the Dodo test-mode reserve layer everything else in this build uses. Not treating "the code path is ready" as authorization to run it.
- **2026-07-29 (later still):** Closed the last two open items on this track. M4 (webcmd) picked up one more real fix: `tracePath`/`trace_digest`, open since B-002's resolution, turned out to be empty by structural necessity (`--trace retain-on-failure` never traces a success, and receipts are only built on success) rather than an unfound file path — switched to `--trace on`, real `sha256` digest now flows into receipts (ADR-009). M7 (dashboard acceptance checklist) closed its one remaining item for real — the CLI-kill-mid-run test — which in turn surfaced and fixed a real bug in Agent A's `gate.ts` (`events.jsonl` was never written at all, so `/events` would have been dead during the real demo — ADR-008). **All of M1-M8 are now fully done, zero open items anywhere on this track.** M9 (joint Phase 5 rehearsal, including Beats 5-8) remains the only thing left, gated entirely on the user's real-purchase decision.

## Sequencing rule

M3 and M4 don't depend on each other or on Agent A's 1a/1b/1e — work whichever of M1/M2 clears first, do that phase, then the other. Do not sit idle waiting for both blockers to clear before starting anything: if only M1 clears, do 1c; if only M2 clears, do 1d.

M5 (dashboard shell) is the one thing on this roadmap with zero upstream dependency at all besides Node/npm existing — if both M1 and M2 stay blocked for a while, this is legitimate work to pull forward rather than idling, per `docs/common/05-PHASE-OWNERSHIP.md` Sync Point 4's "shell can start after 1c/1d" note (shell scaffolding specifically doesn't even need that — only the data routes do).
