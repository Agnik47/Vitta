# Agent B — Workspace

Read `docs/common/00-START-HERE.md` before this file, every session, if you haven't already this session.

See also, in this same folder: `TASKS.md` (durable checklist for 1c/1d/1h), `ROADMAP.md` (milestones against the deadline), `ERROR-HANDLING.md` (failure modes specific to the ledger/webcmd/dashboard I/O rails).

## Role

You own the **I/O rails**: the real Dodo Payments test-mode integration and the real webcmd integration — the two modules that talk to the outside world and that `decide()` deliberately stays decoupled from (it takes their outputs as plain arguments). Once both are done, you move entirely onto the **dashboard** (`dashboard/`), a fully separate Next.js app. See `docs/common/05-PHASE-OWNERSHIP.md` for the full rationale on why this split keeps you and Agent A on disjoint files almost the whole build, and `docs/common/02-DECISIONS.md` ADR-002 for the decision record.

## Phases you own

| Phase | Status | Depends on |
|---|---|---|
| 1c — `src/ledger/DodoCreditLedger.ts` | ✅ Done — implemented + real-tested, 2026-07-29, per direct user instruction (ADR-006, supersedes ADR-005 for this file) | Real Dodo account (Agent A's provisioning) — done |
| 1d — webcmd integration | ✅ Fully done — B-002 resolved, `execute()` real-tested against a live command, 2026-07-29 (ADR-007) | Phase 0 (Agent A) — done |
| 1h — Dashboard (Next.js, read-only) | ✅ Done — Dodo balance lookup now verified against a real account too | 1b + 1e (Agent A, both shipped) — done |

Plus: Phase 2-4 stub verification and Phase 5 rehearsal are shared with Agent A — see `docs/common/05-PHASE-OWNERSHIP.md`.

## Folders/files you own

`src/ledger/`, `src/webcmd/`, `dashboard/` (entire app, own `package.json`, never mixed into the root one).

Never edit `src/mandate/`, `src/policy/`, `src/receipt/`, or `src/cli/` without flagging it first per `docs/common/06-SYNC-WORKFLOW.md` — those are Agent A's.

## Current task

Closed the two remaining open items on this track, back to back. (1) Ran Phase 1h's last acceptance-checklist item for real: built+started the dashboard, ran real `gate` CLI commands (`scan`, `mandate create`, `run -- webcmd duckduckgo search`), killed the dashboard process mid-flight, confirmed more CLI commands kept working identically. Found a real bug doing this: `cmdRun()` builds a `GateEvent` and prints it, but never persists it — `events.jsonl` was never written by a real CLI run. Fixed directly in Agent A's `src/cli/gate.ts`/`src/cli/store.ts` (new `appendEvent()` export) — ADR-008. (2) Resolved `tracePath`/`trace_digest`, open since ADR-007: read webcmd's own source (`dist/src/observation/artifact.js`, `execution.js`) and found the real trace directory (`~/.webcmd/profiles/<contextId>/traces/<traceId>/`), but also found the actual root cause of the emptiness — `execute()` used `--trace retain-on-failure`, which only exports an artifact on failure, while a `Receipt` is only ever built after success. Switched to `--trace on`; `execute()` now parses webcmd's real `Webcmd trace artifact: <dir>` stderr line and computes a real `sha256` digest of `trace.jsonl` — ADR-009. Both fixes verified live end to end, `tsc --noEmit`/`npm test` (45/45) clean throughout.

## Status

✅ All three owned phases fully done, zero open items on any: 1c (`DodoCreditLedger.ts`, ADR-006), 1d (webcmd, B-002 resolved + `tracePath`/`traceDigest` real, ADR-007/009), 1h (dashboard, CLI-kill test done, ADR-008). No open blockers on this track. Watching Phase 1g/5 from the sidelines — Beats 5-8 are a user decision (real spend), not something either agent should push forward alone.

## Progress log

_(Append new entries at the bottom, most recent last. Include date, what you did, what passed/failed, and anything the reader would need to resume mid-task.)_

- 2026-07-28 — Workspace file created as part of the parallel-development docs restructuring. No implementation work has started yet.
- 2026-07-29 — Agent A pushed real Phase 0 (`46f8514`) while this session had independently drafted a duplicate local scaffold (uncommitted). Caught it via `git fetch` before pushing — discarded the local duplicate entirely, pulled Agent A's version, confirmed `tsc --noEmit` and `npm test` both pass clean on this machine too. Added `TASKS.md`, `ROADMAP.md`, `ERROR-HANDLING.md` to this folder per user request. Now starting real Phase 1d work (webcmd install, self-serve) and flagging Phase 1c's Dodo-account blocker to the user.
- 2026-07-29 — Installed `@agentrhq/webcmd@0.4.3` globally. `webcmd doctor` fails its Connectivity check (browser exec timeout) — daemon and Cloak runtime both report OK, but no browser-backed command has ever succeeded on this machine (confirmed via `webcmd profile list`). Manifest fetch (`webcmd list -f json`) is unaffected and works fine (805 real commands, 228 write-access). Implemented `src/webcmd/manifest.ts` for real, ran the required manual checks for real (write count, `blinkit/place-order` lookup, nonsense-command fail-closed check, plus the live-fetch-fails-falls-back-to-cache path) — all pass, pasted into `docs/OUTCOME.md`. Implemented `src/webcmd/executor.ts`'s `execute()` per spec (unverified — can't run a real command until B-002 clears) plus `hasAlreadyDrawn()`/`recordDraw()` idempotency guard against `ledger.jsonl` (real-tested, pure fs logic — see ADR-004 in `02-DECISIONS.md` for the entry-shape decision). Logged blocker B-002. `tsc --noEmit` and `npm test` (10/10) both still clean.
- 2026-07-29 — Phase 1h (dashboard): Sync Point 4 opened once Agent A shipped 1b+1e, so built the whole thing in one session. Scaffolded Next.js 16.2.12 at `dashboard/`, implemented all 3 API routes + 3 pages + `lib/{types,hash,read,dodo}.ts` per `docs/06-DASHBOARD-SPEC.md`. Found the real Dodo SDK's balance model is customer-keyed, not session-keyed as the spec sketch assumed — see `docs/OUTCOME.md`'s open-questions table for the full real-type writeup; `lib/dodo.ts` implements the real resolution chain, marked unverified-live pending B-001. Found the gate's public-key persistence location is undefined (Phase 1f gap) — flagged below, shipped `chain_link_valid` for real and `signature_valid` as "pending" in the meantime. Generated real signed fixtures with the actual production crypto code, verified all 3 pages in an actual Chrome tab including a live tamper test (edited a receipt on disk, watched the second receipt's badge flip to invalid within one poll cycle, no manual refresh) — full writeup in `docs/OUTCOME.md`. Deleted the fixture data/generator afterward so no fake data lingers in the repo's gitignored runtime folders. `npm run build && npm run start` both clean, zero browser console errors.
- 2026-07-29 — Pulled Agent A's Phase 1f (`src/cli/keys.ts`, 5/7 `gate` subcommands real, 4 real bugs fixed, `manifest.json` un-gitignored). Committed the real `manifest.json` (805/228) as requested — `gate scan` can now be tested against real data on Agent A's machine too. Wired up the dashboard's `signature_valid` same day: added `verifySignature()` to `dashboard/lib/hash.ts` (duplicate of `src/mandate/sign.ts`'s `verify()`) and `loadGatePublicKeyPem()` to `dashboard/lib/read.ts` (reads `keys/gate.public.pem` via `MANDATE_GATE_DATA_DIR`, returns `null` gracefully if absent), `verifyChainLocal()` now takes the key and computes real signature checks. Verified for real: bootstrapped a local `keys/gate.*.pem` via the actual `getOrCreateKeyPair('gate')` (not a mock), signed real fixture receipts with it, confirmed `/api/receipts` shows real `signature_valid: true/false`, then repeated the tamper test watching all four signals — tampered receipt's own signature flipped false while its chain-link stayed true, untouched receipt's signature stayed true while its chain-link flipped false. Exactly the Beat 7 distinction, now with real crypto both ways. Deleted the bootstrapped key + fixtures afterward. `tsc --noEmit` clean on both `dashboard/` and root; `npm test` 45/45 (Agent A's new tests included).
- 2026-07-29 (later) — Pulled Agent A's Phase 1c provisioning work + ADR-005 (Phase 1c reassigned to them). The user then shared the same real Dodo test-mode credentials directly with this session. Populated `.env` + `dashboard/.env.local` (read-only key + entitlement id only) and tested `dashboard/lib/dodo.ts`'s balance-resolution chain against the real account — found and fixed a real bug: the SDK client never set `environment: 'test_mode'`, so every call was silently hitting `https://live.dodopayments.com` (the SDK's default) and failing there with a generic 401, not the test host. Fixed by passing `environment: 'test_mode'` explicitly. After the fix, verified both resolution paths for real through the actual `/api/mandate` route: a fixture mandate pointing at Agent A's real paid checkout session (`cks_0NkBw28CUxmbI2KsSdVFu`) correctly walked the 2-hop chain to the real customer and returned `balanceInr: 1000`; a second fixture with the customer id directly (`cus_0NkBwH3N9Ld41wgNzK6ty`) returned the same. Confirmed visually in Chrome — the mandate page's balance card showed real "₹1,000", zero console errors. Flagged the `environment: 'test_mode'` bug prominently for Agent A below since it's a generic SDK-client gotcha they'll hit too when writing `DodoCreditLedger.ts`. Deleted diagnostic scripts and test fixtures afterward. Full writeup: `docs/OUTCOME.md` Phase 1h addendum.
- 2026-07-29 (later still) — The user directly instructed this session to implement `src/ledger/DodoCreditLedger.ts`. Checked first: still a comment stub on this machine, Agent A hadn't started it. Wrote `src/ledger/Ledger.ts` (also still a stub, despite being marked "frozen") and `src/ledger/DodoCreditLedger.ts` for real — `fund()` creates a real checkout session with a `credits_amount` override matching the requested paise amount (can't complete payment itself, that's a prohibited agent action, matches the product's own "human signs" premise); `balance()`/`draw()`/`release()` fully real. Found a real bug in my own first test-script attempt (not the ledger): module-level `const X = process.env.Y!` gets evaluated at import time, before the test script's own `.env` loader ran, producing a confusing Dodo 422 instead of a clear error — fixed by reading env vars lazily via a `requireEnv()` helper at each call site instead. Ran the real integration test: `fund()` created a real session for ₹800; `balance()` read the real 100000-paise demo customer; `draw()` deducted ₹100 for real, confirmed exact; **calling `draw()` again with the identical `runId` did not double-deduct** — real, positive confirmation that Dodo's `idempotency_key` genuinely works, resolving a question left open since Phase 1d; `release()` confirmed as a no-op; credited the deducted amount back afterward, restoring the real demo account to its original balance. Added `DODO_TOPUP_PRODUCT_ID` to `.env`/`.env.example` (new var, needed by `fund()`). Full record: `docs/OUTCOME.md` Phase 1c follow-up, `02-DECISIONS.md` ADR-006. `tsc --noEmit` clean on the whole project throughout.
- 2026-07-29 (later still) — The user directly asked to resolve B-002. Root cause: webcmd's Cloak runtime is powered by `cloakbrowser`, a stealth-Chromium wrapper that needs its own separately-downloaded ~535MB binary — never installed on this machine (`cloakbrowser info` → `Installed: false`), and independently on Agent A's too, which is why the symptom matched on both. Installing it (`cloakbrowser install`) hit a second real bug twice: `Error: spawnSync powershell ENOENT`, because this sandboxed environment's `PATH` doesn't include `System32` at all — fixed by prepending the real path for that one command. After that, `webcmd doctor` passed for real ("Connectivity: connected in 3.1s") and a live browser command (`webcmd duckduckgo search`) returned real results. Verifying `src/webcmd/executor.ts`'s `execute()` itself against a live command then surfaced three more real bugs, invisible until now: `spawn('webcmd', args)` throws `ENOENT` on Windows (`.cmd` wrapper, no shell); the naive fix `{ shell: true }` trades that for a real command-injection risk (Node's own DEP0190 warning) — fixed properly by resolving webcmd's actual `.js` entry point and invoking it via `node` directly, no shell anywhere; and the real JSON output has no `{runId,columns,tracePath}` wrapper at all (bare array only) — `execute()` now generates its own real `runId` via `crypto.randomUUID()` instead of depending on an internal value webcmd never exposes. Confirmed `result.runId` was `undefined` before this fix and would have silently corrupted Agent A's `gate run` receipts (`execution.run_id`) — flagged prominently for them below. Full record: `docs/OUTCOME.md` Phase 1d addendum, `02-DECISIONS.md` ADR-007. `tsc --noEmit` and `npm test` (45/45) both clean throughout; all temp diagnostic scripts deleted after use.
- 2026-07-29 (later still) — Pulled Agent A's Phase 1g live-rehearsal commits. They independently reproduced B-002's exact symptom on their own machine (same missing-Chromium-binary root cause), then ran the actual rehearsal: Beats 1-4 for real (real Dodo checkout + real payment, real blinkit login/search/add-to-cart correctly treated as ₹0 spend, real cart read, real DENY on `OVER_PER_TXN_CAP`), finding and fixing 4 more real bugs along the way, one of which touches this track's files (`Ledger.fund()`'s return type additively widened to include an optional `checkoutUrl`, since a human needs the real Dodo checkout URL to actually pay — it was being silently discarded before). Re-verified on this machine: `tsc --noEmit` clean (root + `dashboard/`), `npm test` 45/45. Fixed a real documentation gap this track owns: `03-INTERFACES.md`'s `Ledger` row still said "not yet wired" and didn't mention `checkoutUrl` at all. Synced the shared tables in `01-PROJECT-STATUS.md` (phase 1g row, sync points 5/7, "at a glance") to reflect Beats 1-4 being real and Beats 5-8 (real merchant purchase) being an explicit, not-yet-authorized decision point rather than a code gap.
- 2026-07-29 (later still) — Ran Phase 1h's last acceptance item for real: `npm run build && npm run start` in `dashboard/`, confirmed `HTTP 200`. Ran real `gate` CLI commands (`scan`, `mandate create`, `run -- webcmd duckduckgo search`) while it was up — `/api/events` returned `[]`, and `events.jsonl` didn't exist on disk at all, despite the CLI printing a real `ALLOW` line. Root cause: `cmdRun()` in `src/cli/gate.ts` builds a valid `GateEvent` and passes it to `formatGateEventLine()` for the terminal UI, but nothing ever appended it anywhere — this bug was invisible until now because every prior dashboard verification used a fixture-generator script, never a real CLI run (which wasn't possible on this machine until B-002 cleared). Fixed directly in Agent A's `gate.ts`/`store.ts`: added `appendEvent()` to `store.ts`, called at both event-construction sites in `gate.ts` alongside the existing print. Re-ran the same commands — `events.jsonl` gained real lines, `/api/events` reflected them, confirmed live in a browser tab too. Then found the dashboard's PID (`Get-NetTCPConnection -LocalPort 3000 -State Listen`), killed it (`Stop-Process -Force`), confirmed it was actually dead (`curl` → connection refused), and ran three more real CLI commands (`scan`, another `run`, `mandate resign`) — all succeeded identically, `events.jsonl` kept growing, nothing about the CLI path referenced the dashboard at all. `tsc --noEmit` clean (root + `dashboard/`), `npm test` 45/45, no regressions. Full record: `docs/OUTCOME.md` Phase 1h addendum, `02-DECISIONS.md` ADR-008. Phase 1h now has zero open items.
- 2026-07-29 (later still) — Resolved `tracePath`/`trace_digest`, open since ADR-007. Read webcmd's own installed source directly (`dist/src/observation/artifact.js`, `dist/src/execution.js`) rather than guessing: the real trace directory is `~/.webcmd/profiles/<contextId>/traces/<traceId>/` (`trace.jsonl`, `network.jsonl`, `console.jsonl`, `summary.md`, `receipt.json`, `screenshots/`, `state/`) — but the more important finding was that `execution.js` only exports a trace artifact on **success** when the mode is `on`; under `retain-on-failure` (what `execute()` had always used), it only exports on **failure**. Since a `Receipt` is only ever built after `execute()` succeeds, `trace_digest` was structurally guaranteed to always be empty — confirmed empirically (a real successful search under `retain-on-failure` produced zero trace directories; a real failure produced one, with webcmd printing its own path in the error output). Fixed by switching to `--trace on`; confirmed webcmd prints `Webcmd trace artifact: <dir>` to **stderr** on success (stdout stays the unwrapped array, unaffected — ADR-007's finding still holds). `execute()` now parses that line, reads `<dir>/trace.jsonl`, and returns a real `sha256:<hex>` digest as a new, additive `traceDigest` field; `gate.ts`'s receipt-building now reads it instead of hardcoding `''`. Verified directly via a temporary script (`execute()` called standalone, deleted after use) and via the CLI's non-commit write path (`add-to-cart`) — not yet through an actual commit/`place-order` receipt, since that needs the same Beats 5-8 authorization. `tsc --noEmit` clean, `npm test` 45/45. Full record: `docs/OUTCOME.md` Phase 1d addendum, `02-DECISIONS.md` ADR-009.

## Next steps

1. All three owned phases (1c, 1d, 1h) are fully done with no open blockers or open items on any of them. Phase 1g Beats 1-4 are real (Agent A's machine) — Beats 5-8 are waiting on the user's go-ahead to spend real money on a real merchant purchase, not on either agent's code. Don't run that step without an explicit "yes, do it" in chat.
2. Once a real receipt exists from an actual Beats 5-8 run (whenever the user authorizes it), worth re-testing `/api/receipts` and `/api/mandate` against that real file/`reserve.ref`, and confirming `trace_digest` shows up real and non-empty on that receipt too (this session only verified `execute()`'s own output and the non-commit write path, not an actual commit-path receipt).
3. Nothing else is currently a known open item on this track. If genuinely idle, the next highest-value thing is likely supporting Agent A/the user through the Phase 5 rehearsal itself rather than inventing new solo work.

## Known issues / rough edges

_(Running list of anything you're leaving unfinished, hacky, or worth a second look — not a bug tracker, just enough that the other agent (or you, next session) isn't surprised. Clear an item when it's fixed; move anything genuinely blocking into `docs/common/04-BLOCKERS.md` instead.)_

- `ManifestCommand` in `manifest.ts` only types the 3 fields (`site`, `name`, `access`) actually consumed — the real payload has 14 fields per command. If a future phase needs another field (e.g. `columns` for display), extend the interface then rather than speculatively now.
- Every real write command now leaves a ~1-2MB trace directory under `~/.webcmd/profiles/.../traces/` (a consequence of switching to `--trace on`, ADR-009) — webcmd's own retention/pruning handles cleanup, nothing for this project to manage, just worth knowing if disk usage ever looks surprising during a long rehearsal session.

## Notes for Agent A

_(Use this section to leave context Agent A should know but that doesn't rise to the level of a changelog entry or a blocker — e.g. "Dodo's real balance field turned out to be called X, here's the raw response" type notes, though the authoritative place for that is still docs/OUTCOME.md per CLAUDE.md's existing rule. If it's a real design decision with alternatives, it likely belongs in `docs/common/02-DECISIONS.md` instead — use this section for lighter background.)_

- The real webcmd manifest has 805 commands (228 write), not the spec doc's guessed ~302/~192 — if Phase 1f/1g's demo script assumes a specific count anywhere, it doesn't need to; nothing in the demo script actually hardcodes these numbers, just flagging in case.
- `ledger.jsonl`'s entry shape is now concrete (`{ runId, reserveRef, amountInrPaise, ts }`, see ADR-004) — when you wire `gate run`/`gate fund` in Phase 1f, call `hasAlreadyDrawn(runId)` from `src/webcmd/executor.ts` before `Ledger.draw()`, and `recordDraw(...)` after a successful draw.
- **RESOLVED 2026-07-29** — the gate-keypair gap flagged above is fixed by your `src/cli/keys.ts` (`keys/gate.public.pem`, read via `MANDATE_GATE_DATA_DIR`). Dashboard's `/receipts` route now computes real `signature_valid`, verified against a bootstrapped keypair via your actual `getOrCreateKeyPair()`. Thanks for the clean design — no new config surface needed, exactly as your comment promised.
- One thing worth double-checking on your side when `gate run` first produces a real receipt: my `verifySignature()` in `dashboard/lib/hash.ts` destructures `const { sig, ...unsigned } = receipt` and verifies `unsigned` against `sig` — this matches `verifyReceipt()` in `src/receipt/chain.ts` exactly (`const { sig, ...rest } = receipt; return verify(rest, sig, gatePublicKey)`), so it should agree, but I've only tested it against my own bootstrapped fixtures, not a receipt your CLI actually produced end to end.
- **⚠️ Direct request, 2026-07-29 (superseded a few hours later, see below) — leaving in place for the timeline.**
- **🚨 Environment bug, 2026-07-29:** the `DodoPayments` client defaults to `environment: 'live_mode'` when that option is omitted (confirmed in `node_modules/dodopayments/client.d.ts`'s own doc comment). Silently sends every call to the live host, failing there with a generic `401` that looks like a bad key. Fixed everywhere on my side (`dashboard/lib/dodo.ts`, `src/ledger/DodoCreditLedger.ts`) with `environment: 'test_mode'` passed explicitly. Full writeup: `docs/OUTCOME.md` Phase 1h addendum.
- Also verified for real (your Phase 1c account): both `balances.retrieve()` resolution paths — via your real paid checkout session `cks_0NkBw28CUxmbI2KsSdVFu` (2-hop chain) and directly via `cus_0NkBwH3N9Ld41wgNzK6ty` — return the real `balance: 100000` (confirmed as a JSON number, matching your FINDING A). Dashboard's `/api/mandate` now shows a real "₹1,000" balance card end to end.
- **🚨 `src/ledger/DodoCreditLedger.ts` implemented, 2026-07-29 (later still) — the user directly instructed this, not a unilateral reclaim.** Checked first that you hadn't started it (still a stub). `fund()`/`balance()`/`draw()`/`release()` are all real and tested against your Phase 1c account — full record in `docs/OUTCOME.md`'s Phase 1c follow-up entry and `02-DECISIONS.md` ADR-006. **Please read `DodoCreditLedger.ts`'s own header comment before wiring `gate run`/`gate fund` in `src/cli/gate.ts`** — it has every real-vs-spec divergence in one place (method names, the resolution chain, the balance type coercion, the environment default). Confirmed for real: Dodo's `idempotency_key` genuinely prevents a double `draw()` — you don't need to work around its absence, it's there and it works. If you'd already started writing this file independently before pulling, please treat it as a real conflict (per `06-SYNC-WORKFLOW.md`) rather than assuming either version just wins — I'd rather reconcile than have either of us silently overwrite real, tested work.
- **🚨🚨 CRITICAL — read this before your next `gate run`, 2026-07-29 (later still):** B-002 is resolved (root cause: `cloakbrowser`'s Chromium binary was never downloaded on either of our machines — see `docs/common/04-BLOCKERS.md`). Once webcmd could actually run a live command, I found `src/webcmd/executor.ts`'s `execute()` had a real bug that would have **silently corrupted your `gate run` receipts**: the real webcmd JSON output has no `{runId, columns, tracePath}` wrapper at all (bare array only), so `result.runId` was `undefined` before my fix — and your `cmdRun()` threads that value straight into `Ledger.draw()`, the idempotency ledger entry, and the signed `Receipt.execution.run_id`. All three would have silently received `undefined` in a real run, not thrown an error. Fixed: `execute()` now generates its own real, unique `runId` via `crypto.randomUUID()` before invoking webcmd at all — your `cmdRun()` code doesn't need to change, `result.runId` is just a real UUID now instead of `undefined`. Also fixed two Windows-specific process-spawning bugs in the same file (real `ENOENT`, and a `{ shell: true }` fix I rejected because it reintroduced a genuine command-injection risk — see `02-DECISIONS.md` ADR-007 for the full reasoning if you're curious). **Still open, not fixed:** `tracePath`/`evidence.trace_digest` — webcmd's real trace-artifact location on disk hasn't been found. Your existing `evidence: { trace_digest: '', ... }` placeholder in `gate.ts` is consistent with this, not something to "fix" on your side yet.
- **Nice catch on the `checkoutUrl` bug and the other 3 from the live rehearsal, 2026-07-29 (later still).** Pulled `5f906b2`/`79d9451` cleanly, `tsc --noEmit` and `npm test` (45/45) both still clean against the widened `Ledger.fund()` return type — no fallout on this side. One thing I fixed on your behalf since it's technically my file to maintain: `03-INTERFACES.md`'s `Ledger` row still said `fund()`'s call site was "not yet wired" and didn't mention `checkoutUrl` at all — updated it to reflect the real, additive change. Good call not running Beats 5-8 without checking first — agreed that's a real-money decision for the user, not something either of us should push forward solo just because the code path is ready. Whenever that goes ahead (solo or as part of Phase 5), happy to have the dashboard cross-checked against whatever real receipt/reserve it produces.
- **🚨 I edited `src/cli/gate.ts` and `src/cli/store.ts` directly, 2026-07-29 (later still) — please read this before your next `gate run`.** Doing Phase 1h's CLI-kill test for real (first time possible on this machine — needed both B-001 and B-002 resolved) surfaced a real bug: `cmdRun()` builds a full `GateEvent` at both the read short-circuit and the write-decision point, formats it for the terminal via `formatGateEventLine()`, but never persisted it anywhere — `events.jsonl` was never created by a real `gate run`. This silently breaks the dashboard's `/events` page during an actual demo (it reads exactly that file). I fixed it directly rather than only flagging it, since the change is small and purely additive: added `appendEvent(event: GateEvent)` to `store.ts` (same file-I/O pattern as `saveMandate`/`saveReceipt`), and both places in `cmdRun()` that build an `event` now call `appendEvent(event)` right alongside the existing print — nothing about `decide()`, the decision logic, or the printed terminal output changed. I also added `reserve_ref: mandate.reserve.ref` to the write-path event, since it was cheaply available there and `01-ARCHITECTURE.md` asks for every `GateEvent` field to be populated in Phase 1. Full reasoning for fixing vs. only flagging (given this is your file): `02-DECISIONS.md` ADR-008. **Deliberately left alone:** `run_id`/`trace_digest` still aren't set on the write-path event, since they're only known after `execute()` runs — i.e., after that event is already built and persisted. Backfilling those needs a real design call (second event? overwrite the first?) that felt too big to make inside an unrelated bug fix — over to you if/when it matters, or happy to think it through together. If you were mid-edit on `gate.ts` when this lands, please treat it as a real conflict per `06-SYNC-WORKFLOW.md` rather than assuming either version wins outright — this touches lines right next to your own Phase 1g work.
- **🚨 One more small edit to `src/cli/gate.ts`, same day — the `trace_digest` line.** Resolved the `tracePath`/`trace_digest` question that's been open since ADR-007 (full story: `02-DECISIONS.md` ADR-009). Short version: `execute()` in `src/webcmd/executor.ts` now passes `--trace on` instead of `--trace retain-on-failure` (the latter never traces a *successful* command, and your receipts only ever get built on success — the field was structurally guaranteed empty, not just unresolved), and returns a real `traceDigest` (a new, additive field on `ExecuteResult`). The one line I changed in your file: `evidence: { trace_digest: '', ... }` → `evidence: { trace_digest: result.traceDigest, ... }` in the commit-path receipt-building block — nothing else in `cmdRun()` touched. Not yet verified against an actual `place-order` receipt (needs Beats 5-8), only against `execute()` directly and the non-commit write path — worth a quick real check the first time a real commit-path receipt gets built, just to confirm the field shows up non-empty as expected. One real cost worth knowing about: every real write command now leaves a ~1-2MB trace directory under `~/.webcmd/profiles/.../traces/` (webcmd's own retention logic handles cleanup, nothing for either of us to manage) — small in absolute terms since `execute()` is never called for reads, but worth knowing if you notice new files accumulating there during rehearsal.
