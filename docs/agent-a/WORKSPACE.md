# Agent A — Workspace

Read `docs/common/00-START-HERE.md` before this file, every session, if you haven't already this session.

See also, in this same folder: `TASKS.md` (durable checklist for 0/1a/1b/1e/1f/1g), `ROADMAP.md` (milestones against the deadline, plus a critical-path-risk note on the two external blockers B-001/B-002), `ERROR-HANDLING.md` (failure modes specific to the mandate/policy/receipt/CLI track). Mirrors Agent B's equivalent files in `docs/agent-b/` for structural consistency between the two workspaces.

## Role

You own the **pure-logic-and-integration chain**: Mandate → Policy Engine → Receipts → CLI/UI → the end-to-end acceptance run. This is the sequential backbone of Phase 1 — each of your phases imports from the one before it, which is why it's one continuous track rather than split further. See `docs/common/05-PHASE-OWNERSHIP.md` for the full rationale, and `docs/common/02-DECISIONS.md` ADR-002 for why this split beats a 50/50 one.

**2026-07-29: you also now own Phase 1c (`src/ledger/`)**, reassigned from Agent B per direct user instruction — see ADR-005 for why (B-002 would keep blocking Agent B on webcmd execution even after B-001 clears; `src/ledger/` has zero webcmd dependency).

You also default-own **Phase 0** (whole-repo scaffolding) — see that file's Sync Point 1 for the exception case.

## Phases you own

| Phase | Status | Depends on |
|---|---|---|
| 0 — Scaffolding | ✅ Done, tests passing | none |
| 1a — Mandate schema, canonical JSON, Ed25519 signing | ✅ Done, tests passing | Phase 0 |
| 1b — Policy Engine `decide()` | ✅ Done, tests passing | 1a |
| 1e — Receipts and verify chain | ✅ Done, tests passing | 1a |
| **1c — Dodo Payments integration** | ❌ Blocked on B-001 (real Dodo account) | Phase 0. **Reassigned here 2026-07-29, ADR-005.** |
| 1f — CLI and two-pane UI | ⚠️ Done with deviations — see below | 1a, 1b, 1e (done), 1c (blocked — no cross-agent wait anymore, just your own sequencing); `gate run`/`gate fund` need 1c |
| 1g — Full end-to-end run | ❌ Blocked | 1f's remaining pieces, which need 1c (B-001) and B-002 (Agent B's webcmd connectivity) |

Plus: Phase 2-4 stub verification (done) and Phase 5 rehearsal are shared with Agent B — see `docs/common/05-PHASE-OWNERSHIP.md`.

## Folders/files you own

`src/mandate/`, `src/policy/`, `src/receipt/`, `src/cli/`, `src/events/` (frozen after Phase 0, see `docs/common/03-INTERFACES.md`), **`src/ledger/` (new, ADR-005)**, plus the repo-wide scaffolding from Phase 0 (`package.json`, `tsconfig.json`, `.env.example`, `src/phase2-4-stubs/*` stub contents).

Never edit `src/webcmd/` or `dashboard/` without flagging it first per `docs/common/06-SYNC-WORKFLOW.md` — those stay Agent B's.

## Current task

**Plan Phase 2 AND Phase 3 (dashboard visual overhaul + concept-preview layer) are both built.** `dashboard/DESIGN.md`'s "Ledger/Notarial" system covers all of it: shadcn/ui + Tailwind v4 tokens, Figtree/Geist typography, navbar+sidebar shell, all 3 real pages, and now all 3 concept pages with real mocked content (not placeholders anymore) — `/concept/compare` (marketplace comparison table, one real anchor: Blinkit's ₹476 matches the actual Beats 5-8 receipt), `/concept/rules` (shopping rule builder → local-state-only mandate preview), `/concept/timeline` (6-stage pipeline stepper wired to real `/api/events`/`/api/receipts` where data exists + a `recharts` bar chart blending real receipts with labeled sample projections). New dependency: `recharts@3.10.1` (v3, not v2 — v2 is unmaintained upstream). Full detail in the progress log entry below and `docs/OUTCOME.md`'s new Plan Phase 3 entry. Still operating inside the **scoped, temporary exception** from ADR-010: `dashboard/app/**/page.tsx`, `dashboard/components/**`, `dashboard/app/globals.css` are fair game; `dashboard/lib/*` and `dashboard/app/api/**` remain Agent B's, untouched this session.

Earlier history (kept for continuity): Phase 1c (Dodo Payments integration) was reassigned to you 2026-07-29 per ADR-005, completed, and Phase 1f/1g work (CLI, live rehearsal Beats 1-4) also completed — all real, all tested. See `docs/OUTCOME.md` for the full record.

## Status

✅ All of your original Phase 1 work (0, 1a, 1b, 1e, 1c, 1f, 1g Beats 1-4) is done and tested. ✅ **Plan Phase 2 AND Phase 3 both done** — all 3 real pages + all 3 concept pages built, `tsc`/`lint`/`build` all clean. Phase 2-4 stub verification done. See `docs/OUTCOME.md` and `docs/common/04-BLOCKERS.md` (no open blockers). **One open gap, not a blocker:** neither this session nor the prior one achieved real browser-based visual QA — this session's attempt found the Chrome extension itself not connected at all (Phase 2's session had a connected browser but a non-functional `resize_window`). Verified instead via `tsc`/`lint`/`build` + real `curl` checks against a real `npm run start` server — see progress log below.

## Progress log

_(Append new entries at the bottom, most recent last. Include date, what you did, what passed/failed, and anything the reader would need to resume mid-task.)_

- 2026-07-28 — Workspace file created as part of the parallel-development docs restructuring. No implementation work has started yet.
- 2026-07-28 — Phase 0 complete. Full `src/` tree scaffolded per `docs/01-ARCHITECTURE.md`, `tsc --noEmit` and `npm test` both pass clean. Found and fixed two toolchain issues (TS 7 incompatible with `ts-node`, pinned to 5.9.3; `node --test <dir>` fails on this Node version, dropped the path arg) and two spec gaps (`DenyCode` was missing `ALREADY_EXECUTED`; `03-INTERFACES.md`'s `Ledger` ownership row was wrong). Full detail in `docs/OUTCOME.md` Phase 0 entry and `docs/common/08-CHANGELOG.md`. Committed and pushed (`46f8514`).
- 2026-07-29 — Phase 1a complete. `src/mandate/schema.ts` (`Mandate` + `isMandate()` guard), `sign.ts` (`canonicalJSON`/`generateKeyPair`/`sign`/`verify`, matches the spec exactly), `render.ts` (`renderConsent()`, deviated from the spec's plain `.join(', ')` sketch — see below). 9 real tests across `sign.test.ts` (5) and `render.test.ts` (4, added after finding bugs), all passing; `tsc --noEmit` clean. Found and fixed two more real bugs by smoke-testing `renderConsent()` against Beat 2's exact expected sentence: the spec's plain comma-join doesn't match Beat 2's grammatical "a, b or c" wording, and a naive `capitalize()` can't turn `bigbasket` into `BigBasket`. Full detail in `docs/OUTCOME.md` Phase 1a entry. Committed and pushed (`b8e9882`).
- 2026-07-29 — Phase 1b complete. `src/policy/decide.ts` + `decide.test.ts` (11 tests). Found a real, significant rule-order bug: the spec's decide() checked signature/expiry *before* the read-access short-circuit, which would deny a read against an expired/badly-signed mandate — contradicting both `docs/03-WEBCMD-INTEGRATION.md`'s explicit "no signature verification" for reads and the required test's own title ("read access always allows, regardless of mandate state"). Moved read-access to Rule 0. Wrote up the full reasoning as `docs/common/02-DECISIONS.md` ADR-003 (not just a changelog note — this changes a contract). Also widened `SpendRequest.access` to include `undefined` (the original `'read'|'write'`-only type made the `UNKNOWN_COMMAND` check unreachable dead code). Updated `docs/04-POLICY-ENGINE-SPEC.md` to match, per CLAUDE.md's rule-order-change requirement. 20/20 tests passing, `tsc --noEmit` clean. Did NOT implement `rules.ts` — not required by the Phase 1b prompt, left as its Phase 0 stub. Full detail in `docs/OUTCOME.md` Phase 1b entry. Committed, pushed — hit a real merge conflict in `08-CHANGELOG.md` with Agent B (who'd pushed their own Phase 0 sync entry in the meantime), resolved per `06-SYNC-WORKFLOW.md`'s append-only-conflict protocol (kept both entries, ordered by push order). Rebased cleanly, re-verified `tsc --noEmit`/`npm test` after, pushed (`37bf928`).
- 2026-07-29 — Phase 1e complete. `src/receipt/schema.ts` (`Receipt`) and `chain.ts` (`buildAndSignReceipt()`, `verifyReceipt()` from the spec exactly, plus `sha256Hex()`/`verifyChain()` — the spec describes these in prose, no code shown, so their shape here is this phase's own design). 4 tests, all passing, no spec bugs found this time. Also synced `01-PROJECT-STATUS.md`'s 1c/1d table rows to match what Agent B had already written in their own status section (table was getting stale — I didn't invent anything, just propagated). Full detail in `docs/OUTCOME.md` Phase 1e entry. Committed, pushed (`e9467d8`).
- 2026-07-29 — Pulled and read Agent B's Phase 1c/1d/1h progress in full (`01-PROJECT-STATUS.md`, `02-DECISIONS.md` ADR-004, `04-BLOCKERS.md` B-001/B-002, `docs/agent-b/WORKSPACE.md`). Per the user's request, added `TASKS.md`/`ROADMAP.md`/`ERROR-HANDLING.md` to this folder, mirroring Agent B's structure. Then built Phase 1f as far as possible without Agent B's Phase 1c: `src/cli/gate.ts`, `ui.ts`, `store.ts`, `keys.ts` (resolves the gate-keypair-location gap Agent B flagged), plus `src/mandate/id.ts`/`did.ts`/`currency.ts`. Ran every implemented subcommand for real (not just unit tests) — `mandate create`, `mandate resign`, `receipt show`, `verify` (full Beat 7 tamper test via real `sed -i`), `scan` (against a local fixture, since this machine has no `webcmd` install). Found and fixed 3 more real bugs while doing this: `renderConsent()`'s time format (a Phase 1a bug, "06:00 pm" not "6:00 PM"), missing INR comma-grouping, and a real Phase-0 `.gitignore` mistake (`manifest.json` was ignored, which meant `gate scan` could never be tested against real data on this machine — un-ignored it, flagged for Agent B to push their real one). `gate run`/`gate fund` left as honest blocked-message stubs — genuinely can't be built until `Ledger.ts` is real. 45/45 tests passing, `tsc --noEmit` clean. Full detail in `docs/OUTCOME.md` Phase 1f entry and a long changelog entry addressing Agent B's two open questions directly. Committed, pushed.
- 2026-07-29 — Per the user, added the `dodo-knowledge` MCP server (project-scoped, `.mcp.json`, committed) — needs a session restart to connect, not usable yet. Pulled Agent B's follow-up work: real `manifest.json` committed (805 commands, 228 write — `gate scan` can now be tested against real data), and `signature_valid` wired up for real on the dashboard using `src/cli/keys.ts`. Both pulled cleanly, 45/45 tests still pass. Ran Phase 2-4 stub verification (per `docs/PROMPTS.md`) before Agent B got to it, since both of us were independently circling it — all 4 stub files already correct, nothing to fix. Both tracks' remaining real work (my `gate run`/`gate fund`/1g, Agent B's 1c/`execute()`) now genuinely blocked on the same two external prerequisites (B-001, B-002) — flagging to the user directly since neither agent can resolve either one.
- 2026-07-29 — Per direct user instruction, took over Phase 1c (Dodo Payments integration) from Agent B. Wrote up the reassignment as `docs/common/02-DECISIONS.md` ADR-005 (why: B-002 would keep blocking Agent B on webcmd execution even after B-001 clears; `src/ledger/` has zero webcmd dependency, so it can unblock independently). Updated `05-PHASE-OWNERSHIP.md`, `03-INTERFACES.md`'s `Ledger` row, `01-PROJECT-STATUS.md`, and `04-BLOCKERS.md`'s B-001 entry to all point at the new owner, with an explicit note for Agent B not to start `src/ledger/` work even if they separately get Dodo access. User then shared a screenshot of their real Dodo test-mode account (Settings → Promotions) showing a "$1,000 fee waiver" promo — confirmed this is a transaction-fee waiver on Dodo's own processing fees, NOT spendable credit, and doesn't complete B-001's checklist (still need real `DODO_API_KEY`/`DODO_API_KEY_READONLY`/a Product/its Credit Entitlement ID). Recorded the finding in `docs/OUTCOME.md`'s open-questions table (this directly answered a question that had been open since the original spec). Good news: confirms the account itself is real and in Test Mode. Asked the user directly, in chat, for the remaining concrete setup steps.
- 2026-07-29 (later still) — B-001 cleared, Phase 1c/1f/1g Beats 1-4 all completed for real in the sessions between the previous entry and this one (full detail already in `docs/OUTCOME.md` and the changelog — not re-summarized here to avoid drift between the two logs). **New assignment, same day, per direct user instruction:** all original Phase 1 work is done, so Plan Phase 2 (dashboard visual overhaul) and Phase 3 (concept-preview layer) are now this track's active work — see `docs/common/09-HACKATHON-WOW-PLAN.md` and ADR-010. Before writing any component code, wrote `dashboard/DESIGN.md` per the user's explicit request — a full design system ("Ledger / Notarial" style: light/paper palette, Fraunces serif for headings/money + existing Geist Sans/Mono kept for UI/data, shadcn/ui as the component layer, hairline-border elevation with no drop shadows, small radii, a single reserved oxide accent color kept outside the ALLOW/DENY/STEP_UP trio, navbar+sidebar shell with real pages visually separated from Phase 3's "Concept Preview" pages), built using the `frontend-design` skill's process. Explicitly encodes the user's ask to avoid generic AI-dashboard tropes (banned hover-scale/translate, gradient hero sections, oversized rounded corners, shadow-heavy floating cards) with concrete replacements (color-only hover, functional data-driven motion — the receipt chain visibly breaking on tamper being the one deliberate "big" animation moment). Logged planned package additions (`shadcn/ui`, `lucide-react`, `sonner` now; `recharts` deferred to Phase 3) with rationale, per `CLAUDE.md` rule 5. Left 4 open questions in `DESIGN.md` for the user to confirm before writing implementation code (font choice, accent color, package scope, concept-page nav separation) — no component implementation started yet.
- 2026-07-29 (later still) — User confirmed all 4 `DESIGN.md` open questions (Figtree instead of Fraunces as the display face — corrected the doc's serif framing to match, since Figtree is actually a sans; oxide accent; `lucide-react`+`sonner` now, `recharts` deferred; concept-nav separation), then said to build Phase 2 for real, "act like a senior full-stack Next.js developer," with good folder structure and responsive Tailwind. Built it:
  - `npx shadcn@4.16.0 init` (Nova preset, Radix base) + `add` for card/badge/separator/tooltip/sheet/tabs/skeleton/sidebar/sonner/scroll-area. Rewrote `app/globals.css`'s token block to the Ledger/Notarial palette (paper/ink/hairline/oxide + domain tokens `--allow`/`--deny`/`--step-up`/`--seal`), deleted the auto-dark `@media` query and the generated `.dark` block entirely (light-only, per direct instruction — no theme toggle exists to ever apply a `.dark` class anyway). Added Figtree via `next/font/google` alongside the existing Geist Sans/Mono, wired `--font-heading` to it.
  - New folder structure under `components/`: `layout/` (`app-sidebar.tsx`, `app-navbar.tsx`, `sidebar-balance-stat.tsx`, `page-header.tsx`), `mandate/` (`mandate-hero.tsx`, `expiry-ring.tsx`, `reserve-balance-gauge.tsx`, `merchant-chip.tsx`), `events/` (`event-table.tsx`, `event-row.tsx`, `verdict-badge.tsx`), `receipts/` (`receipt-chain.tsx`, `receipt-entry.tsx`), `shared/` (`panel.tsx`, `empty-state.tsx`, `test-mode-badge.tsx`, `concept-preview-badge.tsx`, `concept-placeholder.tsx`), plus `hooks/use-polling.ts` (two small shared hooks — `usePolledFetch`/`useIncrementalPoll` — deduping the near-identical poll-on-interval logic that used to be copy-pasted across the three page files). Deleted the four old flat components (`StatusBadge.tsx`, `EventRow.tsx`, `ReceiptCard.tsx`, `ReserveBalanceCard.tsx`), fully superseded.
  - Rebuilt all 3 real pages (`app/page.tsx`, `app/events/page.tsx`, `app/receipts/page.tsx`) on the new components — same API contracts, same data, new presentation only. Added a `sonner` toast on every new DENY event (detected via a ref-tracked delta against the incremental poll, not a separate data source).
  - Added `/concept/compare`, `/concept/rules`, `/concept/timeline` — the sidebar already linked to these for the nav-separation requirement, so left as honest "Coming in Phase 3" placeholders (via a shared `ConceptPlaceholder`/`ConceptPreviewBadge`) rather than shipping 404s; the actual mocked content (comparison table, rule-builder form, timeline chart) is genuinely not built yet.
  - **Real bug found and fixed while visually verifying with fixture data** (real signed receipts generated via the actual `buildAndSignReceipt()`, not hand-typed JSON, deleted after use): my first pass at `receipt-entry.tsx`'s status logic collapsed two different failure modes into one "Tampered" badge — a receipt's own signature being wrong (it was directly edited) vs. a receipt being unable to prove continuity from its predecessor (the *predecessor* was edited instead). This is the exact distinction `docs/05-DEMO-SCRIPT.md` Beat 7 and the root test suite's own test title ("editing any field in the first receipt breaks the SECOND receipt's chain link, not just the first receipt's own signature") are built to demonstrate — collapsing it would have undercut the single most demo-able moment in the redesign. Fixed: `overallStatus()` now returns "Tampered" only when `signature_valid === false`, and a distinct "Chain broken" (different icon, same deny color) when `signature_valid === true` but `chain_link_valid === false`. Verified live: tampering the middle of 3 real fixture receipts flips that one to "Tampered" and the next one (chronologically) to "Chain broken," with the connecting line between them turning from a solid ink line to a dashed deny-red line — confirmed within one poll cycle, no manual refresh.
  - **Real tooling gotcha, worth remembering:** started the dashboard on port 3000 by default and got real-looking `200`/`307` responses for a while before noticing they were coming from a completely unrelated pre-existing server (`E:\Products\Job Pilot\jobpilot-dashboard`, already bound to 3000) redirecting to `/login` — not this app at all. Always confirm the actual process (`Get-CimInstance Win32_Process -Filter "ProcessId=..."`) before trusting a curl 200 on a shared machine; now runs on an explicit `PORT=3100`.
  - **Real tooling limitation, also worth remembering:** `mcp__claude-in-chrome__resize_window` did not actually change this tab's viewport in this session — confirmed via `window.innerWidth` staying at 1536 regardless of the requested size. Responsive behavior (sidebar → icon rail → `Sheet`, table → stacked cards) is implemented via standard Tailwind breakpoints and shadcn's own `useIsMobile()`/`Sheet`, not custom logic, but was **not** independently screenshot-verified at those widths this session — flagged in `09-HACKATHON-WOW-PLAN.md` as a known gap, worth a real check on an actual small window before the live demo.
  - Verified throughout: `npx tsc --noEmit` clean, `npm run lint` clean (fixed 2 real `react-hooks/set-state-in-effect` findings — one in my own `expiry-ring.tsx`, one in shadcn's own generated `hooks/use-mobile.ts` — both legitimate SSR-safe-deferral patterns, not real bugs, documented inline rather than silently suppressed), `npm run build` clean (all 9→12 routes, including the 3 new concept routes). Root `src/` re-verified unaffected (`npx tsc --noEmit` clean, `npm test` 45/45) after generating/deleting the visual-QA fixtures via a temporary root-level script (deleted, not committed — same discipline as every prior fixture-generation pass in this build).
- 2026-07-29 (later still) — Plan Phase 3: built real mocked content for all 3 concept pages, replacing the placeholder-only routes from the previous entry. Loaded `frontend-design` (Step 0: found `DESIGN.md` already established, extended it rather than reinventing) and `dataviz` (for the timeline chart's mark/color choices) skills first, then read every existing component this work would touch or echo (`panel.tsx`, `merchant-chip.tsx`, `verdict-badge.tsx`, `mandate-hero.tsx`, `expiry-ring.tsx`, `page-header.tsx`, `reserve-balance-gauge.tsx`, `event-table.tsx`, `receipt-entry.tsx`, `use-polling.ts`) to match established conventions exactly rather than approximating them.
  - Installed `recharts@3.10.1` (v3, not the v2 `DESIGN.md` had loosely scoped — v1/v2 are no longer actively maintained upstream per npm's own deprecation notice, and since this was a fresh install with zero existing v2 code to preserve, v3 was the correct target). Confirmed clean against React 19.2.4.
  - `/concept/compare`: new `components/concept/marketplace-comparison-table.tsx` — Blinkit/Zepto/BigBasket/Instamart compared for the real Beats 5-8 SKU ("Aashirvaad Atta 5kg ×2"), with Blinkit's final cost (₹476) deliberately kept as a real anchor matching the actual signed receipt; the other three merchants' numbers are representative, hand-picked for a realistic non-rigged spread. Cheapest option flagged "Best value" using the oxide accent (the system's one-accent-per-page rule).
  - `/concept/rules`: new `components/concept/{rule-builder-form,merchant-toggle,mandate-preview-panel}.tsx` — a form (product/target price/budget/merchant toggles restricted to this system's actual 4 supported merchants, not the compare page's illustrative Instamart/expiry/purchase limit/manual-vs-auto-buy toggle defaulting to manual) that renders a **dashed-border** preview panel on submit — deliberately distinct from the real Mandate hero's solid border, shaped like the real `Mandate` type's fields, entirely local `useState`, no `fetch` call anywhere in the submit path (satisfies the read-only rule by construction).
  - `/concept/timeline`: new `components/concept/{pipeline-stepper,checkout-activity-chart}.tsx` — a 6-stage stepper (Search/Compare/Checkout always concept-only; Mandate approval/Payment/Receipt sourced from the real `/api/events`/`/api/receipts` routes via the pre-existing polling hooks, zero new API routes added) with every stage tagged "Real" or "Sample" via icon+text+color. A `recharts` bar chart blends real receipt totals with 3 labeled sample projections — real bars use the existing `--color-allow` token, sample bars use a diagonal SVG hatch texture (the `dataviz` skill's CVD-safe secondary-encoding guidance) rather than just a lighter tint. Handles the real 0-receipts-on-this-machine case gracefully (confirmed via `ls receipts/`) with an honest inline count rather than an empty-looking chart.
  - Deleted `components/shared/concept-placeholder.tsx` — confirmed via `grep -rl "ConceptPlaceholder" app components` that nothing referenced it anymore once all 3 pages had real content, then removed it (fully superseded, not left as an orphaned stub).
  - **Real tooling gap found this session, one step past the previous session's:** attempted the same real-browser visual QA used throughout this build; `mcp__claude-in-chrome__tabs_context_mcp` reported the Chrome extension itself not connected (Phase 2's session at least had a connected browser, just a `resize_window` that didn't resize). Fell back to the next-best real verification: started the actual production server (`PORT=3100 npm run start`), `curl`'d all 6 real routes (all `200`), grep-checked the 3 new pages' real server-rendered HTML for expected content (e.g. `/concept/compare`'s HTML genuinely contains "₹476" and every merchant name), and confirmed a clean server log with no runtime errors across any request — including the timeline page's two live polling calls and its `recharts`/`ResponsiveContainer` client component, the piece most likely to throw during SSR if something were wrong. This confirms correct real markup and no server-side crash, but not pixel-level layout or the responsive breakpoints `DESIGN.md` calls for — flagged, not glossed over.
  - Verified throughout: `npx tsc --noEmit` clean, `npm run lint` clean (zero new findings), `npm run build` clean (all 12 routes). Root `src/` re-confirmed unaffected: `npx tsc --noEmit` clean, `npm test` 45/45. Full detail: `docs/OUTCOME.md`'s new Plan Phase 3 entry.

## Next steps

1. **Real browser visual QA is still owed** — two sessions running now, neither achieved it (Phase 2: connected browser, broken `resize_window`; Phase 3: browser extension itself not connected). Worth a real pass — including the narrow-viewport responsive check — on whichever machine has working Chrome extension access, before the live demo.
2. Once Agent B's Plan Phase 1 produces a real receipt/event from an actual `gate run` on a machine this dashboard points at, worth a quick pass confirming both the real pages AND the new `/concept/timeline` stepper/chart pick it up correctly (the timeline page's real-data wiring has never been exercised against actual non-empty `/api/receipts` data — only the honest empty-state path was confirmed this session).
3. Consider building the "pipeline" component (mandate → funded → decided → executed → receipted) from the original Phase 2 checklist — still deferred, not started, see `09-HACKATHON-WOW-PLAN.md`.
4. Re-run the dashboard-killed-mid-`gate-run` resilience check now that Phase 2 AND 3's visual changes have both landed, once Agent B has a live CLI session to test against.

## Known issues / rough edges

_(Running list of anything you're leaving unfinished, hacky, or worth a second look — not a bug tracker, just enough that the other agent (or you, next session) isn't surprised. Clear an item when it's fixed; move anything genuinely blocking into `docs/common/04-BLOCKERS.md` instead.)_

- TypeScript is pinned to `5.9.3`, not the `^7.x` `npm install typescript` would resolve to today, because `ts-node@10.9.2` doesn't support TS 7's new native compiler yet (`ts.sys` is undefined). If `ts-node` ships TS 7 support later, this pin can be revisited — not urgent, current setup works cleanly.
- `render.ts`'s `BRAND_NAMES` lookup only covers `blinkit`/`zepto`/`bigbasket`/`district`. A merchant added later that isn't in this table and has an internal capital in its brand name (like `bigbasket` did) will render wrong via the `capitalize()` fallback. Not a blocker — just remember to extend the table if the demo ever adds a merchant.
- `src/policy/rules.ts` is still just its Phase 0 comment stub — not populated in Phase 1b, since nothing currently needs the "rule table as data" decomposition `01-ARCHITECTURE.md` describes for it. If Phase 1f's CLI later wants a friendlier rule-name lookup for display, that's the natural point to fill it in.
- `src/cli/ui.ts` is unit-tested with sample data but never exercised against a real terminal — its actual caller (`gate run`) isn't wired yet. Legibility/column-alignment in a real terminal is unconfirmed until then.
- `gate verify`'s tamper message is generic ("receipt tampered") rather than naming the specific field like Beat 7's illustrative text — accurately identifying the field would need a stored copy of the original to diff against, which nothing in this system keeps. Not going to fake a field name to look more precise than it is.
- `/concept/timeline`'s real-data wiring (Mandate approval/Payment/Receipt stages, the chart's real bars) has only been confirmed against the honest empty-state case (0 receipts on this machine) — never against actual non-empty `/api/receipts`/`/api/events` data, since none exists locally. Should just work (same hooks, same response shapes the real pages already consume), but "should" isn't "confirmed" until it's seen with real data present.
- No real-browser visual QA this session (Chrome extension not connected) or last (connected but `resize_window` didn't work) — see "Next steps" above. Everything is verified via `tsc`/`lint`/`build`/`curl`-against-a-real-server, which confirms correctness of markup and absence of crashes, not actual visual layout.

## Notes for Agent B

_(Use this section to leave context Agent B should know but that doesn't rise to the level of a changelog entry or a blocker — e.g. "I picked a ULID library approach for mandate_id, here's why" type notes. If it's a real design decision with alternatives, it likely belongs in `docs/common/02-DECISIONS.md` instead — use this section for lighter background.)_

- The main asks are in the Phase 1f changelog entry (`08-CHANGELOG.md`) — the gate public-key location answer, and the request to push your real `manifest.json`. Keeping this section short since that entry already covers it in full; didn't want to duplicate.
- Added a `dodo-knowledge` MCP server (project-scoped, `.mcp.json`, committed) — Dodo's own docs-search MCP, no API key needed. Needs a session restart to connect. See `08-CHANGELOG.md` for the add command and the note about a *second*, credential-requiring Dodo MCP worth adding once B-001 clears.
- **Reply to your ADR-013 changelog entry's ask for my input on the cart-total under-reporting bug** (I did not implement anything here — no `webcmd` install on this machine either, and I don't have a live case to verify against, so per `CLAUDE.md` rule 6 I'm giving an opinion, not code): "prefer the larger of cart/checkout" (option 1) doesn't actually fix *this specific* incident — both readings agreed at ₹20 while the real total was ₹55, so max-of-two-equally-wrong numbers changes nothing; worth ruling out explicitly before spending time on it. Between your other two options, "parse the real grand total from the checkout page" (option 2) is the more correct fix in principle (matches `03-WEBCMD-INTEGRATION.md`'s "authoritative total" premise directly), but only if webcmd's checkout flow actually exposes a page/field with the true delivery+handling total somewhere — worth checking the manifest for a further-along checkout step before assuming it's there. If it isn't, "treat a fee-bearing checkout as a step-up trigger" (option 3) is the one I'd lean toward as a fail-closed-flavored fallback: it doesn't require trusting any total at all for the risky case, it just declines to auto-approve when the known-unreliable condition (a cart below the free-delivery threshold, i.e. exactly where fees attach) is present — consistent with "fail closed, always" rather than trying to compute a number that's been shown twice now to be wrong. This is a real design decision either way, not something either of us should guess at solo — flagging my reasoning, not asserting an answer.
