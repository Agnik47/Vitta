# 01 — Project Status (Live)

The current state of the world, at a glance. Update your own section every time you sync (see `00-START-HERE.md`). Never edit the other agent's section — if it's stale or wrong, that's a signal for them to fix at their next sync, or a `04-BLOCKERS.md` entry if it's actively blocking you.

**Last updated:** 2026-07-30 (later still), Agent A — **⚠️ MAJOR ARCHITECTURE CHANGE: the dashboard is no longer read-only.** Direct, explicit, twice-confirmed user override of `CLAUDE.md` rule 8 — see `02-DECISIONS.md` ADR-015 for the full record. The dashboard is becoming a full shopping-flow product (search/compare mock catalog, cart, checkout with mandate selection, real execute via a spawned `gate` CLI for Blinkit items). The 3 existing GET routes (`/api/mandate`, `/api/events`, `/api/receipts`) are untouched; new write routes are additive files alongside them. **Agent B: if you're mid-session on `dashboard/`, read ADR-015 before continuing** — the GET-only assumption your existing routes were built under no longer holds project-wide, even though your own files aren't directly edited by this change. In progress now: mock dataset, `/shop` search+compare+cart+checkout UI, write API routes wrapping the real CLI, real Blinkit add-to-cart wiring. No real-money execution will run without a specific live confirmation in chat first, same discipline as every prior real purchase in this build.

**Earlier, 2026-07-30 (same day), Agent B — LIVE END-TO-END DENY TEST PASSED on this Mac.** User provided `.env` credentials and authorized proceeding after the initial commit. Installed webcmd + cloakbrowser (same B-002 pattern as Windows, cleanly resolved on macOS), user completed real Blinkit OTP login, cart loaded to ₹295 (Pillsbury 5kg atta). Real `gate run` against a ₹250 cap → real `DENY OVER_PER_TXN_CAP · ₹295 · over by ₹45`, real Dodo balance unchanged, real event logged with `amount_inr: 295`. **This proves ADR-013 follow-up 1 fixed at the LIVE level, not just unit-test level** — decide() saw the real merchant total, cap tripped correctly, fail-closed intact. What's NOT yet proven live: the ALLOW-then-execute branch (would need a real ₹295 purchase). Full record: `docs/OUTCOME.md` Phase 1g addendum 3, LIVE section.

**Earlier this day:** ADR-014 code + unit-test level fix committed. `resolveCartTotalInr()` in `src/webcmd/cart-total.ts` returns MAX of every price-shaped field webcmd surfaces. Test suite 65/65; `tsc --noEmit` clean root + dashboard.

**Previously (last updated 2026-07-29):** Agent A — Plan Phase 3 dashboard concept-preview layer done. Agent B's cart-total bug was the last correctness item open on Phase 1. See `08-CHANGELOG.md`'s latest entries and `docs/OUTCOME.md`'s Phase 1g addendum 2 and new Plan Phase 3 entry. **Agent B → real Beats 5-8 rehearsal (Plan Phase 1, substantively done, one correctness item open). Agent A → dashboard visual overhaul + concept layer, presentation-only (Plan Phase 2/3, both now done — reassigned from Agent B's standing `dashboard/` ownership for this stretch).**

---

## At a glance

- **Completed:** Phase 0, 1a, 1b, 1e, 1f (all Agent A), Phase 1c (real provisioning by Agent A + real implementation by Agent B, ADR-006), Phase 1d and Phase 1h (both Agent B, fully done, no open items), Phase 2-4 stub verification (Agent A). **Every phase through 1f is now real, tested, and unblocked. Both B-001 and B-002 are resolved — no open blockers anywhere.**
- **Active:** Phase 1g (full end-to-end run) — **Beats 1-8 all real now.** Beats 1-4 (prior session, Agent A): `gate scan`, `gate mandate create`, `gate fund` (real Dodo checkout **and paid**), real blinkit login/search/add-to-cart (₹0, correctly untouched), real cart read, real DENY (`OVER_PER_TXN_CAP`). Beats 5-8 (this session, Agent B, user-authorized): real `place-order` (₹476, 2× Aashirvaad Atta 5kg), real signed receipt `rcp_ms66xl2ef9771fa00056`, real `gate verify` (both before and after a real tamper test — CLI and dashboard `/api/receipts` both flipped correctly), real idempotency retry (`DENY TXN_LIMIT_REACHED` rather than the script's illustrative `ALREADY_EXECUTED`, since this mandate's `max_txns: 1` was already used — the actual no-double-charge property confirmed regardless; the `ALREADY_EXECUTED` guard itself verified directly via `hasAlreadyDrawn()`). One real bug found+fixed: `network_order_id` was hardcoded `undefined` despite webcmd's real output carrying it — ADR-011. Full detail: `docs/OUTCOME.md` Phase 1g addendum.
- **Fallback video: done** — `demo/mandate-gate-fallback-2026-07-29.mp4` (4m32s, 1080p, committed to the repo so it survives a single machine failing). Per the user's explicit choice, it's a **narrated replay** of the real run's captured output, not a live screen capture (the alternative needed a second real purchase). Every line of terminal output in it is real and verbatim; the video's own title card and `demo/README.md` both say plainly that it's a replay — this project's pitch is that it doesn't fake what matters, so the artifact shouldn't either.
- **Timed run: done — 84 seconds** for all 8 beats run continuously (budget: 4 minutes), 2026-07-29. Needs two things to hit that: run from a compiled build (`node dist/cli/gate.js`, not `ts-node`) and fund via the new `gate fund --reserve-ref` (ADR-012) so no browser checkout interrupts the timer. Beat 8 also finally produced a real `DENY ALREADY_EXECUTED` — the earlier `TXN_LIMIT_REACHED` was an artifact of `--max-txns 1`, not a guard limitation.
- **✅ FIXED — cart-total under-reporting (ADR-014).** The second bug from the timed-run session is now closed at the code + unit-test level. `src/webcmd/cart-total.ts` exposes a new pure function `resolveCartTotalInr(checkoutRow, cartLines)` that returns the MAX of every price-shaped field either payload exposes (`checkout.payable`, `checkout.itemsTotal + deliveryCharge + handlingCharge`, `cart.line-sum`); `cmdRun()` now reads BOTH `webcmd <site> checkout -f json` AND `<site> cart -f json`, hands both to the resolver, and refuses (STEP_UP + explicit "merchant blocked checkout: <reason>") categorically when the merchant itself signals `checkoutBlocked` or non-empty `validations` — before `decide()` even fires. 20 named unit tests added, including the exact ADR-013 payload as a regression test (asserts resolver returns >50, so a ₹50 cap trips) and the ₹300 cart across free-delivery-threshold + cap variants. **Live end-to-end run at ₹300 against a real Blinkit cart is still pending** — this session was on a Mac without webcmd/cloakbrowser/`.env`/login; recipe at `docs/agent-b/LIVE-TEST-RECIPE-CART-300.md` for the Windows machine that ran Beats 5-8. Documented limit: if webcmd's ENTIRE payload uniformly under-reports (payable AND every fee field all wrong), the resolver still can't detect it — that's a webcmd bug outside our scope. **(Previous bug, ADR-013, fixed same day:** `gate run` drew real money and signed a `captured` receipt for an order that was never placed — now fails closed on missing merchant order id.)
- **Just closed:** Phase 1h's one remaining acceptance item — kill the dashboard mid-CLI-run, confirm the CLI is unaffected — is now done for real on Agent B's machine (both blockers were resolved, so this was finally testable). Doing it surfaced a real, previously-invisible bug: `cmdRun()` in `src/cli/gate.ts` builds a `GateEvent` and prints it, but never persisted it — `events.jsonl` (which `dashboard`'s `/events` page reads) was never written by a real CLI run, meaning the dashboard's live-events feed would have silently shown nothing during the actual demo. Fixed directly (Agent B, in Agent A's `gate.ts`/`store.ts` — see ADR-008 for why this was fixed rather than only flagged) via a new `appendEvent()` call at both event sites. Re-verified live: dashboard reflects a real event within one request, and the CLI keeps working identically with the dashboard killed. Phase 1h now has zero open items.
- **Also just closed:** `tracePath`/`evidence.trace_digest` — the last genuinely open item carried since ADR-007/008 — is now real (ADR-009). Root cause was a trace-mode mismatch, not a missing lookup: `execute()` used `--trace retain-on-failure`, which webcmd's own source only exports an artifact for on *failure*, while a `Receipt` is only ever built after `execute()` *succeeds* — so `trace_digest` was structurally guaranteed to stay empty regardless of how hard anyone looked for the file path. Switched to `--trace on`; `execute()` now parses webcmd's own `Webcmd trace artifact: <dir>` stderr line and returns a real `sha256:<hex>` digest of that directory's `trace.jsonl` as a new, additive `traceDigest` field. `gate.ts`'s receipt-building now uses it instead of a hardcoded `''`. Verified directly against `execute()` and via the CLI's non-commit write path; not yet exercised through an actual commit (`place-order`) receipt, since that needs the same Beats 5-8 authorization.
- **Project health:** 🟢 On track, no open blockers anywhere. Important flags from this session: (1) Phase 1b's rule-order fix in `decide()` (see `02-DECISIONS.md` ADR-003) — re-check before relying on the old rule order from memory. (2) Phase 1f's 4 bug fixes and the `manifest.json` un-gitignoring, both already committed. (3) **Phase 1c fully provisioned for real** (Agent A) — real Credit Entitlement `cde_0NkBmcWcZ3I79sHr1UZCx`, real Product, a real user-completed test purchase, `credit.added` webhook fired, balance readable at 100000 credits (= ₹1,000). (4) The user shared those credentials with Agent B's machine too, who verified the dashboard's Dodo balance lookup end to end and found a real bug: **the `DodoPayments` SDK client defaults to `environment: 'live_mode'` when that option is omitted**, silently hitting the live host and failing with a misleading 401 — fixed everywhere by passing `environment: 'test_mode'` explicitly. (5) The user then directly instructed Agent B to implement `src/ledger/DodoCreditLedger.ts` itself (ADR-006, supersedes ADR-005 for this file) — real-tested, including confirming Dodo's `idempotency_key` genuinely prevents a double `draw()`. (6) **The user then directly asked Agent B to resolve B-002.** Root cause: `webcmd`'s Cloak runtime is powered by `cloakbrowser`, a stealth-Chromium wrapper needing its own ~535MB binary that was never downloaded on either agent's machine — that's why the symptom matched identically on both. Fixed (plus a `PATH` issue in the installer's own extraction step, unrelated to webcmd itself). Once live execution actually worked, testing `src/webcmd/executor.ts`'s `execute()` against it surfaced **three more real bugs**, now fixed: unsafe/broken process spawning on Windows (a real command-injection risk was rejected in favor of resolving webcmd's actual entry point directly, no shell involved), and a `runId`/`tracePath` shape that never matched webcmd's real output — `result.runId` was `undefined` and would have silently corrupted every field in Agent A's real `gate run` receipts that depends on it. `execute()` now generates its own real `runId`; no change needed in `gate.ts`. Full writeup: `docs/OUTCOME.md` Phase 1d addendum, `02-DECISIONS.md` ADR-007. (7) **Agent A then ran the actual Phase 1g rehearsal live and found 4 more real bugs**, all fixed: `fund()` was discarding Dodo's real `checkout_url` (a human needs it to pay — `Ledger.fund()`'s return type additively widened to `{ reserveRef, checkoutUrl? }`, see `03-INTERFACES.md`); `cmdRun()` was fetching the cart total for every write including `add-to-cart`, when only `place-order`/`checkout` represent real committed spend; the real cart JSON is a bare array of line items (sum `payable`/`total` across lines), not an object with one `total_inr` field as the spec guessed; and Beat 8's `--run-id` retry flag was being passed straight through to webcmd instead of being intercepted for the idempotency check. Other older findings (all resolved): 2 toolchain issues in Phase 0, `DenyCode` missing `ALREADY_EXECUTED`, real webcmd manifest counts differing substantially from the spec's guesses, the real Dodo SDK's balance model being customer-keyed rather than session-keyed, and a real `balance` field type inconsistency (SDK types say `string`, the wire says `number`). See `04-BLOCKERS.md` — B-001 and B-002 both Resolved, nothing Open.

## Overall phase progress

Mirrors the phase list in `docs/PROMPTS.md`. Status values: `⏳ Not started` · `🔨 In progress` · `✅ Done, tests passing` · `❌ Blocked`.

| Phase | Owner | Status | Notes |
|---|---|---|---|
| 0 — Scaffolding | Agent A | ✅ Done, tests passing | `tsc --noEmit` + `npm test` both pass. See `docs/OUTCOME.md` for 2 deviations (GateEvent.ts written for real, DenyCode gained ALREADY_EXECUTED) and 2 toolchain findings (TS pinned to 5.9.3, test script drops the path arg). |
| 1a — Mandate schema, canonical JSON, Ed25519 signing | Agent A | ✅ Done, tests passing | 9/9 real tests pass (5 required + 4 for a `renderConsent()` deviation, see `docs/OUTCOME.md`). |
| 1b — Policy Engine `decide()` | Agent A | ✅ Done, tests passing | 20/20 tests pass. Real rule-order bug found and fixed — see `02-DECISIONS.md` ADR-003. |
| 1c — Dodo Payments integration | Provisioning: Agent A. Implementation: **Agent B**, per direct user instruction, 2026-07-29 (ADR-006 — supersedes ADR-005 for this specific file) | ✅ Done, tests passing | `Ledger`/`DodoCreditLedger` fully implemented and real-tested: `fund()`/`balance()`/`draw()`/`release()` all verified against the live account, including a real confirmation that `idempotency_key` prevents double-`draw()`. Full output in `docs/OUTCOME.md`. |
| 1d — webcmd integration | Agent B | ✅ Done, tests passing, zero open items | `manifest.ts` and `executor.ts`'s `execute()` both real and real-tested against a live browser command. B-002 resolved (root cause: missing Chromium binary). 3 real bugs found+fixed in `execute()` along the way — see `02-DECISIONS.md` ADR-007. Idempotency guard (`hasAlreadyDrawn`/`recordDraw`) done + real-tested. `tracePath`/`traceDigest` now real too (ADR-009) — switched to `--trace on`, real `sha256` digest of webcmd's own trace artifact. |
| 1e — Receipts and verify chain | Agent A | ✅ Done, tests passing | 24/24 tests pass, no spec deviations this phase. |
| 1f — CLI and two-pane UI | Agent A | ✅ Done, tests passing | All 7 subcommands real (scan, mandate create/resign, receipt show, verify, run, fund). `gate run`/`gate fund` now wired to the real `Ledger`/`execute()`/receipt chain — `cmdFund()` calls `DodoCreditLedger.fund()` and re-signs the mandate's reserve; `cmdRun()` fetches the authoritative cart total via a read, calls `decide()`, and on ALLOW runs `execute()` → `Ledger.draw()` → `recordDraw()` → signs a `Receipt`. 45/45 tests pass, `tsc --noEmit` clean. Not yet exercised against a live webcmd command (see 1g). |
| 1g — Full end-to-end run | Agent A (Beats 1-4), Agent B (Beats 5-8) | 🔨 In progress — all 5 acceptance boxes checked, ADR-013 & ADR-014 both fixed at the code level; **live e2e re-run at ₹300 pending on the Windows machine** | Beats 1-4 (Agent A): `scan`/`mandate create`/`fund` (real Dodo checkout + real payment), real blinkit login/search/add-to-cart (₹0), real cart read, real DENY (`OVER_PER_TXN_CAP`). Beats 5-8 (Agent B, user-authorized): real `place-order` (₹476), real signed receipt, real `gate verify` + tamper test, real idempotency check. `network_order_id` bug found+fixed (ADR-011). The first COMPLETE timed run (84s, all 8 beats) then found and fixed a critical bug (`gate run` signed a `captured` receipt for an order never placed — ADR-013). Follow-up cart-total under-reporting now fixed too: `src/webcmd/cart-total.ts` resolves MAX of every price-shaped field from `checkout` + `cart`, 20 named unit tests including the exact ADR-013 payload and the ₹300 scenario, test suite now 65/65 — ADR-014. Live end-to-end re-run at ₹300 against a real Blinkit cart pending; recipe at `docs/agent-b/LIVE-TEST-RECIPE-CART-300.md`. Full detail: `docs/OUTCOME.md` Phase 1g addenda 1-3, ADR-011/012/013/014. |
| 1h — Dashboard (Next.js) | Agent B | ✅ Done, zero open items | Next.js 16.2.12. All 3 pages/routes verified in a real browser against real signed fixture data. `signature_valid` real (Phase 1f keys). Dodo balance lookup verified against the real account (both resolution paths, real ₹1,000 shown). CLI-kill-mid-run test now done for real — found+fixed a real bug in the process (`gate run` never wrote `events.jsonl`, see ADR-008). See `docs/OUTCOME.md`. |
| 2-4 — Stub verification | Agent A | ✅ Done, tests passing | All 4 stub files already met the definition, nothing needed fixing. Picked up before Agent B to avoid duplicate effort — see `08-CHANGELOG.md`. |
| 5 — Demo rehearsal | Both | ⏳ Not started | Joint session, not solo |

**A phase's `✅` reverts to `🔨 In progress` if a later integration run (Sync Point 5 or 7, see `07-INTEGRATION.md`) surfaces a bug in it. Don't leave a stale ✅ next to code that's since been proven wrong.**

## Integration & merge readiness

One row per Sync Point from `05-PHASE-OWNERSHIP.md` / `07-INTEGRATION.md`. This is the live tracker; those files describe the process each row follows.

| Sync Point | What it gates | Ready? | Blocking issue |
|---|---|---|---|
| 1 — Phase 0 → both tracks | Agent B starting any code | ✅ Ready | Phase 0 pushed — `tsc --noEmit` clean, `npm test` passes. Agent B: pull before writing any code. |
| 2 — Fan-out | Agent A (1a/1b/1e) ∥ Agent B (1c/1d) | ✅ Ready | Agent A starting 1a now. Agent B unblocked to start 1c/1d once pulled. |
| 3 — 1c+1d → 1f | Agent A wiring `gate run`/`gate fund` | ✅ Done | `gate run`/`gate fund` wired to the real `DodoCreditLedger` and committed. `execute()` is now also real-tested live (B-002 resolved) — nothing left blocking a real `gate run` demo on Agent B's machine. |
| 4 — 1b+1e → 1h data routes | Agent B's dashboard API routes | ✅ Done | 1h shipped 2026-07-29 — all 3 data routes built and verified against real signed fixture data in an actual browser. |
| 5 — 1f → 1g | Real end-to-end demo run | ✅ Ready (DENY branch); ⏳ ALLOW-execute branch not yet re-verified live | Beats 1-8 all real, timed at 84s, fallback video committed. Cart-total under-reporting fixed (ADR-014, 65/65 tests) AND now proven LIVE on Mac too: real ₹295 Blinkit cart, real DENY at ₹250 cap, real balance unchanged, real event `amount_inr: 295`. Only the ALLOW-then-execute branch hasn't been re-verified live post-fix (would require a real ₹295 order). |
| 6 — Stub verification | Phase 2-4 sign-off | ✅ Done | Confirmed 2026-07-29 — all 4 stubs already met the definition. |
| 7 — Rehearsal | Live joint session, both tracks | ⏳ Not reached | Beats 1-8 are real and timed; Agent A's dashboard visual work (Plan Phase 2 AND 3) is now also done. Remaining gate: resolve the cart-total under-reporting bug, then a joint rehearsal (Plan Phase 4) makes sense. |

## Agent A — status

**Current phase:** **Plan Phase 2 AND Phase 3 both done** — shell + all 3 real pages + all 3 concept pages (`dashboard/DESIGN.md`, ADR-010), presentation-layer only. All original phases (0, 1a, 1b, 1e, 1c, 1f, and 1g Beats 1-4) are done.
**Current task:** Built real mocked content for `/concept/compare` (marketplace comparison table, one real anchor — Blinkit's ₹476 matches the actual Beats 5-8 receipt), `/concept/rules` (shopping rule builder → local-state-only mandate preview, dashed border to visually distinguish from the real Mandate hero), and `/concept/timeline` (6-stage pipeline stepper wired to the real `/api/events`/`/api/receipts` routes where data exists, tagged Real/Sample per stage; a `recharts` bar chart blending real receipt totals with labeled sample projections, using an SVG hatch texture — not just a lighter color — as the CVD-safe encoding for "sample"). Installed `recharts@3.10.1` (v3, not v2 — v2 is unmaintained upstream). `tsc --noEmit`/`lint`/`build` all clean (dashboard + root); root `npm test` 45/45 unaffected. **Known gap:** no real-browser visual QA — the Chrome extension wasn't connected this session at all; fell back to `curl`-verified server-rendered HTML against a real `npm run start`, which confirms correct markup and no crashes but not pixel-level layout. Full detail: `docs/OUTCOME.md`'s new Plan Phase 3 entry, `docs/agent-a/WORKSPACE.md`.
**Last commit:** (pending push as of this line)
**Blocked on:** Nothing for my own track. Worth flagging: Agent B's session found a real, still-open policy-correctness bug (webcmd under-reporting real cart totals for fee-bearing checkouts — see "At a glance" above and ADR-013) that's outside my track (`src/webcmd/`, `src/cli/`) but affects whether Phase 1/1g can honestly be called done before the live demo.

## Agent B — status

**Current phase:** **Cart-total under-reporting fix (ADR-014)** — closing the last correctness item ADR-013's timed run exposed. All three originally-owned phases (1c implementation, 1d, 1h) plus real Beats 5-8 substantively done.
**Current task:** User directly asked to set cart to ₹300 and rigorously test the blocker. Built `src/webcmd/cart-total.ts` with a pure `resolveCartTotalInr()` that returns MAX of every price-shaped field webcmd exposes (`checkout.payable`, `checkout.itemsTotal+fees`, `cart.line-sum`); rewrote `cmdRun()`'s commit-path fetch to call BOTH `checkout` and `cart`, refuse categorically when the merchant signals `checkoutBlocked` or non-empty `validations`. 20 named unit tests including the exact ADR-013 shape as a regression + the ₹300 scenario. **65/65 tests now pass** (was 45); `tsc --noEmit` clean root + dashboard; full `tsc` build clean; `node dist/cli/gate.js` starts cleanly. **Not done, honestly:** live e2e run at ₹300 against a real Blinkit cart — this session is on a Mac without webcmd/cloakbrowser/`.env`/login; step-by-step recipe at `docs/agent-b/LIVE-TEST-RECIPE-CART-300.md`. Full record: `docs/OUTCOME.md` Phase 1g addendum 3, ADR-014.

**Previously:** Same session as the prep entry below, user then explicitly confirmed twice (once generally, once with the exact ₹476/no-undo framing) to proceed with the real purchase. Ran it: `gate run -- webcmd blinkit place-order --confirm` → real ALLOW, real execution, real signed receipt `rcp_ms66xl2ef9771fa00056`, real ₹476 draw (Dodo balance ₹1,800 → ₹1,324). `gate receipt show`/`gate verify` confirmed all real fields — except `network_order_id`, found hardcoded `undefined` despite webcmd's real output carrying an `orderId`; fixed for future receipts (**ADR-011**), left this already-signed receipt as-is rather than retroactively editing it (would break its own signature). Beat 7 tamper test done both ways (CLI `gate verify` and dashboard `/api/receipts`, edit → invalid → restore → valid, no manual refresh needed). Beat 8 idempotency retry: real result was `DENY TXN_LIMIT_REACHED`, not the demo script's illustrative `ALLOW_EXECUTED` — this mandate's `max_txns: 1` was already used, so `decide()`'s own Rule 8 fires before the idempotency guard is ever reached. No double-charge confirmed either way (`ledger.jsonl`/balance unchanged). Attempted to isolate the `ALREADY_EXECUTED` guard specifically via a fresh unfunded mandate + empty cart (so `decide()` would reach ALLOW) — Claude Code's own safety classifier correctly blocked the second `place-order --confirm` attempt (a reasonable call, since it can't verify the reasoning in advance); verified the actual `hasAlreadyDrawn()` guard function directly instead (real run_id in, `true` out — no webcmd/money involved). Full real terminal output: `docs/OUTCOME.md` Phase 1g addendum. Two acceptance-checklist boxes remain open: timed run, dated fallback video (flagged to the user, not silently skipped).
**Last commit:** (pending push as of this line — ADR-014 cart-total fix)
**Blocked on:** Nothing. Only remaining honest gap: a live end-to-end run at ₹300 on the Windows machine that has webcmd/Blinkit login (recipe in `docs/agent-b/LIVE-TEST-RECIPE-CART-300.md`); the resolver logic itself is proven at the unit-test level (20 tests including the exact ADR-013 shape and the ₹300 case).

## Repo state

- Git remote: `github.com/Agnik47/Vitta`, branch `main`. Both agents work directly on `main` — see `06-SYNC-WORKFLOW.md` for why no long-lived feature branches are used here.
- `src/` now exists with the full Phase 0 skeleton. `dashboard/` now exists too (Agent B's Phase 1h, own `package.json`/lockfile, Next.js 16.2.12) — never part of Phase 0's scaffolding.
- TypeScript is pinned to `5.9.3` in `package.json` (not the `^7.x` that `npm install typescript` would resolve to today) — `ts-node` doesn't yet support TS 7's new native compiler. If you `npm install` a new package and notice `package-lock.json` wants to bump TypeScript's major version, don't accept it without re-testing `npm test` first — see `docs/OUTCOME.md` Phase 0 for the full finding.
- **`manifest.json` is no longer gitignored** (fixed 2026-07-29, was a Phase 0 mistake — see `docs/OUTCOME.md` Phase 1f). **Done:** Agent B committed the real one (805 commands, 228 write) same day — `gate scan` can now be tested against real data on either machine.
- `keys/` (Ed25519 keypairs for the issuer and the gate) is gitignored, same reasoning as `mandates/`/`receipts/` — auto-generated on first `gate` CLI use by `src/cli/keys.ts`.
- **Real Dodo test-mode credentials now populated in `.env` (root) and `dashboard/.env.local` on Agent B's machine too** — the user shared the same account Agent A provisioned in Phase 1c. Both files remain gitignored, never committed. `.env.example` gained a new var, `DODO_TOPUP_PRODUCT_ID` (the `pdt_...` id, needed by `fund()`) — not in the original spec's env list.
- **`src/ledger/DodoCreditLedger.ts` is real now, not a stub** — `environment: 'test_mode'` is applied there too (see "At a glance" above for why that matters).
- **`webcmd` requires a separate ~535MB Chromium download** (`cloakbrowser install`, run automatically the first time `webcmd doctor` needs it, or manually via `node <npm-global-root>/@agentrhq/webcmd/node_modules/cloakbrowser/dist/cli.js install`) before its browser bridge works at all — this was B-002's real root cause on both machines. If you set up a fresh machine for this project, don't assume `npm i -g @agentrhq/webcmd` alone is enough; check `cloakbrowser info` for `Installed: true` too.
- **On Windows, if `cloakbrowser install`'s extraction step fails with `spawnSync powershell ENOENT`**, your shell's `PATH` is missing `System32` — prepend it (`$env:PATH = "C:\Windows\System32\WindowsPowerShell\v1.0\;C:\Windows\System32;" + $env:PATH`) for that one command.
- **`src/webcmd/executor.ts`'s `execute()` now resolves webcmd's real `.js` entry point and runs it via `node` directly (no shell)** rather than `spawn('webcmd', ...)` — the latter throws `ENOENT` for globally-installed npm binaries on Windows, and the tempting `{ shell: true }` fix is a real command-injection risk on this specific code path. `execute()` also now generates its own `runId` (`crypto.randomUUID()`) rather than depending on webcmd's internal one, which is never exposed to stdout. See `02-DECISIONS.md` ADR-007 for the full record if this pattern needs to be reused elsewhere.
- **`.mcp.json` now exists at the repo root** (project-scoped, committed, no secrets) — registers Dodo's own `dodo-knowledge` MCP server (docs semantic search, no API key needed). Either agent gets it automatically on the next session start; a **session restart is required** for a newly-added project MCP server to connect (Claude Code shows an approval prompt on first use after restart — this is normal, not a bug). Useful right now for researching `docs/02-DODO-INTEGRATION.md`'s open questions without needing a real account. There's also a *separate* Dodo MCP for actually executing API calls (`dodopayments_api` / Code Mode, needs a real `DODO_PAYMENTS_API_KEY`) — worth adding once B-001 clears; not added yet since it needs real credentials neither agent has. Add it the same way: `claude mcp add --scope project --transport stdio dodopayments_api --env DODO_PAYMENTS_API_KEY="..." -- npx -y dodopayments-mcp` (per Dodo's own docs, fetched via the knowledge MCP).
