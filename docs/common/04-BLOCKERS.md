# 04 — Active Blockers

A fast-scan list of anything currently stopping either agent. This is deliberately separate from `docs/OUTCOME.md`'s per-phase `❌ Blocked` status (which stays the detailed official record, per `CLAUDE.md`'s existing rule) — this file is the thing you check in five seconds at the start of every session to see if the other agent is stuck on something you can unblock, or if you're about to walk into a known wall.

**Rules:**
- Add an entry the moment something blocks you — don't wait until your next scheduled sync to mention it, push a `04-BLOCKERS.md` update immediately per `00-START-HERE.md`.
- Move resolved entries to the "Resolved" table below (don't delete them — a resolved blocker is useful history for `docs/OUTCOME.md`'s "Running list of open questions resolved during the build" section, and sometimes worth its own `02-DECISIONS.md` entry if resolving it involved a real judgment call).
- If a blocker is genuinely a project-level prerequisite issue (missing Dodo account, webcmd not logging in, an API behaving differently than documented in a way that blocks progress), this is also exactly the case `CLAUDE.md` § "If you're blocked" describes — log it here **and** in `docs/OUTCOME.md`'s current phase section, and stop rather than inventing a workaround that violates a hard rule.

## Open

| ID | Raised by | Date | Blocking | Description | Status |
|---|---|---|---|---|---|
| _(none — B-001 and B-002 both resolved, see Resolved table below)_ | | | | | |

## Resolved

| ID | Raised by | Date raised | Date resolved | Description | Resolution |
|---|---|---|---|---|---|
| B-001 | Agent B (originally); reassigned to Agent A per ADR-005 | 2026-07-29 | 2026-07-29 (same day) | No real Dodo test-mode account/credentials → blocked Phase 1c (`src/ledger/`), and downstream Phase 1g / `gate run` / `gate fund`. Originally listed four missing things: `DODO_API_KEY`, `DODO_API_KEY_READONLY`, the "Agent Spend Credits" Product + `product_id`, and `DODO_CREDIT_ENTITLEMENT_ID`. | **Fully resolved — real money path proven end to end in test mode.** Both API keys turned out to already be real in `.env` (the "read-only key still missing" note was simply stale; live-probed to confirm it reads and 401s on write). Created for real via the API: Credit Entitlement `cde_0NkBmcWcZ3I79sHr1UZCx` ("Agent Spend Credits", `unit: "INR paise"`, `precision: 0`, so 1 credit = 1 paise) and top-up Product `pdt_0NkBmcZQJLSicxFMHlNHX`. The user then completed a real test-mode purchase (checkout `cks_0NkBw28CUxmbI2KsSdVFu`, test Visa) — the agent could not do this step itself, since entering payment details is prohibited even in test mode. Result verified three independent ways: the live `credit.added` webhook fired, `balances.retrieve()` under the **read-only** key returns `balance: 100000, overage: 0` for customer `cus_0NkBwH3N9Ld41wgNzK6ty`, and the ledger shows one `credit_added` entry `0 → 100000`. `DODO_WEBHOOK_SECRET` is still empty but is genuinely optional per `docs/02-DODO-INTEGRATION.md` and blocks nothing. Along the way this turned up a real SDK-vs-wire type bug worth reading before writing `balance()`: **the SDK declares `balance` a `string`, the API returns a `number`** — full writeup in `docs/OUTCOME.md` Phase 1c FINDING A, plus a correction stamped onto the original open-questions row. **Next step is no longer blocked on anything external: write `src/ledger/DodoCreditLedger.ts`.** |
| B-002 | Agent B; reproduced independently by Agent A, 2026-07-29 | 2026-07-29 | 2026-07-29 (later still) | `webcmd doctor` reported Daemon OK, Runtime (Cloak) connected, but **Connectivity: FAIL** ("Browser exec command timed out after 8s") — reproduced identically on both agents' machines, ruling out a one-off machine misconfiguration. | **Fully resolved — real root cause found and fixed, not a webcmd/Cloak bug.** `webcmd`'s browser automation runs on `cloakbrowser` (a stealth-Chromium Playwright wrapper) which needs its own ~535MB Chromium binary downloaded separately (`cloakbrowser install`) — it never uses the system's regular Chrome, no matter how many Chrome windows are already open. `cloakbrowser info` showed `Installed: false` on this machine; the same is almost certainly true on Agent A's, explaining the identical symptom on both. Running the installer itself hit a second real bug twice (`Error: spawnSync powershell ENOENT`) — this sandboxed environment's `PATH` doesn't include `System32`, so `powershell.exe` (needed for ZIP extraction) couldn't be resolved, even from inside an actual PowerShell session. Fixed by prepending the real `System32` path to `PATH` for that one command; download, signature/checksum verification, and extraction all then succeeded for real. `webcmd doctor` now reports **"Connectivity: connected in 3.1s — Everything looks good!"**, and a real live browser command (`webcmd duckduckgo search ...`) returned real results. See `docs/common/02-DECISIONS.md` ADR-007 for the full record, including three further real bugs found in `src/webcmd/executor.ts`'s `execute()` once live execution was finally testable (unsafe/broken process spawning on Windows, and a `runId`/`tracePath` shape that never matched webcmd's real output) — all fixed in the same pass. |
