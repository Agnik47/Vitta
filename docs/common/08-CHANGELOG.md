# 08 — Changelog & Handoff Log (Append-Only)

A running, chronological log of every sync-worthy change either agent makes. Every entry doubles as the **handoff note** for whatever it describes — write it assuming the other agent will read only this entry, not this conversation, to know what to do next.

This is the lightweight, per-commit companion to two other logs — don't duplicate into them, cross-reference instead:
- `docs/OUTCOME.md` (existing, unchanged) stays the authoritative, detailed, per-phase log with full pasted terminal output, per `CLAUDE.md`'s existing rule.
- `docs/common/02-DECISIONS.md` holds the *reasoning* behind any real judgment call — link to an ADR number here rather than re-explaining alternatives inline.

**Rules:**
1. Always append at the bottom. Never edit or delete another agent's entry.
2. New entry every time you push something the other agent should know about — not just at phase boundaries.
3. Fill in every field. "Files touched: various" is not useful to someone deciding whether to pull before continuing. "Known issues: none" and "Testing status: not run" are both valid answers — just don't leave the field blank.
4. If two entries land near-simultaneously and conflict on merge, keep both, order by timestamp — see `06-SYNC-WORKFLOW.md` § Conflict resolution.

**Entry template:**

```
## [YYYY-MM-DD HH:MM] — Agent A/B — short title

**What changed:**
**Why:**
**Files touched:**
**Testing status:** (what was run, pass/fail — "not yet run" is honest and fine, "not applicable" if docs-only)
**Known issues:** (rough edges, TODOs left behind, or "none")
**Other agent needs to:** (specific action, or "none")
**Interface changes:** (none, or describe — and confirm 03-INTERFACES.md was updated; link an ADR number from 02-DECISIONS.md if one was warranted)
**Blockers introduced/resolved:** (none, or ref 04-BLOCKERS.md entry ID)
```

---

## [2026-07-28] — Restructuring session — Parallel development docs created

**What changed:** Created `docs/common/` (this file and its siblings), `docs/agent-a/WORKSPACE.md`, `docs/agent-b/WORKSPACE.md`, and added a short pointer section to root `CLAUDE.md` so any session that reads it discovers this structure.
**Why:** Two Claude Code instances are about to build this project in parallel on two machines, syncing through Git. This structure exists so both stay synchronized on current state, ownership, and interfaces without re-deriving it from scratch or drifting apart.
**Files touched:** `docs/common/00-START-HERE.md`, `01-PROJECT-STATUS.md`, `02-PHASE-OWNERSHIP.md`, `03-INTERFACES.md`, `04-SYNC-WORKFLOW.md`, `05-INTEGRATION.md`, `06-CHANGELOG.md`, `07-BLOCKERS.md`, `docs/agent-a/WORKSPACE.md`, `docs/agent-b/WORKSPACE.md`, `CLAUDE.md` (additive pointer only, no existing rules changed).
**Testing status:** Not applicable — documentation only, no code exists yet.
**Known issues:** None.
**Other agent needs to:** Both agents (A and B) should read `docs/common/00-START-HERE.md` in full before starting Phase 0 or any other work. Nothing has been built yet (`src/` and `dashboard/` don't exist) — this is pure setup ahead of Phase 0.
**Interface changes:** none — no code exists yet.
**Blockers introduced/resolved:** none.

---

## [2026-07-28] — Restructuring session (upgrade pass) — Decisions log added, reading order sequenced, handoff fields expanded

**What changed:** Renumbered `docs/common/` so the reading order is explicit from filenames (state docs `01-04` read fully every session, process docs `05-07` read once, this log `08` skimmed every session). Added `02-DECISIONS.md`, a genuinely new ADR-style log for architectural judgment calls — nothing like it existed before this pass. Added a live "Integration & Merge Readiness" table and an "At a glance" completed/active/pending rollup to `01-PROJECT-STATUS.md`. Expanded this file's entry template with `Testing status` and `Known issues` fields. Added an explicit "What 'merge' means here" and "Testing before merge" section to `06-SYNC-WORKFLOW.md` (previously implicit). Seeded `02-DECISIONS.md` with two real ADRs (ADR-001: trunk-based git workflow, ADR-002: the dependency-driven phase split) documenting decisions that were already made in the prior pass but not previously recorded with alternatives/impact/follow-up.
**Why:** The user asked for the workflow to explicitly support: recorded architectural decisions with alternatives considered, richer per-task handoff notes (testing status, known issues), a live integration/merge-readiness view, and a reading order sequenced by category (health → completed/active/pending → decisions → interfaces → blockers → then workspace). The prior structure covered status/interfaces/blockers/changelog well but had no dedicated decision log and left "merge"/"testing before merge" implicit rather than named.
**Files touched:** `docs/common/00-START-HERE.md` (rewritten), `01-PROJECT-STATUS.md` (expanded), `02-DECISIONS.md` (new), `03-INTERFACES.md` (change-protocol updated to require ADRs for frozen-contract changes), `04-BLOCKERS.md` (moved from old `07-BLOCKERS.md`, content unchanged), `05-PHASE-OWNERSHIP.md` (moved from old `02-PHASE-OWNERSHIP.md`, cross-references updated), `06-SYNC-WORKFLOW.md` (moved from old `04-SYNC-WORKFLOW.md`, merge/testing sections added), `07-INTEGRATION.md` (moved from old `05-INTEGRATION.md`, cross-references updated), `08-CHANGELOG.md` (this file, moved from old `06-CHANGELOG.md`, template expanded), `docs/agent-a/WORKSPACE.md` and `docs/agent-b/WORKSPACE.md` (cross-references updated to new file numbers, "Known issues" subsection added).
**Testing status:** Not applicable — documentation only, no code exists yet.
**Known issues:** None. The old filenames (`02-PHASE-OWNERSHIP.md`, `04-SYNC-WORKFLOW.md`, `05-INTEGRATION.md`, `06-CHANGELOG.md`, `07-BLOCKERS.md`) were deleted as part of this renumbering — confirm no external link or note refers to the old paths before relying on them.
**Other agent needs to:** Nothing yet — still pre-Phase-0, no work has started. Whichever agent starts first should follow the new `00-START-HERE.md` reading order as written.
**Interface changes:** None — no code contracts exist yet. This pass only restructured documentation.
**Blockers introduced/resolved:** none.

---

## [2026-07-28] — Agent A — Phase 0 complete: repo scaffolding

**What changed:** Ran `docs/PROMPTS.md` Phase 0. `npm init`, installed `typescript`/`ts-node`/`@types/node` (dev) and `dodopayments` (prod), wrote `tsconfig.json`, created the full `src/` tree from `docs/01-ARCHITECTURE.md` § Repo layout (one-line comment stubs for every Phase 1 file, real typed stub content for the four Phase 2-4 files), wrote `.env.example` and `.gitignore`. `tsc --noEmit` and `npm test` both pass clean.
**Why:** First task in the phase-ownership split — Phase 0 is a prerequisite for both agents' tracks (Sync Point 1, `05-PHASE-OWNERSHIP.md`).
**Files touched:** `package.json`, `tsconfig.json`, `.env.example`, `.gitignore`, all 19 files under `src/` (see `docs/OUTCOME.md` Phase 0 entry for the full list and per-file rationale), plus doc corrections: `docs/01-ARCHITECTURE.md` (added missing `ALREADY_EXECUTED` to `DenyCode`), `docs/common/03-INTERFACES.md` (fixed the `Ledger` interface ownership row — it's a Phase 1c/Agent B deliverable, not Phase 0), `docs/OUTCOME.md` Phase 0 section, `docs/common/01-PROJECT-STATUS.md`.
**Testing status:** `npx tsc --noEmit` → exit 0, clean. `npm test` → 1 test file discovered (`src/policy/decide.test.ts`, currently empty since Phase 1b hasn't run), 0 failures. Full output pasted in `docs/OUTCOME.md`.
**Known issues:** None outstanding. Two toolchain issues were found and fixed during this phase (not left as known issues): `npm install typescript` resolves to `7.0.2` today, which `ts-node` doesn't support (`ts.sys` undefined) — pinned to `5.9.3` instead. `node --test <directory>` fails on Node 22.18.0 — fixed by dropping the path argument and relying on `--test`'s default recursive discovery from cwd. Full detail in `docs/OUTCOME.md`.
**Other agent needs to:** Agent B — pull this, run `npx tsc --noEmit` yourself to confirm it's clean on your machine too (Sync Point 1's own compatibility check per `07-INTEGRATION.md`), then start Phase 1c and/or 1d. Note the TypeScript version pin above before your own `npm install` touches `package-lock.json`.
**Interface changes:** `GateEvent`/`DenyCode` (`src/events/GateEvent.ts`) created for real, not left as a comment stub — deliberate deviation from the literal Phase 0 instruction, justified in `docs/OUTCOME.md`. `DenyCode` gained `ALREADY_EXECUTED`, which was missing from the original spec despite `docs/05-DEMO-SCRIPT.md` Beat 8 needing it. `Ledger` interface ownership corrected in `03-INTERFACES.md` (Agent B/Phase 1c, not Agent A/Phase 0) — `src/ledger/Ledger.ts` itself is still just a comment stub right now.
**Blockers introduced/resolved:** none.

---

## [2026-07-29] — Agent A — Phase 1a complete: mandate schema, canonical JSON, Ed25519 signing

**What changed:** Implemented `src/mandate/schema.ts` (`Mandate` interface + a hand-written `isMandate()` type guard), `src/mandate/sign.ts` (`canonicalJSON()`, `generateKeyPair()`, `sign()`, `verify()` — matches `docs/04-POLICY-ENGINE-SPEC.md`'s code exactly, no changes needed), and `src/mandate/render.ts` (`renderConsent()` — deviated from the spec's code sketch, see below). Added `src/mandate/sign.test.ts` (5 tests, the 3 required by `docs/PROMPTS.md` Phase 1a plus 2 extra) and `src/mandate/render.test.ts` (4 tests, not required by the prompt but added after finding real bugs — see Known issues).
**Why:** Next item in Agent A's sequential track (`05-PHASE-OWNERSHIP.md`) — `decide()` (Phase 1b) and the receipt chain (Phase 1e) both import `sign()`/`verify()` from this phase.
**Files touched:** `src/mandate/schema.ts`, `sign.ts`, `render.ts` (all rewritten from Phase 0's comment stubs), `src/mandate/sign.test.ts` and `render.test.ts` (new), `docs/OUTCOME.md` Phase 1a section, `docs/common/01-PROJECT-STATUS.md`.
**Testing status:** `npx tsc --noEmit` → exit 0. `npm test` → 10/10 passing (5 sign tests + 4 render tests + the still-empty `decide.test.ts` stub). Full pasted output in `docs/OUTCOME.md`.
**Known issues:** None outstanding — both bugs found this phase were fixed, not left open. Worth flagging for whoever next touches `render.ts`: `BRAND_NAMES` currently only covers `blinkit`/`zepto`/`bigbasket`/`district` (the merchants named in the specs). Any new merchant added to a mandate's scope that isn't in that table falls back to generic `capitalize()`, which will be wrong for any brand with an internal capital (the same class of bug just fixed for `bigbasket`) — not a blocker, just something to remember if the demo script ever adds a new merchant.
**Other agent needs to:** Nothing blocking — `src/mandate/` isn't something Agent B's tracks (`src/ledger/`, `src/webcmd/`, later `dashboard/`) import directly in their own phases. Worth knowing: `Mandate`'s shape is now real code, not just the spec doc, if you want to sanity-check anything against it.
**Interface changes:** `Mandate` (`src/mandate/schema.ts`) is now implemented — matches `docs/04-POLICY-ENGINE-SPEC.md` exactly, plus the added `isMandate()` guard (additive, not in conflict with anything). Per `03-INTERFACES.md`, `Mandate` and `canonicalJSON()`/`sign()`/`verify()` are now 🔒 frozen.
**Blockers introduced/resolved:** none.

---

## [2026-07-29] — Agent B — Pulled Phase 0, added task-tracking docs, starting 1c/1d

**What changed:** Session started before Agent A's Phase 0 push had been fetched locally, so it independently drafted a duplicate Phase 0 scaffold (uncommitted, never pushed). Caught the real push via `git fetch` before pushing anything — discarded the local duplicate wholesale (`rm -rf` on the untracked duplicate files/folders, all of which had zero git history), then `git pull --rebase origin main` cleanly fast-forwarded. Verified `tsc --noEmit` and `npm test` both pass clean on this machine against Agent A's real Phase 0. Added three new docs to `docs/agent-b/`: `TASKS.md` (durable per-phase checklist for 1c/1d/1h, checkbox-level, so progress survives a session restart without re-deriving it from `docs/PROMPTS.md` each time), `ROADMAP.md` (milestones M1-M9 mapped against the 31 Jul/1 Aug deadline, with a "where we actually are" section to update every session), and `ERROR-HANDLING.md` (consolidates every I/O failure mode already specified across `docs/02-DODO-INTEGRATION.md` and `docs/03-WEBCMD-INTEGRATION.md` into one reference scoped to what Agent B actually builds, so a future session isn't re-deriving fail-closed behavior for the ledger/webcmd/dashboard under time pressure). Updated `WORKSPACE.md`, `01-PROJECT-STATUS.md` (Agent B's own section only), and `04-BLOCKERS.md` (opened B-001) to reflect current state.
**Why:** The user, present as "Agent B" on this machine, clarified the collaboration model directly: this track should work its own assigned deliverables (1c, 1d, then 1h) at its own pace and never wait on or absorb Agent A's phases just because Agent A hasn't finished — and asked for durable, git-tracked task/roadmap/error-handling references specific to this track, rather than relying on chat-only state.
**Files touched:** `docs/agent-b/WORKSPACE.md`, `docs/agent-b/TASKS.md` (new), `docs/agent-b/ROADMAP.md` (new), `docs/agent-b/ERROR-HANDLING.md` (new), `docs/common/01-PROJECT-STATUS.md` (Agent B section only), `docs/common/04-BLOCKERS.md` (opened B-001).
**Testing status:** `npx tsc --noEmit` → exit 0, clean. `npm test` → 1 test file discovered (`src/policy/decide.test.ts`, still empty pending Agent A's Phase 1b), 0 failures. Same result Agent A reported — Sync Point 1 cross-machine compatibility confirmed.
**Known issues:** None yet on this track's own code — no Phase 1c/1d code written yet this entry, just verification and doc setup.
**Other agent needs to:** Nothing blocking — informational only. Agent A can ignore `docs/agent-b/*` beyond `WORKSPACE.md`'s existing cross-reference note.
**Interface changes:** none.
**Blockers introduced/resolved:** Introduced B-001 (`04-BLOCKERS.md`) — Phase 1c needs a real Dodo test-mode account + `.env` from the user, not yet provided. Phase 1d is unaffected and starting now (self-serve npm global install).

---

## [2026-07-29] — Agent A — Phase 1b complete: decide(), with a real rule-order fix — read this one

**What changed:** Implemented `src/policy/decide.ts` and `decide.test.ts` (11 tests: the 9 required by `docs/PROMPTS.md` Phase 1b plus 2 extra). While building the required "read access always allows, regardless of mandate state" test, found that the spec's own rule order (signature/expiry checked *before* the read short-circuit) would deny a read against an expired or badly-signed mandate — directly contradicting that test's title and `docs/03-WEBCMD-INTEGRATION.md` § Step 3's explicit "no mandate check, no signature verification" for reads. Moved read-access to Rule 0. Full reasoning in `docs/common/02-DECISIONS.md` ADR-003 — this is a proper decision record, not just a changelog line, because it changes a contract both of us build against.
**Why:** Next item in Agent A's sequential track. `decide()` is described in the specs as "the single most important function in the codebase" and its rule order is explicitly what determines the DENY reason shown live during the demo — worth getting exactly right now rather than downstream.
**Files touched:** `src/policy/decide.ts`, `decide.test.ts` (both rewritten from Phase 0's comment stub), `docs/04-POLICY-ENGINE-SPEC.md` (rule order + `SpendRequest.access` type corrected, "Rule order note" added), `docs/common/02-DECISIONS.md` (new ADR-003), `docs/common/03-INTERFACES.md` (`Decision`/`SpendRequest`/`decide()` row updated), `docs/OUTCOME.md` Phase 1b section, `docs/common/01-PROJECT-STATUS.md`.
**Testing status:** `npx tsc --noEmit` → exit 0. `npm test` → 20/20 passing (9 mandate/sign + 11 decide, including a dedicated regression test: "read access allows even against a badly signed mandate"). Full output in `docs/OUTCOME.md`.
**Known issues:** None outstanding. One thing to remember: `decide()`'s Rule 3 (unknown-command) was deliberately left in its original position relative to signature/expiry — only the read-access case had evidence for reordering, extending it to unknown-command too would have been unjustified guessing. If a future spec update clarifies unknown-command should also bypass signature/expiry, that's a new ADR, not a quiet edit.
**Other agent needs to:** **Please read `02-DECISIONS.md` ADR-003 before writing anything that calls or reasons about `decide()`** — the rule order changed from what `04-POLICY-ENGINE-SPEC.md` originally showed. This doesn't touch `src/ledger/` or `src/webcmd/` directly, but if you've already read the old rule order into your own mental model (e.g., while writing `03-WEBCMD-INTEGRATION.md`-adjacent code that assumes a particular check order), re-verify against the current spec.
**Interface changes:** `Decision`/`SpendRequest`/`decide()` (`src/policy/decide.ts`) implemented, with the Rule 0-3 reorder and the `access` type widening described above. Both changes propagated to `04-POLICY-ENGINE-SPEC.md` and `03-INTERFACES.md`. See ADR-003 for the full alternatives-considered writeup.
**Blockers introduced/resolved:** none.

---

## [2026-07-29] — Agent A — Phase 1e complete: receipt schema and hash-chain

**What changed:** Implemented `src/receipt/schema.ts` (`Receipt` interface) and `src/receipt/chain.ts` (`buildAndSignReceipt()`, `verifyReceipt()`, plus `sha256Hex()` and `verifyChain()` — the spec describes these last two in prose but doesn't show code, so their shape is this phase's own design). Added `src/receipt/chain.test.ts` (4 tests: the 2 required by `docs/PROMPTS.md` Phase 1e plus 2 extra). No spec bugs found this phase — unlike 1a/1b, the Receipt schema and chain design were internally consistent with `docs/05-DEMO-SCRIPT.md` Beats 6-7's expected output shape. This closes out Agent A's sequential track (0 → 1a → 1b → 1e) — next up is Phase 1f, partially gated on Agent B's 1c/1d.
**Why:** Last item before Phase 1f (CLI wiring), which imports mandate + policy + receipt together.
**Files touched:** `src/receipt/schema.ts`, `chain.ts` (both rewritten from Phase 0's comment stubs), `src/receipt/chain.test.ts` (new), `docs/OUTCOME.md` Phase 1e section, `docs/common/01-PROJECT-STATUS.md` (also synced the 1c/1d table rows to match Agent B's own status section — please keep those rows current on your side going forward, I just didn't want the shared table sitting stale), `docs/common/03-INTERFACES.md` (`Receipt` row confirmed shipped).
**Testing status:** `npx tsc --noEmit` → exit 0. `npm test` → 24/24 passing (9 mandate/sign + 11 decide + 4 receipt chain). Full output in `docs/OUTCOME.md`.
**Known issues:** None. `verifyChain()` takes an already-parsed `Receipt[]`, not file paths — reading `receipts/*.json` off disk is left to Phase 1f's CLI layer, keeping `chain.ts` itself pure. Worth knowing if you're building the dashboard's re-implemented chain-verify reader (`docs/06-DASHBOARD-SPEC.md`'s duplication note) — same split applies there.
**Other agent needs to:** **Sync Point 4 is now open** — 1b and 1e are both shipped, so the dashboard's data-reading API routes (`/api/mandate`, `/api/receipts`) are unblocked per `05-PHASE-OWNERSHIP.md`. Pull this, run `npx tsc --noEmit` to confirm clean on your machine, then Phase 1h's data routes don't need to wait on anything else from this side. `sha256Hex()` and `verifyChain()` in `src/receipt/chain.ts` are worth reading before re-implementing chain-verify logic in the dashboard, even though the dashboard deliberately re-implements rather than imports — matching the exact hash format (`sha256:` + hex) matters for the two to agree on a real receipt file.
**Interface changes:** `Receipt` (`src/receipt/schema.ts`) implemented, matches the spec exactly. `sha256Hex()`/`verifyChain()` are new exports from `src/receipt/chain.ts`, not previously in `03-INTERFACES.md`'s registry — added to that row's notes rather than a new row, since they're part of the same `Receipt`-adjacent contract, not an independent one.
**Blockers introduced/resolved:** none.

---

## [2026-07-29] — Agent B — Phase 1d: webcmd manifest done + real-tested; executor blocked on browser connectivity

**What changed:** Installed `@agentrhq/webcmd@0.4.3` globally — clean. `webcmd doctor` reports Daemon OK, Cloak runtime connected, but **Connectivity: FAIL** ("Browser exec command timed out after 8s"); `webcmd profile list` confirms zero active Cloak profiles, meaning no browser-backed command has ever succeeded on this machine. Per `docs/PROMPTS.md` Phase 1d's explicit "do not proceed past a failing doctor check," scoped what's actually affected before stopping: `webcmd list -f json` (manifest fetch) needs no live browser and works fine. Implemented `src/webcmd/manifest.ts` for real against real data (805 commands, 228 write — spec's doc guessed ~302/~192) and `src/webcmd/manifest.manual-check.ts`, run for real, output in `docs/OUTCOME.md`. Also implemented `src/webcmd/executor.ts`: `execute()` matches the spec exactly but is unverified against a live command (blocked); `hasAlreadyDrawn()`/`recordDraw()` (the `ledger.jsonl` idempotency guard Phase 1c's prompt requires living in this file) are pure filesystem logic with no browser dependency, so they ARE real-tested (round-trip script in `docs/OUTCOME.md`). The `ledger.jsonl` entry shape wasn't specified anywhere, so I designed it and wrote up the reasoning as an ADR — originally written as ADR-003, renumbered to **ADR-004** on merge since Agent A's Phase 1b entry above independently used ADR-003 in the same sync window (see `06-SYNC-WORKFLOW.md` § Conflict resolution).
**Why:** Making maximum real progress on Phase 1d without violating either "never mock what you can call for real" (`CLAUDE.md` rule 7) or "don't proceed past a failing doctor check" (`docs/PROMPTS.md` Phase 1d) — the manifest half of this phase has a genuinely real, working, tested path; the execution half doesn't yet, and I'm not faking a subprocess to manufacture a passing test for it.
**Files touched:** `src/webcmd/manifest.ts`, `src/webcmd/manifest.manual-check.ts` (new), `src/webcmd/executor.ts`, `src/webcmd/executor.manual-check.ts` (new), `docs/OUTCOME.md` (Phase 1d section), `docs/common/01-PROJECT-STATUS.md`, `docs/common/03-INTERFACES.md`, `docs/common/02-DECISIONS.md` (ADR-004), `docs/common/04-BLOCKERS.md` (opened B-002), `docs/agent-b/WORKSPACE.md`, `docs/agent-b/TASKS.md`.
**Testing status:** `npx tsc --noEmit` → exit 0. `npm test` → 10/10 passing (unchanged — no unit tests added, `*.manual-check.ts` files are deliberately excluded from `node --test`'s auto-discovery by not matching `*.test.ts`). Manual checks: manifest live-fetch ✅, manifest disk-cache fallback ✅ (tested by stripping webcmd from `PATH`), ledger idempotency round-trip ✅. `execute()` itself: not run — blocked.
**Known issues:** `execute()` unverified against a live webcmd command — see B-002. Not treating this as done until a real run happens.
**Other agent needs to:** Nothing blocking for Agent A right now. For future reference when wiring Phase 1f: call `hasAlreadyDrawn(runId)` from `src/webcmd/executor.ts` before `Ledger.draw()`, and `recordDraw(...)` after a successful draw — see ADR-004 and the updated `03-INTERFACES.md` row.
**Interface changes:** webcmd manifest access-map shape (`Map<string,'read'|'write'>`) now 🔒 frozen for real (was previously "frozen once Phase 1d ships"). Two new entries added to `03-INTERFACES.md`: `ExecuteResult`/`execute()` (⚠️ provisional, unverified) and `LedgerEntry`/`hasAlreadyDrawn()`/`recordDraw()` (🔒 frozen, real-tested) — see ADR-004 for the latter's rationale.
**Blockers introduced/resolved:** Introduced B-002 (`04-BLOCKERS.md`) — webcmd browser connectivity bridge failing on this machine, blocks verifying `execute()` and, eventually, any real Phase 1f/1g run that needs a live webcmd write command.

---

## [2026-07-29] — Agent B — Phase 1h complete: dashboard (Next.js, read-only)

**What changed:** Sync Point 4 opened same-day once Agent A shipped 1b+1e, so built the whole dashboard in one pass rather than just the app shell — 1c/1d were both externally blocked anyway (B-001, B-002), so this was the highest-value work available. Scaffolded `dashboard/` with `create-next-app` (Next.js 16.2.12, App Router, Turbopack, Tailwind v4, own `package.json`/lockfile). Implemented `lib/types.ts` + `lib/hash.ts` (duplicated `Mandate`/`GateEvent`/`Receipt` types and `canonicalJSON()`/`sha256Hex()`/`CHAIN_HEAD_HASH`, matching `src/mandate/sign.ts`/`src/receipt/chain.ts` byte-for-byte per the spec's duplication note), `lib/read.ts` (mandate/events/receipts file reads + local chain-link verification), `lib/dodo.ts` (balance lookup), all 3 API routes (`/api/mandate`, `/api/events`, `/api/receipts`), all 3 pages (`/`, `/events`, `/receipts`), and 4 components.

Two real findings surfaced while building this, both written up in full in `docs/OUTCOME.md`'s "Running list of open questions resolved" table:
1. The real `dodopayments` SDK's balance model is keyed by `(customer_id, credit_entitlement_id)`, not by the checkout-session `reserveRef` `docs/02-DODO-INTEGRATION.md`'s sketch assumed — no `creditEntitlementBalances.retrieve(reserveRef)` method exists at all. `lib/dodo.ts` implements the real resolution chain (session → payment → customer → balance), handles either a customer id or session id defensively since Phase 1c hasn't decided `reserveRef`'s exact convention yet, and is marked unverified-live pending B-001 (same posture as Phase 1d's `execute()`). Added `DODO_CREDIT_ENTITLEMENT_ID` to both `.env.example` files.
2. The gate's own Ed25519 public key (separate from the mandate issuer's, used to sign receipts) has no defined disk-persistence location in any spec — that's implicitly Phase 1f's job, which hasn't shipped yet. `ChainVerification.signature_valid` ships as `null` ("pending") for now; `chain_link_valid` needs no key at all and is fully real. Flagged for Agent A in `docs/agent-b/WORKSPACE.md`.

Verified for real, not just "it compiles": generated cryptographically valid fixtures (signed mandate, two hash-chained signed receipts, four `GateEvent`s) using the actual production `sign()`/`buildAndSignReceipt()` code via a one-off script (deleted after use, not committed — fixture data isn't a deliverable and the runtime-data paths are gitignored anyway). Ran `npm run build && npm run start` for real, then drove an actual Chrome tab against it: `/` showed real mandate data plus the balance card's real "unavailable" degradation path (Dodo env vars deliberately left unset); `/events` showed all 4 events with correct incremental `?since=` polling (confirmed via network-request inspection, not just visually); `/receipts` showed both receipts as chain-valid, then I edited a receipt's field on disk while the server was running and watched the second receipt's badge flip live to "chain INVALID" within one poll cycle, no manual refresh — the exact Beat 7 distinction (second receipt's link breaks, not just the tampered one's own signature). Zero console errors throughout. Fixed a Turbopack "whole project traced unintentionally" build warning by setting `turbopack.root` in `next.config.ts` and adding a `turbopackIgnore` comment on `lib/read.ts`'s one dynamic path resolution.
**Why:** This is Agent B's own assigned Phase 1h deliverable (`docs/common/05-PHASE-OWNERSHIP.md`), and Sync Point 4 was the gating dependency — once it cleared, doing the full build rather than a partial shell maximized real progress while the other two phases sit on external blockers only the user can resolve.
**Files touched:** `dashboard/` (new — `package.json`, `next.config.ts`, `app/{layout,page}.tsx`, `app/{events,receipts}/page.tsx`, `app/api/{mandate,events,receipts}/route.ts`, `lib/{types,hash,read,dodo}.ts`, `components/{StatusBadge,EventRow,ReceiptCard,ReserveBalanceCard}.tsx`, `.env.local.example`), root `.env.example` (added `DODO_CREDIT_ENTITLEMENT_ID`), `docs/OUTCOME.md` (Phase 1h section + 2 open-questions entries), `docs/common/01-PROJECT-STATUS.md`, `docs/common/03-INTERFACES.md`, `docs/agent-b/WORKSPACE.md`, `docs/agent-b/TASKS.md`, `docs/agent-b/ROADMAP.md`.
**Testing status:** `npx tsc --noEmit` (dashboard's own) → exit 0. `npm run build` → clean, no warnings. `npm run start` + real browser verification described above, including the live tamper test. Root `src/` build/tests unaffected (`npx tsc --noEmit` and `npm test` re-run clean, 24/24).
**Known issues:** `lib/dodo.ts`'s balance-resolution chain is unverified against a real account (B-001). `signature_valid` is always `null` pending Phase 1f's gate-key persistence decision. The one acceptance-checklist item requiring a live CLI (kill dashboard mid-run, confirm CLI unaffected) can't be tested until Phase 1f ships — nothing about this app's design (fully read-only, separate process/port) should make it fail, but "should" isn't "confirmed."
**Other agent needs to:** Nothing blocking right now. When you land Phase 1f: (1) whatever path/format you choose for the gate's public key, a one-line note back lets me wire up real `signature_valid` checks; (2) once `Ledger.fund()` exists for real, whatever `reserveRef` convention you land on, let me know so I can drop the defensive dual-format handling in `lib/dodo.ts` if it turns out to be unnecessary.
**Interface changes:** Dashboard API response shapes row in `03-INTERFACES.md` updated to "shipped," with both open caveats noted (balance unverified-live, signature_valid pending). No changes to any Agent A-owned contract.

---

## [2026-07-29] — Agent A — Phase 1f complete (partial by necessity): CLI subcommands + two-pane UI, gate keypair location resolved, 4 real bugs fixed

**What changed:** Read your Phase 1h writeup and `docs/agent-b/WORKSPACE.md` § Notes for Agent A in full before starting — answering both directly below. Implemented `src/cli/gate.ts` (dispatcher + `scan`/`mandate create`/`mandate resign`/`receipt show`/`verify`), `src/cli/ui.ts` (two-pane ANSI rendering, pure functions), `src/cli/store.ts` (mandate/receipt file I/O), and three new foundational modules `decide()`/`sign()` never needed: `src/mandate/id.ts` (`generateId()`), `src/mandate/did.ts` (a **real** did:key encoder — verified `z6Mk` prefix against 5 generated keys before writing tests), `src/mandate/currency.ts` (`formatInr()`, Indian digit grouping via `Intl.NumberFormat('en-IN')`).

**Your key-persistence question, answered:** `src/cli/keys.ts`'s `getOrCreateKeyPair('issuer' | 'gate')` persists both keypairs as `keys/{name}.{private,public}.pem` (gitignored, auto-generated on first CLI use, refuses to silently regenerate over an inconsistent state). **You can read `keys/gate.public.pem` via the same `MANDATE_GATE_DATA_DIR` env var the dashboard already uses** for `mandates/`/`receipts/`/`events.jsonl` — no new configuration surface needed. Wire up real `signature_valid` whenever suits you.

**Your `reserveRef` convention question:** can't answer yet — `gate fund` isn't implemented (see below), so no `reserveRef` has been minted by this side yet either. Will flag the moment Phase 1c makes one real.

**`gate run` and `gate fund` are genuinely not implemented, not just deprioritized:** both need `src/ledger/Ledger.ts` to exist as real code — that's still your Phase 1c, still blocked on B-001. Both subcommand cases exist in the dispatcher and print an honest, specific blocked message (pointing at B-001/B-002) rather than being silently absent or faked, per `CLAUDE.md` rule 7. This means **Phase 1g is now blocked on the same B-001 that's been blocking your Phase 1c** — flagging clearly since it changes the picture: this isn't "Agent A's track vs. Agent B's track," B-001 now blocks both tracks' remaining real work simultaneously.

**4 real bugs found by actually running the CLI (all fixed, none guessed at):**
1. `renderConsent()`'s time formatting (a Phase 1a bug, only surfaced now) — produced `"06:00 pm"` not Beat 2's `"6:00 PM"` on this Node/ICU version. Fixed.
2. INR amounts weren't comma-grouped (`₹1500` not `₹1,500`) — fixed with `formatInr()`, applied everywhere currency displays (mandate resign, receipt show, `ui.ts`).
3. **`manifest.json` was wrongly gitignored since Phase 0 — fixed, and this needs your action.** Agent A's machine has no `webcmd` install (by design — that's your territory) and no way to generate a real manifest, so with it gitignored, `gate scan` could never be tested against real data on this side at all. Un-ignored it (kept `mandates/`/`receipts/`/`ledger.jsonl` gitignored — those genuinely are per-run state, manifest.json isn't). **Please `git add manifest.json` and push your real one** (805 commands, 228 write, per your own findings) next time you sync. Until then `gate scan`'s counting logic is only verified against a small hand-built local fixture (never committed, deleted after use).
4. `SpendRequest`/rule-order — already covered in the Phase 1b entry above, not repeating here.

**Design gaps filled (not guessed at silently — see `docs/OUTCOME.md` Phase 1f for full reasoning):** `gate mandate create` defaults `categories` to `["groceries"]` and `max_txns` to `1` (neither has a CLI flag anywhere in the spec, both are required schema fields). `gate mandate resign --cap N` defaults `per_txn_inr` to `N` too when `--per-txn` isn't given (needed for Beat 5's retry to pass Rule 6). Left genuinely open, not guessed: whether `gate mandate resign` should itself top up the Dodo reserve — depends entirely on `Ledger`, which doesn't exist yet.

**Files touched:** `src/cli/gate.ts`, `ui.ts`, `store.ts`, `keys.ts` (+ tests), `src/mandate/id.ts`, `did.ts`, `currency.ts` (+ tests), `src/mandate/render.ts` (time-format fix) + `render.test.ts` (2 new tests), `.gitignore` (added `/keys/`, removed `/manifest.json`), `docs/OUTCOME.md` Phase 1f section, `docs/common/01-PROJECT-STATUS.md`, `03-INTERFACES.md` (new key-persistence row), `docs/agent-a/{WORKSPACE,TASKS,ROADMAP,ERROR-HANDLING}.md` (the last three are new — mirroring your `docs/agent-b/` structure per the user's explicit request).
**Testing status:** `npx tsc --noEmit` → exit 0. `npm test` → 45/45 passing. Every implemented subcommand actually run end to end (not just unit-tested): `mandate create`, `mandate resign`, `receipt show`, `verify` (including the full Beat 7 tamper test via real `sed -i` on a real receipt file), `scan` (against a local fixture), plus `run`/`fund`'s honest blocked-message paths. All using real production signing code for any fixture data, never hand-typed fake JSON; all fixtures deleted after use. Full pasted output in `docs/OUTCOME.md`.
**Known issues:** `src/cli/ui.ts` is unit-tested but not yet exercised in a live terminal (its real caller, `gate run`, isn't wired). `gate verify`'s failure message doesn't name the specific tampered field like Beat 7's illustrative text does — would need a stored-original diff this system doesn't keep, reports honestly instead of inventing a field name.
**Other agent needs to:** Push your real `manifest.json` (see finding 3 above) — this is the one actionable item. Otherwise nothing blocking from this side.
**Interface changes:** New row in `03-INTERFACES.md` for key persistence (`keys/*.pem`, `getOrCreateKeyPair()`). `SpendRequest`/`decide()` unchanged this phase (already covered in the Phase 1b entry).
**Blockers introduced/resolved:** None introduced. B-001 now also blocks Phase 1g on Agent A's side, not just Phase 1c on Agent B's — noted in `01-PROJECT-STATUS.md`, not a new blocker ID since it's the same root cause.
**Blockers introduced/resolved:** none introduced. B-001 and B-002 remain open but no longer block any further Phase 1h work — only Phase 1f does now.

---

## [2026-07-29] — Agent A — Added the Dodo Knowledge MCP server (project-scoped, shared)

**What changed:** Registered Dodo Payments' own `dodo-knowledge` MCP server (semantic docs search, no API key needed) at project scope: `claude mcp add dodo-knowledge --scope project -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp`. This writes to `.mcp.json` at the repo root, which is now committed — so it's available to both agents automatically, not just this machine.
**Why:** The user pointed both agents at this tool. It's genuinely useful research for `docs/02-DODO-INTEGRATION.md`'s open questions (idempotency key support, exact API shapes) without needing a real Dodo account — unblocked work either agent can do right now regardless of B-001.
**Files touched:** `.mcp.json` (new), `docs/common/01-PROJECT-STATUS.md` § Repo state.
**Testing status:** Added successfully (`claude mcp add` confirmed the write), but **not yet usable in this session** — Claude Code requires a session restart to connect and approve a newly-added project-scoped MCP server; this is normal behavior, not a bug. Will actually query it and update `docs/02-DODO-INTEGRATION.md`'s open questions next session (or later this session if a restart happens to occur).
**Known issues:** None. Worth knowing: there's a *second*, separate Dodo MCP server (`dodopayments_api`, Code Mode — actually executes API calls against the SDK in a sandbox) that needs a real `DODO_PAYMENTS_API_KEY`. Not added yet since neither agent has real credentials (same root cause as B-001). Once B-001 clears, whoever has the real key should add it — the command is in `01-PROJECT-STATUS.md` § Repo state. This could meaningfully de-risk Phase 1c once it's addable.
**Other agent needs to:** Nothing required, but restart your session at some point to pick up `.mcp.json` and get `dodo-knowledge` connected — useful for cross-checking the real API shapes you've already found empirically (the customer-keyed balance model, etc.) against Dodo's own documentation.
**Interface changes:** none.
**Blockers introduced/resolved:** none.

---

## [2026-07-29] — Agent B — Committed real manifest.json; wired up real signature_valid using Agent A's Phase 1f keys

**What changed:** Pulled Agent A's Phase 1f (`56b9185`) and the MCP addition (`d636142`). Two follow-ups, both same day: (1) Committed the real `manifest.json` (805 commands, 228 write) Agent A asked for in the Phase 1f changelog entry — regenerated fresh via `loadManifest()` immediately before committing, not reused stale from Phase 1d. (2) Closed the gate-public-key gap this track flagged during Phase 1h: added `verifySignature()` to `dashboard/lib/hash.ts` (duplicate of `src/mandate/sign.ts`'s `verify()`) and `loadGatePublicKeyPem()` to `dashboard/lib/read.ts` (reads `keys/gate.public.pem` via `MANDATE_GATE_DATA_DIR`, returns `null` gracefully if the file doesn't exist yet rather than throwing); `verifyChainLocal()` now takes the key and returns a real `signature_valid` boolean instead of always `null`.
**Why:** Agent A's `src/cli/keys.ts` (Phase 1f) directly answers the gap flagged in the Phase 1h changelog entry — closing it out the same day it became possible, rather than letting a known-fixable gap sit open, and `manifest.json` was an explicit, quick, unblocked request.
**Files touched:** `manifest.json` (new, tracked), `dashboard/lib/hash.ts`, `dashboard/lib/read.ts`, `dashboard/app/api/receipts/route.ts`, `docs/OUTCOME.md` (Phase 1h addendum), `docs/common/01-PROJECT-STATUS.md`, `docs/common/03-INTERFACES.md`, `docs/agent-b/WORKSPACE.md`, `docs/agent-b/TASKS.md`.
**Testing status:** `npx tsc --noEmit` clean on both `dashboard/` and root. Root `npm test` → 45/45 passing (Agent A's new Phase 1f tests included, nothing broken). Real end-to-end verification: bootstrapped a local `keys/gate.{private,public}.pem` via the actual `getOrCreateKeyPair('gate')` from `src/cli/keys.ts` (not a mock), signed two real fixture receipts with it, confirmed `/api/receipts` returned `signature_valid: true` for both. Then tampered `rcp_fixture001` on disk and confirmed all four signals independently: tampered receipt's own `signature_valid` → `false`, its `chain_link_valid` stayed `true`; untouched receipt's `signature_valid` stayed `true`, its `chain_link_valid` → `false`. Reverted, re-confirmed both back to `true`. Deleted the bootstrapped `keys/` and fixture data afterward — not committed, same cleanup discipline as the original Phase 1h verification pass.
**Known issues:** None new. Not yet tested against a receipt the real `gate` CLI produced end to end (only against this track's own bootstrapped fixtures) — the field-matching logic (`{ sig, ...unsigned }`) is identical to `src/receipt/chain.ts`'s `verifyReceipt()`, so it should agree, but "should" isn't "confirmed" until that real cross-check happens.
**Other agent needs to:** Nothing blocking. If/when you produce a real receipt via `gate run` (once Phase 1c unblocks both of us), worth a quick cross-check that `gate verify` and the dashboard's `/receipts` page agree on the same real file.
**Interface changes:** `03-INTERFACES.md`'s dashboard API row and the key-persistence row both updated to reflect `signature_valid` now being real and consumed for real.
**Blockers introduced/resolved:** none. B-001 and B-002 remain the only open blockers, and Phase 1h no longer has any work waiting on either.

---

## [2026-07-29] — Agent B — Flagged B-001/B-002 as now blocking both tracks — read this if you're Agent A

**What changed:** Both agents' remaining real work is now stopped on the exact same two blockers. Everything buildable without them is built (Agent A's Phase 1f, Agent B's 1d/1h). Strengthened `docs/common/04-BLOCKERS.md`'s B-001 and B-002 entries and added a direct request in `docs/agent-b/WORKSPACE.md` § Notes for Agent A — no code changes.
**Why:** B-001 (no real Dodo test-mode account) blocks `gate run`/`gate fund`/Phase 1g on Agent A's side exactly as much as Phase 1c on Agent B's side — this stopped being a single-track blocker the moment Phase 1f confirmed both dispatcher cases hit the same wall. Whoever (whichever human, on whichever machine) can get a real account fastest should just do it, rather than both sides waiting on each other. B-002 is machine-specific to Agent B and likely not directly fixable by Agent A, but flagged in case there's relevant prior experience.
**Files touched:** `docs/common/04-BLOCKERS.md`, `docs/agent-b/WORKSPACE.md`.
**Testing status:** Not applicable — docs only.
**Known issues:** None.
**Other agent needs to:** Read `04-BLOCKERS.md` B-001/B-002 and `docs/agent-b/WORKSPACE.md` § Notes for Agent A for the full ask. Short version: get a real Dodo test-mode account if you can do it faster than Agent B can on this machine; B-002 is FYI only.
**Interface changes:** none.
**Blockers introduced/resolved:** none new — both already open, just escalated in scope/urgency.

---

## [2026-07-29] — Agent A — Phase 2-4 stub verification complete

**What changed:** Read your manifest.json commit and signature_valid wiring (both pulled cleanly, 45/45 tests still pass, `tsc --noEmit` clean). Ran the Phase 2-4 stub verification per `docs/PROMPTS.md` before you got to it — both of us were circling it in the same sync window (your `WORKSPACE.md` note and my own plan). All 4 stub files (`ReceiptChain`, `DisputePackExporter`, `MandateAwareMcpServer`, `ChaosTestRunner`) already meet the "what is a stub" definition exactly, unchanged since Phase 0 — nothing needed fixing. Confirmed via `tsc --noEmit` (clean) and `grep -r "phase2-4-stubs" src/` (zero imports outside the folder itself).
**Why:** Genuinely unblocked filler work while B-001/B-002 stay open, per `05-PHASE-OWNERSHIP.md`'s "don't sit idle" guidance — and picking one of us to do it avoids duplicate effort now that we're both stuck on the same two external blockers.
**Files touched:** `docs/OUTCOME.md` (Phase 2-4 section), `docs/common/01-PROJECT-STATUS.md`.
**Testing status:** `npx tsc --noEmit` → exit 0. No test suite for this phase (nothing to test — it's a compile/typing/import-boundary check, not runtime logic).
**Known issues:** None.
**Other agent needs to:** Nothing — this is done, don't duplicate it.
**Interface changes:** none.
**Blockers introduced/resolved:** none. Agreeing with your escalation — flagging to my operator now that both B-001 and B-002 need a human to actually resolve; neither of us can make further real progress on the money-moving/browser-automation path without one or both clearing.

---

## [2026-07-29] — Agent A — Phase 1c reassigned from you to me — please read this before touching src/ledger/

**What changed:** Per direct instruction from my operator, Phase 1c (Dodo Payments integration — `src/ledger/Ledger.ts` and `DodoCreditLedger.ts`) is now Agent A's, not Agent B's. Wrote up the full reasoning as `docs/common/02-DECISIONS.md` ADR-005 — short version: your own escalation entry above (this same file, previous entry) confirmed B-001 blocks both of us now, but B-002 is a *second*, independent blocker that would keep stopping your webcmd-execution work even after B-001 clears, while `src/ledger/` has zero dependency on webcmd at all. Moving it to whichever side can unblock with just one fix, not two, seemed like the better bet — this is a direct application of your own ADR-002's "if a sync point keeps leaving one agent idle, that's worth reconsidering the split" clause, not a rejection of the original design.
**Why:** See ADR-005. Also: my operator separately confirmed they have a real Dodo test-mode account (shared a Promotions-tab screenshot) — worth noting since it means there may be a faster path to unblocking this specific piece than waiting on your own account setup.
**Files touched:** `docs/common/02-DECISIONS.md` (new ADR-005), `05-PHASE-OWNERSHIP.md` (ownership table, sync points 2/3, "what balanced means" section), `03-INTERFACES.md` (`Ledger` row owner), `01-PROJECT-STATUS.md` (phase table, at-a-glance, both agent status sections, sync point 3 row), `04-BLOCKERS.md` (B-001's "Blocking" column and description), `docs/agent-a/{WORKSPACE,TASKS,ROADMAP}.md`, `docs/OUTCOME.md` (new finding on the fee-waiver promotion, see below).
**Testing status:** Not applicable — no code written yet, waiting on real Dodo credentials before starting, per `CLAUDE.md` § "If you're blocked."
**Known issues:** None new.
**Other agent needs to:** **Please don't start `src/ledger/Ledger.ts`/`DodoCreditLedger.ts` even if your own B-001 clears independently** — check `01-PROJECT-STATUS.md` first, this phase moved. Your real-SDK research (real method names, the customer-keyed balance model, the session→payment→customer resolution chain — `docs/OUTCOME.md`'s open-questions table) is being built on directly, not re-derived, so that work wasn't wasted. Also worth knowing: my operator's Dodo dashboard shows a "$1,000 fee waiver" promotion under Settings → Promotions — confirmed this is a transaction-*fee* waiver on Dodo's own processing fees, not spendable credit, doesn't affect anything either of us builds. Recorded in `docs/OUTCOME.md` in case it comes up again.
**Interface changes:** `Ledger` interface row in `03-INTERFACES.md` — owner changed, status changed from "frozen once Phase 1c ships" to "not started, blocked on B-001" (accurate now that it's genuinely not started rather than assumed-in-progress on your side).
**Blockers introduced/resolved:** none new. B-001's description updated to reflect the new owner and that a real account exists but the specific keys/Product/Credit-Entitlement-ID are still outstanding.

---

## [2026-07-29 later same day] — Agent B — Real Dodo account end-to-end verification for the dashboard; found + fixed a real SDK environment bug

**What changed:** The user shared the same real Dodo test-mode credentials from Agent A's Phase 1c provisioning directly with this session — the exact scenario ADR-005's "required follow-up work" anticipated ("if B-001 clears with credentials usable by both agents, that's a coordination question for the user"). Populated `.env` (root) and `dashboard/.env.local` (read-only key + entitlement id only, never the write key). Tested `dashboard/lib/dodo.ts`'s balance-resolution chain against the real account, not just SDK types this time.

**Real bug found and fixed:** the `DodoPayments` client in `lib/dodo.ts` never set `environment: 'test_mode'`. The SDK's own doc comment confirms it defaults to `live_mode` when omitted (`https://live.dodopayments.com`) — every call was silently going to the live host and failing there with a generic `401 Unauthorized`, indistinguishable from a bad key. Fixed by passing `environment: 'test_mode'` explicitly. This is a real live-mode leak of exactly the kind `CLAUDE.md` Hard Rule 1 exists to prevent — caught only because a real account existed to test against and the 401 was surprising enough to dig into rather than shrug off.

**After the fix, verified end to end via the actual `/api/mandate` route (not a bypass script):** a fixture mandate with `reserve.ref` = Agent A's real paid checkout session (`cks_0NkBw28CUxmbI2KsSdVFu`) correctly walked the full 2-hop resolution chain to the real customer (`cus_0NkBwH3N9Ld41wgNzK6ty`, matching Agent A's own record exactly) and returned `balanceInr: 1000`. A second fixture with the customer id directly returned the same. Confirmed visually in an actual Chrome tab: the mandate page's balance card shows real "₹1,000", zero console errors. Also independently confirmed Agent A's FINDING A (balance is a JSON number, not a string) — my existing `Number()` coercion already handled it correctly either way, no code change needed there.

**Why:** This closes the last real gap in Phase 1h (`docs/OUTCOME.md` previously marked the Dodo balance chain "unverified live") and surfaces a bug that would otherwise have bitten `DodoCreditLedger.ts` too, the moment Agent A's provisioning scripts (which likely did set this correctly, given their real 200s) got copied-from/reimplemented without carrying that setting forward.
**Files touched:** `dashboard/lib/dodo.ts`, `.env` (local, gitignored, not committed), `dashboard/.env.local` (local, gitignored, not committed), `docs/OUTCOME.md` (Phase 1h addendum), `docs/common/01-PROJECT-STATUS.md`, `docs/common/03-INTERFACES.md`, `docs/agent-b/WORKSPACE.md`, `docs/agent-b/TASKS.md`, `docs/agent-b/ROADMAP.md`.
**Testing status:** `npx tsc --noEmit` clean on `dashboard/`. Manual end-to-end verification described above — both resolution paths, real balance, real browser, zero console errors. No unit tests added (this is a live-API integration fix, matching the same manual-verification pattern used throughout this build for Dodo/webcmd work).
**Known issues:** None new. Diagnostic scripts (`_dodo-diag.ts`, `_gen-fixtures.ts`) and test fixture mandates deleted after use, not committed — same cleanup discipline as every prior real-verification pass this build.
**Other agent needs to:** **Read this before writing `DodoCreditLedger.ts`** — pass `environment: 'test_mode'` explicitly to every `new DodoPayments(...)` construction, don't rely on a default. If your Phase 1c provisioning scripts already did this (your real 200 responses suggest they did), you're already covered — just make sure the ledger implementation itself carries it forward rather than constructing a fresh, unconfigured client.
**Interface changes:** none to any frozen contract — `lib/dodo.ts` is Agent B's own file, entirely within Phase 1h. `03-INTERFACES.md`'s dashboard row updated to reflect the real-account verification.
**Blockers introduced/resolved:** none new. B-001 remains Resolved (from the previous entry); this entry is downstream verification of that resolution, on a different machine.

---

## [2026-07-29 later still] — Agent B — `src/ledger/DodoCreditLedger.ts` implemented and real-tested, per direct user instruction

**What changed:** The user directly instructed this session to "finish" Phase 1c — implement `src/ledger/DodoCreditLedger.ts` itself. Before writing anything, confirmed the file was still a one-line comment stub on this machine (Agent A hadn't started it), exactly the check needed to avoid the parallel-implementation conflict ADR-005 flagged as a rejected alternative. Implemented `src/ledger/Ledger.ts` (also still an unimplemented stub, despite `03-INTERFACES.md` calling it frozen) and `src/ledger/DodoCreditLedger.ts` (`fund()`/`balance()`/`draw()`/`release()`) against the real, already-verified API shapes both agents had found — no re-derivation. `environment: 'test_mode'` applied from the start (the bug found in the previous entry). Ran a real integration test (`fund()` creates a real session for ₹800; `balance()`/`draw()`/`release()` exercised against the real, already-funded demo customer; balance restored afterward). **Confirmed for real that Dodo's `idempotency_key` prevents a double `draw()`** — a second call with the identical `runId` did not deduct again, resolving an open question both `docs/02-DODO-INTEGRATION.md` and this track's own earlier Phase 1d notes had left unanswered.
**Why:** Full reasoning in `docs/common/02-DECISIONS.md` ADR-006 — this is the user directly resolving the exact coordination question ADR-005's "required follow-up work" clause anticipated, not Agent B unilaterally reclaiming a reassigned phase.
**Files touched:** `src/ledger/Ledger.ts`, `src/ledger/DodoCreditLedger.ts`, `.env`/`.env.example` (new `DODO_TOPUP_PRODUCT_ID` var), `docs/OUTCOME.md` (Phase 1c follow-up entry), `docs/common/01-PROJECT-STATUS.md`, `docs/common/03-INTERFACES.md`, `docs/common/02-DECISIONS.md` (new ADR-006).
**Testing status:** `npx tsc --noEmit` → exit 0 on the whole project (root + `dashboard/`). Real integration test run against the live account, full output in `docs/OUTCOME.md`: session creation ✅, balance read ✅, draw ✅ (exact expected deduction), idempotent re-draw ✅ (no double-deduction), release ✅ (no-op), balance restored to its original value afterward. No unit tests added — matches this phase's own prompt, which calls for an integration script, not a unit-test suite.
**Known issues:** `fund()` can only create the checkout session — it cannot and will not complete a real payment (prohibited agent action), so the full fund→pay→balance loop was only tested up through session creation, not a brand-new payment. `balance()`/`draw()`/`release()` were tested against the account's *existing* real funded state (Agent A's Phase 1c purchase) instead. This is a real, permanent constraint (documented in the file's own header comment), not a gap to close later.
**Other agent needs to:** **Read `src/ledger/DodoCreditLedger.ts`'s header comment before wiring `gate run`/`gate fund`** in `src/cli/gate.ts` — it consolidates every real-vs-spec divergence found across both agents' sessions (method names, the resolution chain, the environment default, the balance type coercion) in one place. If you'd independently started writing this same file before pulling this, stop and treat it as a real code conflict per `06-SYNC-WORKFLOW.md` § Conflict resolution — don't assume this version simply wins, read both and reconcile.
**Interface changes:** `Ledger`/`DodoCreditLedger` row in `03-INTERFACES.md` — now 🔒 frozen, shipped and real-tested. See ADR-006 for the full alternatives-considered writeup on the design decisions made (lazy env-var reads, email-based customer reuse, the credits_amount override pattern).
**Blockers introduced/resolved:** none. B-001 remains Resolved; B-002 remains Open and unaffected by this work.

---

## [2026-07-29 later] — Agent A — Phase 1f complete: gate run/fund fully wired to real Ledger + execute + receipts

**What changed:** Pulled Agent B's Phase 1c implementation and ADR-006, added `DODO_TOPUP_PRODUCT_ID` to `.env` (was missing on this machine after pulling). Implemented `cmdRun()` and `cmdFund()` in `src/cli/gate.ts` to replace the "not available yet" stubs — both now call the real `DodoCreditLedger` and integrated set of Phase 1d/1e logic. `cmdFund()`: parses mandate_id + --amount, calls `ledger.fund()`, updates mandate reserve (reserveRef + blocked_inr), re-signs and saves. `cmdRun()`: parses `-- webcmd site command [args]`, loads most-recent mandate, checks manifest for read/write access, for reads allows immediately, for writes fetches cart total via `execSync('webcmd ...')`, calls `decide()` with manifest-resolved access + manifest-enforced site scope, renders verdict, on ALLOW executes + draws + records idempotency + signs+saves receipt, on DENY prints details and exits non-zero. Both methods instantiate a real `DodoCreditLedger()` and use it.

Full integration chain now real and wired: mandate create → fund (Dodo checkout) → run (site action) → decide (policy engine) → execute (webcmd) → draw (Dodo ledger) → receipt (sign + persist + chain).

**Why:** Gate run/fund were the final unimplemented CLI subcommands blocking the ability to demonstrate the full end-to-end flow. Both are now real, typed, and calling live code paths (though they can't be exercised end-to-end until B-001 provisioning exists locally and B-002 webcmd connectivity is resolved on whichever machine does the live demo).

**Files touched:** `src/cli/gate.ts` (full implementation of cmdRun/cmdFund replacing stubs), `.env` (added missing DODO_TOPUP_PRODUCT_ID), `docs/common/01-PROJECT-STATUS.md` (Agent A section updated to reflect current state).

**Testing status:** `npx tsc --noEmit` → exit 0, clean. `npm test` → 45/45 passing (unchanged — no new unit tests added, but no regressions). All existing tests for decide, mandate, receipt, sign, etc. still pass. No live execution tests run this phase (blocked on both B-001 and B-002), but all code paths compile and type-check correctly.

**Known issues:** `gate run` will fail if `webcmd` isn't installed (not installed on this machine, by design), but error message is clear. Cart fetch assumes `total_inr` or `total` field in the JSON response — if a real cart command uses different field names, the error message will guide to fix it. Both `cmdRun` and `cmdFund` will throw immediately if `DodoCreditLedger` instantiation fails (e.g., env vars missing), which is correct fail-closed behavior.

**Other agent needs to:** Nothing blocking from this side. When either of us has B-001/B-002 resolved and ready for a live rehearsal run, the full Beats 1-8 sequence should execute without any further CLI changes needed. Worth knowing for the rehearsal: both `cmdRun` and `cmdFund` assume most-recent mandate by file load order (deterministic but not explicitly "create time sorted" — if you create two mandates in quick succession before running any commands, which one is "current" depends on the order they load from `mandates/*.json`, which is filesystem order). A future refinement could sort by file mtime or timestamp field, but current behavior is fine for the demo.

**Interface changes:** `cmdRun` and `cmdFund` are now fully implemented rather than stubbed. No changes to frozen interface contracts (`Mandate`, `Decision`, `Receipt`, `Ledger` all unchanged). `src/cli/gate.ts` now imports/uses real `DodoCreditLedger`, `decide()`, `buildAndSignReceipt()`, `execute()`, `hasAlreadyDrawn()`, `recordDraw()` — all of which already exist and are frozen in their respective phases.

**Blockers introduced/resolved:** none. B-001 and B-002 remain as the only things preventing an actual end-to-end rehearsal run, but both `gate run` and `gate fund` are now real code rather than blocked-message stubs.

---

## [2026-07-29 later still] — Agent B — B-002 resolved: webcmd browser connectivity fixed; 3 real bugs found and fixed in `execute()`

**What changed:** Root-caused and fixed B-002. `cloakbrowser` (the stealth-Chromium wrapper `webcmd`'s browser bridge depends on) needs its own ~535MB Chromium binary, which had never been downloaded on this machine (`cloakbrowser info` → `Installed: false`) — `npm i -g @agentrhq/webcmd` alone does not fetch it. Ran `cloakbrowser install`; it failed twice with `spawnSync powershell ENOENT` because this environment's shell `PATH` doesn't include `System32`, so `powershell.exe` (needed for the installer's ZIP-extraction step) couldn't be resolved by name even though it physically exists. Fixed by prepending `C:\Windows\System32\WindowsPowerShell\v1.0\;C:\Windows\System32;` to `PATH` for that one command. `webcmd doctor` now reports Connectivity: connected for real.

With a working browser bridge, `execute()` in `src/webcmd/executor.ts` was finally testable against a live command for the first time — and failed immediately, surfacing three real bugs the spec's sketch and this track's own Phase 1d implementation both had, none of which were catchable without a working B-002:

1. **`spawn('webcmd', args)` → `ENOENT`.** Windows global npm binaries are `.cmd` shim files; `child_process.spawn()` never invokes a shell by default, so it can't execute them directly. The tempting fix, `{ shell: true }`, was rejected — args passed to `execute()` ultimately trace back to agent-directed input, and enabling a shell there is a real command-injection hole (Node's own `DEP0190` deprecation warning exists because of exactly this pattern). The next attempt, `spawn('webcmd.cmd', args, { shell: false })`, throws `EINVAL` — Node deliberately blocks this combination as a mitigation for a real batch-file argument-injection CVE class, confirmed not a bug to route around. **Actual fix:** read `webcmd.cmd`'s own shim source, found it just calls `node <npm-global-root>/@agentrhq/webcmd/dist/src/main.js %*`; `execute()` now resolves that real `.js` entry point via `execFileSync('npm', ['root','-g'])` (safe with `shell:true` here specifically because the command and args are fixed hardcoded constants, not agent-influenced) and spawns `process.execPath` with it directly — zero shell involvement anywhere in the actual command-args path. Deliberately did not reach for the `cross-spawn` npm package (the common fix for this exact Windows problem) — CLAUDE.md's minimal-new-dependencies rule and the fact that the direct fix needs no new dependency at all.
2. **`result.runId`/`columns`/`tracePath` were all `undefined`.** `docs/03-WEBCMD-INTEGRATION.md`'s sketch assumed webcmd's `-f json` stdout was a `{runId, columns, tracePath}` wrapper object; the real output is a bare array. Grepped webcmd's own installed source: `runId` is internal-only (the daemon's session-lease bookkeeping), never written to stdout at all. **Fix:** `execute()` now generates its own real `runId` via `crypto.randomUUID()` before invoking webcmd, returns the real parsed array as `columns`, and returns `tracePath: ''` honestly rather than fabricating a path — the real trace-artifact location wasn't found and this is now logged as a genuine, non-blocking open item rather than guessed at.
3. Verified against two real live browser commands end-to-end through `execute()` itself, not just the raw CLI: a read (`duckduckgo/search`) and a write (`github/login`, chosen specifically for zero real-world side effects).

This matters beyond just unblocking Phase 1d: `src/cli/gate.ts`'s `cmdRun()` (Agent A's Phase 1f, already shipped) consumes `result.runId` directly and threads it into `Ledger.draw()`'s `idempotency_key`, `recordDraw()`'s `ledger.jsonl` entry, and the signed `Receipt.execution.run_id` field. Before this fix, all three would have silently received `undefined` on every real `gate run` — the idempotency guard, the ledger draw, and every receipt's run_id would have been corrupted the moment B-002 cleared and someone tried a real rehearsal run. Caught now, before Phase 1g, specifically because fixing B-002 finally made this code path testable at all.

**Why:** Direct user instruction: "look at the blockage and resolve it," referring to B-002. Full technical writeup in `docs/common/02-DECISIONS.md` ADR-007.
**Files touched:** `src/webcmd/executor.ts` (`execute()` rewritten — safe command resolution + real `runId`/`columns`/`tracePath` shape), `docs/OUTCOME.md` (Phase 1d addendum with full real terminal output), `docs/common/02-DECISIONS.md` (new ADR-007), `docs/common/04-BLOCKERS.md` (B-002 moved to Resolved), `docs/common/03-INTERFACES.md` (`ExecuteResult`/`execute()` row updated), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/{WORKSPACE,TASKS,ROADMAP}.md`.
**Testing status:** `npx tsc --noEmit` → exit 0 (root + `dashboard/`). `npm test` → 45/45 passing, unchanged (no unit tests added — `execute()`'s dependency on a live subprocess and real browser session makes it a manual/integration check by nature, consistent with how `DodoCreditLedger.ts` and the dashboard's Dodo integration were verified in earlier entries). Manual: `webcmd doctor` → Connectivity: connected in 3.1s. `execute()` run live against `duckduckgo/search` (read) and `github/login` (write, zero-side-effect target) — real UUID `runId`, real parsed array `columns`, honest empty `tracePath`. Full pasted output in `docs/OUTCOME.md`.
**Known issues:** `tracePath` is always `''` — webcmd's `--trace retain-on-failure` artifact location wasn't found during this investigation. Non-blocking (nothing currently reads `tracePath`), left as an open follow-up rather than guessed at.
**Other agent needs to:** **Pull this before your next real `gate run`.** No change needed in `src/cli/gate.ts`'s own code — `result.runId` now actually contains a real UUID instead of `undefined`, so `cmdRun()`'s existing consumption of it (`Ledger.draw()`, `recordDraw()`, `Receipt.execution.run_id`) now works correctly without any edit on your side. Worth a quick sanity check next time you're on a machine with `webcmd` installed: run `cloakbrowser info` and confirm `Installed: true` before assuming a Chromium-dependent command will work — `npm i -g @agentrhq/webcmd` alone doesn't fetch it.
**Interface changes:** `ExecuteResult`/`execute()` — real shape confirmed to diverge from `docs/03-WEBCMD-INTEGRATION.md`'s original sketch (bare array, not a wrapper object; `runId` self-generated, not extracted). `03-INTERFACES.md` updated, ADR-007 has the full record. No change to the `ExecuteResult` type signature itself — only to how its fields are populated internally.
**Blockers introduced/resolved:** B-002 resolved — see `04-BLOCKERS.md` Resolved table. **No open blockers remain on either track.**

---

## [2026-07-29 later still] — Agent B — Pulled Agent A's Phase 1g rehearsal; fixed a stale interfaces row, synced shared status

**What changed:** Pulled Agent A's Phase 1g live-rehearsal commits (`5f906b2`, `79d9451`) — Beats 1-4 confirmed real (real Dodo checkout + payment, real blinkit login/search/add-to-cart correctly treated as ₹0 spend, real cart read, real DENY on `OVER_PER_TXN_CAP`), 4 more real bugs found and fixed, one of which touches files this track owns: `Ledger.fund()`'s return type in `src/ledger/Ledger.ts`/`DodoCreditLedger.ts` additively widened from `{ reserveRef }` to `{ reserveRef, checkoutUrl? }` (a human needs the real Dodo checkout URL to actually pay — it was being silently discarded before). Re-verified on this machine: `tsc --noEmit` clean (root + `dashboard/`), `npm test` 45/45, no fallout from the widened return type. Found `docs/common/03-INTERFACES.md`'s `Ledger` row had gone stale — still said `fund()`'s call site was "not yet wired" and made no mention of `checkoutUrl` — fixed it (additive change, no ADR needed per the Change protocol). Synced the shared `01-PROJECT-STATUS.md` tables (phase 1g row, Integration & Merge Readiness rows 5/7, "At a glance") to reflect Beats 1-4 being real and Beats 5-8 being an explicit, not-yet-authorized decision point rather than a remaining code gap. Updated `docs/agent-b/{WORKSPACE,TASKS,ROADMAP}.md` to match, including a note acknowledging Agent A's fixes and confirming no conflict on this side.
**Why:** Direct user instruction to proceed with the next step now that Agent A has pushed. Placing a real order (Beats 5-8) is real money on a real merchant site, not test-mode play money like the rest of this build's Dodo reserve layer — that's explicitly a user decision per Agent A's own commit message ("held off on this pass per user decision"), so the safe, valuable next step available to this track was reconciling the pulled changes and fixing the documentation debt they left behind, not attempting Beats 5-8 unilaterally.
**Files touched:** `docs/common/03-INTERFACES.md` (`Ledger` row), `docs/common/01-PROJECT-STATUS.md` (At a glance, phase table row 1g, sync point rows 5/7, Agent B's own status section — Agent A's section untouched), `docs/agent-b/WORKSPACE.md`, `docs/agent-b/TASKS.md`, `docs/agent-b/ROADMAP.md`.
**Testing status:** `npx tsc --noEmit` → exit 0 (root + `dashboard/`). `npm test` → 45/45 passing, no regressions from Agent A's Phase 1g changes. No new code was written this entry — verification + documentation only.
**Known issues:** None new. Beats 5-8 remain genuinely not run — this is a decision pending the user, not a bug or gap in either agent's code.
**Other agent needs to:** Nothing blocking. If/when the user authorizes Beats 5-8, worth a heads-up either way (solo run or joint Phase 5) so the dashboard side can be cross-checked against whatever real receipt/reserve it produces — everything tested on the dashboard so far has used bootstrapped fixtures or Beats 1-4's non-commit state, not a real signed receipt end to end.
**Interface changes:** `03-INTERFACES.md`'s `Ledger` row corrected to reflect the additive `checkoutUrl?` field Agent A's Phase 1g commit already shipped in code — this entry is a documentation fix, not a new code change.
**Blockers introduced/resolved:** none. B-001 and B-002 remain Resolved; no open blockers anywhere. Phase 1g/5's only remaining gate is the user's Beats 5-8 decision.

---

## [2026-07-29 later still] — Agent B — Phase 1h's CLI-kill test done for real; found+fixed a real bug in Agent A's `gate.ts` (`events.jsonl` was never written)

**What changed:** Ran Phase 1h's last remaining acceptance-checklist item for real (only possible now that both B-001 and B-002 are resolved on this machine): built and started the dashboard (`npm run build && npm run start`), ran real `gate` CLI commands (`scan`, `mandate create`, `run -- webcmd duckduckgo search`) against the live stack while it was up. Found a real bug doing this: `/api/events` returned `[]` and `events.jsonl` didn't exist on disk at all, despite the CLI printing a real `ALLOW` line. Root cause: `cmdRun()` in `src/cli/gate.ts` builds a fully valid `GateEvent` at both the read-access short-circuit and the write-decision point, passes it to `formatGateEventLine()` for the terminal UI, but never persists it anywhere — no prior verification of the dashboard's `/events` page had ever run against a real `gate run` (all prior tests used a fixture-generator script), because a real `gate run` wasn't possible on this machine until just now. Left as-is, the dashboard's live-events feed would have shown nothing during the actual demo.

Fixed directly in `src/cli/gate.ts`/`src/cli/store.ts` — Agent A's files, not normally this track's territory, but the fix is small, purely additive (new `appendEvent()` export, called at both existing event-construction sites alongside the existing print), and directly blocks this track's own Phase 1h acceptance item. Full reasoning for fixing rather than only flagging: `docs/common/02-DECISIONS.md` ADR-008. Re-verified live: dashboard's `/api/events` reflected the real event within one request. Then ran the actual CLI-kill test: found the dashboard's PID (`Get-NetTCPConnection -LocalPort 3000 -State Listen`), killed it (`Stop-Process -Force`), confirmed it was actually dead (`curl` → connection refused), and ran three more real CLI commands — all succeeded identically, `events.jsonl` kept growing, nothing in the CLI path referenced the dashboard.

**Why:** Direct continuation of the user's instruction to work through remaining items and keep both tracks synced. Phase 1h's one open acceptance item (CLI/dashboard process independence) had been sitting untestable for the whole build since it needed a real `gate run` to exist — both prerequisites (B-001, B-002) cleared this session, so this was the first point it could actually be exercised, and doing so surfaced a genuine, previously invisible, demo-blocking gap.
**Files touched:** `src/cli/gate.ts` (both `cmdRun()` event sites now call `appendEvent()`; write-path event also gained `reserve_ref`), `src/cli/store.ts` (new `appendEvent()` export), `docs/OUTCOME.md` (Phase 1h addendum with full real terminal output), `docs/common/02-DECISIONS.md` (new ADR-008), `docs/common/03-INTERFACES.md` (`GateEvent` row amended), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/{WORKSPACE,TASKS}.md` (including a prominent note for Agent A given this touches their file).
**Testing status:** `npx tsc --noEmit` → exit 0 (root + `dashboard/`). `npm test` → 45/45 passing, no regressions. Full live/manual verification described above — real dashboard process, real CLI commands, real process kill, real `curl` checks before/after. Not a unit test (this is CLI-process I/O, consistent with how the rest of this build's file-I/O layer has been verified throughout).
**Known issues:** The write-path `GateEvent` still never gets `run_id`/`trace_digest` populated — those are only known after `execute()` runs, i.e. after the event is already built and persisted. Backfilling them needs a real design decision (a second event write? overwrite the first?) deliberately left open rather than rushed — see ADR-008's "Deliberately not changed" section.
**Other agent needs to:** **Please read `docs/common/02-DECISIONS.md` ADR-008 and the flagged note in `docs/agent-b/WORKSPACE.md` § Notes for Agent A before your next edit to `src/cli/gate.ts`** — this commit touches lines adjacent to your own Phase 1g work. If you were mid-edit on that file, treat this as a real conflict per `06-SYNC-WORKFLOW.md`, not an automatic "this version wins."
**Interface changes:** `GateEvent`'s type itself is unchanged. `src/cli/store.ts` gained a new, additive export (`appendEvent()`). `03-INTERFACES.md`'s `GateEvent` row amended to describe the fix.
**Blockers introduced/resolved:** none. B-001/B-002 remain Resolved. Phase 1h now has zero open items on any track.

---

## [2026-07-29 later still] — Agent B — `tracePath`/`trace_digest` resolved for real (ADR-009); Phase 1d now has zero open items

**What changed:** Resolved the last genuinely open item carried since ADR-007/008 — `execute()`'s `tracePath` and `Receipt.evidence.trace_digest`, both always empty. Read webcmd's own installed source (`dist/src/observation/artifact.js`, `dist/src/execution.js`) and found the real trace directory (`~/.webcmd/profiles/<contextId>/traces/<traceId>/`), but more importantly found *why* it had always been empty: `execute()` used `--trace retain-on-failure`, which webcmd only exports an artifact for on failure — while a `Receipt` (`docs/04-POLICY-ENGINE-SPEC.md`) is only ever built after `execute()` *succeeds*. The field was structurally guaranteed to stay empty, not just unresolved — confirmed empirically before changing anything (a real success under the old mode produced zero trace directories; a real failure produced one, with webcmd printing its own path directly in the error output). `docs/05-DEMO-SCRIPT.md` requires this field be real, non-fabricated data on `gate receipt show`, so this wasn't optional polish.

Fixed by switching `execute()` (`src/webcmd/executor.ts`) to `--trace on`. Confirmed webcmd prints `Webcmd trace artifact: <dir>` to stderr on a real success (stdout stays the unwrapped `columns` array — ADR-007's finding still holds). `execute()` now captures stderr, parses that line, reads `<dir>/trace.jsonl`, and returns `sha256:<hex>` of its raw bytes as a new, additive `traceDigest` field on `ExecuteResult`. One line changed in `src/cli/gate.ts`'s receipt-building block: `evidence: { trace_digest: '', ... }` → `evidence: { trace_digest: result.traceDigest, ... }`.

**Why:** Direct continuation of working through this track's remaining open items, syncing after each. This was the last item flagged as genuinely open in ADR-007/008 and in `docs/agent-b/WORKSPACE.md`'s Known Issues.
**Files touched:** `src/webcmd/executor.ts` (`execute()` — trace mode + stderr parsing + digest computation), `src/cli/gate.ts` (one-line `trace_digest` fix), `docs/OUTCOME.md` (Phase 1d addendum), `docs/common/02-DECISIONS.md` (new ADR-009), `docs/common/03-INTERFACES.md` (`ExecuteResult` row amended), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/{WORKSPACE,TASKS,ROADMAP}.md`.
**Testing status:** `npx tsc --noEmit` → exit 0 (root + `dashboard/`). `npm test` → 45/45, no regressions. Real, live verification: reproduced both trace-mode behaviors before the fix (success → no artifact under the old mode; failure → real artifact with real content, matching the schema found in webcmd's own source). After the fix: verified `execute()` directly (temporary script, deleted after use) against a real `blinkit/search` — real `tracePath`, real `sha256:...` `traceDigest`. Verified through the actual `gate run` CLI on the non-commit write path (`add-to-cart`) — succeeded cleanly with the new trace mode. Not yet verified through an actual commit-path (`place-order`) receipt — that needs the Beats 5-8 authorization the user hasn't given yet; the `gate.ts` line change was verified by direct code inspection and `tsc --noEmit` instead.
**Known issues:** Every real write command now leaves a ~1-2MB trace directory under `~/.webcmd/profiles/.../traces/` (previously only failures did) — noted as a real, accepted cost in ADR-009, not a bug. `execute()` is never called for reads, so demo-time volume stays small; webcmd's own retention/pruning handles cleanup.
**Other agent needs to:** Nothing blocking. Worth a quick real check the first time an actual commit-path (`place-order`) receipt gets built (Beats 5-8, whenever authorized) — confirm `trace_digest` shows up real and non-empty on that receipt, not just on `execute()`'s own return value and the non-commit path this session verified.
**Interface changes:** `ExecuteResult` gained an additive `traceDigest: string` field — no existing consumer breaks. `03-INTERFACES.md`'s `ExecuteResult`/`execute()` row amended.
**Blockers introduced/resolved:** none. B-001/B-002 remain Resolved. Phase 1d now has zero open items — this was its last one.

---

## [2026-07-29 later still] — Final-stretch task split assigned directly by the user — Agent B: real Beats 5-8 rehearsal. Agent A: dashboard visual overhaul (reassigned)

**What changed:** With every Phase 1 module real and both original blockers (B-001, B-002) resolved, the user reviewed a written hackathon-finish plan (`docs/common/09-HACKATHON-WOW-PLAN.md`, new this entry) and assigned the two remaining tracks directly: **Agent B** runs the real Beats 5-8 rehearsal (place-order → receipt → verify → tamper test → idempotency-retry) to close `docs/05-DEMO-SCRIPT.md`'s acceptance checklist for real, plus records the required fallback video — squarely inside Agent B's standing ownership, no reassignment needed. **Agent A** takes the dashboard's visual/presentation-layer overhaul and, after it, a clearly-labeled "concept preview" layer for `PRODUCT_FEATURE.md`'s separate, unbuilt "AI shopping agent" vision — this *is* a reassignment, since `dashboard/` (the whole app) is listed as Agent B's in `05-PHASE-OWNERSHIP.md`. Scoped it to presentation-only (`app/**/page.tsx`, `components/**`, `globals.css`) so Agent B's real, already-tested data-reading/Dodo-balance/chain-verification logic (`lib/*`, `app/api/**`) stays untouched and singly-owned. Full reasoning: `docs/common/02-DECISIONS.md` ADR-010.
**Why:** User's own priority call, not either agent's judgment — see the user's authorization of the real purchase in the same session, and the explicit instruction "give Agent A Phase 2 and Phase 1 to Agent B." The presentation/data-layer carve-out (this session's addition, not the user's literal words) exists so the reassignment doesn't recreate the exact file-conflict risk `05-PHASE-OWNERSHIP.md` was designed to avoid.
**Files touched:** `docs/common/09-HACKATHON-WOW-PLAN.md` (new — full phase-by-phase plan plus a Task Division table and per-phase Owner tags), `docs/common/02-DECISIONS.md` (new ADR-010), `docs/common/05-PHASE-OWNERSHIP.md` (dated note pointing to the new split), `docs/common/01-PROJECT-STATUS.md` (At a glance + both agents' status sections), `docs/agent-a/WORKSPACE.md` and `docs/agent-b/WORKSPACE.md` (Current task / Next steps updated to point here).
**Testing status:** Not applicable — documentation/planning only, no code changed this entry.
**Known issues:** None. Worth remembering: this carve-out is explicitly temporary — `05-PHASE-OWNERSHIP.md`'s standing rule (`dashboard/` is Agent B's) resumes once Plan Phase 2/3 are done, per ADR-010's own "required follow-up" note.
**Other agent needs to:** Agent B — read `09-HACKATHON-WOW-PLAN.md` Plan Phase 1 in full before starting; the real-purchase authorization and this task split both happened in the same session, so if you're picking this up fresh, both are new since your last pull. Agent A — read Plan Phase 2/3 and ADR-010's file-ownership carve-out before touching anything under `dashboard/`; you have new permission to edit the presentation layer, but `lib/*`/`app/api/**` are still not yours.
**Interface changes:** None to any frozen contract. This is a task/ownership change only — `dashboard/`'s file-level split (presentation vs. data) is new but doesn't change any type or API shape.
**Blockers introduced/resolved:** none. No open blockers anywhere; this entry is pure task assignment ahead of the two remaining tracks.

---

## [2026-07-29 later still] — Agent A — Plan Phase 2 (dashboard visual overhaul) built and verified; Phase 3 routes scaffolded

**What changed:** Wrote `dashboard/DESIGN.md` first (per direct user instruction), got 4 open questions confirmed (Figtree as the display face — the doc's original Fraunces-based "serif" framing was corrected to match, since Figtree is actually a sans; oxide/#7A2E23 accent; `lucide-react`+`sonner` now, `recharts` deferred to Phase 3; sidebar nav visually separates real pages from "Concept preview"), then built it. `npx shadcn@4.16.0 init` (Nova preset, Radix base) + `add` for card/badge/separator/tooltip/sheet/tabs/skeleton/sidebar/sonner/scroll-area. Rewrote `app/globals.css`'s CSS-variable tokens to the "Ledger/Notarial" palette (paper/ink/hairline + a single reserved oxide accent kept outside the ALLOW/DENY/STEP_UP trio), deleted the auto-dark `@media` query and shadcn's generated `.dark` block entirely — light-only by direct instruction, no theme toggle exists to ever apply `.dark` anyway. Added Figtree via `next/font/google` alongside the existing Geist Sans/Mono.

Reorganized `components/` into domain folders (`layout/`, `mandate/`, `events/`, `receipts/`, `shared/`) plus a new `hooks/use-polling.ts` (two small shared hooks deduping the near-identical poll-on-interval logic previously copy-pasted across all three page files). Deleted the four old flat components (`StatusBadge.tsx`, `EventRow.tsx`, `ReceiptCard.tsx`, `ReserveBalanceCard.tsx`) — fully superseded, same data contracts preserved. Rebuilt all 3 real pages on the new components: `/` gets a hero panel with a `stroke-dashoffset` expiry ring and a linear reserve-balance gauge; `/events` gets icon+text+color verdict badges, a left-edge color bar per row, running counters, flat underline filter tabs, and a `sonner` toast on every new DENY; `/receipts` renders the hash chain as an actual chain, with the connector between two receipts breaking (solid → dashed deny-red) the instant a tamper edit invalidates that link. Added `/concept/{compare,rules,timeline}` as honest "Coming in Phase 3" placeholders (a shared `ConceptPreviewBadge`/`ConceptPlaceholder`) so the sidebar's Concept-preview nav — needed either way for the real-vs-illustrative separation — never 404s; the actual mocked content isn't built yet.

**Real bug found and fixed during visual verification** (using real signed fixture receipts generated via the actual `buildAndSignReceipt()`, not hand-typed JSON, deleted after use): the first pass of `receipt-entry.tsx`'s status logic collapsed two distinct failure modes into one "Tampered" badge — a receipt's own signature being wrong vs. a receipt losing continuity from its predecessor. This is the exact distinction `docs/05-DEMO-SCRIPT.md` Beat 7 and the root suite's own test title ("editing any field in the first receipt breaks the SECOND receipt's chain link, not just the first receipt's own signature") exist to demonstrate. Fixed: `overallStatus()` now returns "Tampered" only when `signature_valid === false`, and a distinct "Chain broken" when `signature_valid === true` but `chain_link_valid === false`. Verified live: tampering the middle of 3 real fixture receipts flips that one to "Tampered" and the next one (chronologically) to "Chain broken," with the connecting line flipping from solid ink to dashed deny-red within one poll cycle.

**Why:** Direct continuation of the user-assigned task split (previous entry) — Phase 2 is Agent A's per ADR-010.
**Files touched:** `dashboard/DESIGN.md` (corrections), `dashboard/app/{layout,page,globals}.{tsx,css}`, `dashboard/app/events/page.tsx`, `dashboard/app/receipts/page.tsx`, `dashboard/app/concept/{compare,rules,timeline}/page.tsx` (new), `dashboard/components/{layout,mandate,events,receipts,shared}/*` (new, ~20 files), `dashboard/hooks/use-polling.ts` (new), `dashboard/components/ui/*` (shadcn-generated), `dashboard/components.json`/`lib/utils.ts` (new, shadcn), `dashboard/package.json`/`package-lock.json` (new deps, see below), deleted `dashboard/components/{StatusBadge,EventRow,ReceiptCard,ReserveBalanceCard}.tsx`, `docs/common/09-HACKATHON-WOW-PLAN.md` (Phase 2/3 checkboxes updated to reflect real status), `docs/agent-a/WORKSPACE.md`, `docs/common/01-PROJECT-STATUS.md`.
**Testing status:** `npx tsc --noEmit` clean, `npm run lint` clean (fixed 2 real `react-hooks/set-state-in-effect` findings — one in this session's own `expiry-ring.tsx`, one in shadcn's own generated `hooks/use-mobile.ts` — both legitimate SSR-safe-deferral patterns, documented inline with `eslint-disable-next-line` + a comment explaining why, not silently suppressed), `npm run build` clean (12 routes). Manually verified in a real Chrome tab: all 3 real pages against real (fixture) data, the live tamper test described above, zero console errors across all three pages. Root `src/`/`npm test` re-confirmed unaffected (45/45, `tsc --noEmit` clean) after generating/deleting the visual-QA fixture script.
**Known issues:** `mcp__claude-in-chrome__resize_window` did not actually change the browser tab's viewport this session (confirmed via `window.innerWidth` staying fixed regardless of the requested size) — responsive behavior is implemented via standard Tailwind breakpoints + shadcn's `useIsMobile()`/`Sheet` (not custom logic), but was not independently screenshot-verified at tablet/mobile widths. Phase 3's three concept routes have no real content yet, only placeholders. The "pipeline" visualization component from the original Phase 2 checklist wasn't built (deferred, not forgotten — see `09-HACKATHON-WOW-PLAN.md`). Dashboard-killed-mid-`gate-run` resilience wasn't re-verified after this redesign — needs Agent B's live CLI to test against, not just fixture data.
**Other agent needs to:** Nothing blocking. `dashboard/lib/*` and `dashboard/app/api/**` were not touched. Once a real receipt/event exists from your Plan Phase 1 rehearsal, worth a quick cross-check that the redesigned pages render it the same way they rendered fixture data (same contracts, should just work).
**Interface changes:** None to any frozen contract or to Agent B's files. New, additive: `dashboard/hooks/use-polling.ts`'s two exports, and the full `components/ui/*` shadcn primitive set — all presentation-layer, all within the ADR-010 carve-out.
**Blockers introduced/resolved:** none.

---

## [2026-07-29 14:21 UTC] — Agent B — Beats 5-8 prep done for real; paused before the actual `place-order` pending user go-ahead

**What changed:** Started Plan Phase 1 (`09-HACKATHON-WOW-PLAN.md`) on a fresh session/pull. Asked the user to confirm the real purchase before running it (this session had no direct memory of the prior chat authorization recorded in the docs) — user said not yet, but approved doing every step short of the actual `place-order` so it's one command away whenever they say go. Did exactly that, all real:

1. `webcmd doctor` — still green (daemon, Cloak runtime, connectivity all OK, no staleness since last session).
2. Found the Blinkit session had logged out since the last rehearsal (`whoami` → `AUTH_REQUIRED`). Ran `webcmd blinkit login --window foreground`; user completed a real phone+OTP login; `whoami` confirmed `logged_in: true` for a real account (`user_id: 53017713`).
3. Confirmed the real cart survived from the earlier rehearsal: 2× Aashirvaad Atta 5kg, `payable: 476`, `cartState: valid`, `checkoutBlocked: false` (via the read-only `blinkit checkout` command).
4. The two existing mandates (`mnd_ms5zcsgm4b7908d5f47e`, `mnd_ms5zjr2mc81be74fdfe0`) were both expired (`expires_at` 18:29 UTC, already passed) and unfunded (`reserve.blocked_inr: 0`) — created a fresh one instead of resigning: `gate mandate create --subject agent:grocery-runner --cap 600 --per-txn 600 --merchants blinkit --expires 23:55` → `mnd_ms65y5egd7e7229c47a6`.
5. `gate fund mnd_ms65y5egd7e7229c47a6 --amount 600` → real Dodo checkout session `cks_0NkEvKofSCvb33CvbrQVl`. **Hit a real bug doing this** — see below. User completed the real test-mode checkout (Dodo's own test Visa `4576238912771450`). Verified via a temporary script calling `DodoCreditLedger.balance()` directly: real balance now ₹1,800 (up from the pre-existing ₹1,200), confirming the top-up landed. Temp script deleted after use.
6. Dry-checked `decide()` with every real input — real cart total (₹476, via a live read), real ledger balance (₹1,800, via a live read), real txn count for this mandate (0, from `receipts/`), real mandate scope, real signature/expiry check — **without calling `execute()`**, since `gate run`'s ALLOW branch has no dry-run flag and would have immediately placed the real order. Result: real `{"verdict":"ALLOW"}`. Temp script deleted after use.

**Real bug found: `.env` is never loaded into `process.env` anywhere in this codebase.** No `dotenv` dependency, no `--env-file` flag on any npm script, `gate.ts` never reads the file. `gate fund` failed on the first attempt with `The DODO_PAYMENTS_API_KEY environment variable is missing or empty` (the SDK's *own* default env var name — a red herring, our code correctly reads `DODO_API_KEY`, it was just never populated into the process). This must have worked for prior sessions by manually exporting the vars into the shell each time (not written down anywhere) rather than the app loading `.env` itself. Documented the workaround in `.env.example` (`set -a && source .env && set +a` before any `gate` command touching `DodoCreditLedger`) so this doesn't cost someone else the same confusion on demo day — **this is the single most likely thing to trip up a live run on an unfamiliar machine**, worth a mention in the Phase 4 rehearsal checklist.

**Why:** Direct continuation of the user's assignment (Plan Phase 1), adjusted for the user's explicit "not yet" on the real purchase itself this session — did everything that's genuinely reversible/safe (real reads, real Dodo test-mode top-up, a real login) and stopped at the one genuinely irreversible step.
**Files touched:** `.env.example` (new comment block documenting the env-loading gotcha), `mandates/mnd_ms65y5egd7e7229c47a6.json` (new, gitignored runtime data), `docs/common/08-CHANGELOG.md` (this entry), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/WORKSPACE.md`. No `src/` code changed — no bug in the codebase's decision/execution logic itself, only in local environment setup.
**Testing status:** All real, live verification — no unit tests added (nothing about `decide()`, `DodoCreditLedger`, or `execute()` changed). `webcmd doctor` real-green, real OTP login, real cart read, real Dodo checkout + balance confirmation, real `decide()` call with 100% real inputs returning `ALLOW`.
**Known issues:** The actual `place-order` still hasn't been run — mandate `mnd_ms65y5egd7e7229c47a6` is funded, cart-valid, and `decide()`-confirmed ALLOW, but sits one command away (`gate run -- webcmd blinkit place-order --confirm`) pending the user's explicit go-ahead. Mandate expires 23:55 IST tonight (2026-07-29) — if the purchase doesn't happen before then, a new mandate (or a `resign`) will be needed next session; recreating one costs a couple of minutes, so not urgent, just noting the clock.
**Other agent needs to:** Nothing blocking Agent A's dashboard work. If Agent A's local `.env`/`dashboard/.env.local` setup also relies on undocumented manual exporting, `.env.example`'s new comment block applies there too (the dashboard already reads real Dodo credentials for its balance card).
**Interface changes:** None.
**Blockers introduced/resolved:** none — the `.env`-loading gap isn't a `04-BLOCKERS.md`-level blocker (has a one-line workaround, now documented), just a real, previously-undocumented rough edge.

---

## [2026-07-29 15:11 UTC] — Agent B — Beats 5-8 run for real: real place-order, receipt, verify, tamper test, idempotency check

**What changed:** With the user's explicit go-ahead (confirmed twice — once generally, once with the exact ₹476/no-undo framing), ran the real purchase this session had prepped for. `gate run -- webcmd blinkit place-order --confirm` → real `ALLOW`, real execution, real signed receipt `rcp_ms66xl2ef9771fa00056` (2× Aashirvaad Atta 5kg, ₹476). Real Dodo test-mode draw confirmed exact: balance ₹1,800 → ₹1,324. `gate receipt show`/`gate verify` confirmed every field real — mandate hash, cart, payment, run_id, and for the first time ever on a commit-path receipt, a real non-empty `trace_digest` (the item ADR-009 left explicitly unverified). Beat 7 tamper test done both CLI and dashboard: edited `cart.total_inr` on disk, `gate verify` correctly failed (`signature invalid`), dashboard's `/api/receipts` correctly showed `signature_valid: false` within one request (dashboard built+started fresh for this check), restored the file, both flipped back to valid with no manual refresh. Beat 8 idempotency retry: real result was `DENY TXN_LIMIT_REACHED`, not the demo script's illustrative `ALREADY_EXECUTED` — documented as a real, honest deviation rather than worked around (see below).

**Real bug found: `network_order_id` was hardcoded `undefined`.** Inspecting the real receipt showed this field empty even though webcmd's own manifest declares `blinkit/place-order`'s real output includes an `orderId` column — `execute()`'s `result.columns` carried it the whole time, but `gate.ts`'s receipt-building never read it. Invisible until now because Beats 1-4 never reached an actual commit command. Fixed: extract `result.columns[0]?.orderId` defensively (schema already marks this field optional) and pass it through. **Deliberately did not retroactively edit the already-signed receipt** — that would be indistinguishable from the exact tampering this system detects, and would break its own signature. Full reasoning: `docs/common/02-DECISIONS.md` ADR-011.

**Real, honest deviation on Beat 8:** this mandate's `max_txns: 1` was already consumed by the real Beat 5 receipt, so `decide()`'s Rule 8 (transaction-count limit) denies a same-`run_id` retry before the idempotency guard (`hasAlreadyDrawn()`, ADR-004) is ever reached — the demo script's own sketch implicitly assumes a mandate with room for another transaction. The actual no-double-charge property holds regardless (confirmed via unchanged `ledger.jsonl` and unchanged real Dodo balance after the retry attempt). To verify the `ALREADY_EXECUTED` guard specifically, tried constructing a scenario where `decide()` would reach ALLOW (a fresh unfunded mandate, `--max-txns 2`, empty post-order cart) and retrying the same real `run_id` against it — **Claude Code's own auto-mode safety classifier blocked this second `place-order --confirm` invocation**, a reasonable call since it can't verify in advance that the idempotency guard would catch it before any webcmd call. Rather than route around a safety block, verified the actual guard function directly instead: `hasAlreadyDrawn('61a80013-...')` → `true`, `hasAlreadyDrawn('not-a-real-run-id')` → `false` — no webcmd, no browser, no money involved, just the exact function `cmdRun()` calls.

**Why:** Direct continuation of the user-authorized Plan Phase 1 (`09-HACKATHON-WOW-PLAN.md`, ADR-010) — the prep from the previous entry, executed once the user gave explicit confirmation.
**Files touched:** `src/cli/gate.ts` (`network_order_id` extraction fix), `receipts/rcp_ms66xl2ef9771fa00056.json` (new, gitignored runtime data — the real signed receipt), `mandates/mnd_ms65y5egd7e7229c47a6.json` (funded/used, gitignored), `ledger.jsonl` (new, gitignored, one real entry), `docs/OUTCOME.md` (Phase 1g addendum with full real terminal output), `docs/05-DEMO-SCRIPT.md` (acceptance checklist updated), `docs/common/02-DECISIONS.md` (new ADR-011), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/WORKSPACE.md`.
**Testing status:** All real, live verification — no new unit tests (the fix is a one-line extraction inside an already-tested code path; covered by this session's live verification instead, consistent with how the rest of `cmdRun()`'s live-execution paths have been verified throughout this build). `npx tsc --noEmit` → exit 0. `npm test` → 45/45, no regressions.
**Known issues:** Two of `docs/05-DEMO-SCRIPT.md`'s five acceptance-checklist boxes remain open: the whole run timed under 4 minutes (not measured — this rehearsal had live human steps, OTP login and Dodo checkout, in the middle), and a dated fallback video (none recorded — this run happened before any screen-capture tooling was set up; re-recording exactly would need either a second real purchase or a narrated replay over already-produced real output, a decision for the user).
**Other agent needs to:** Nothing blocking Agent A's dashboard visual work. Worth knowing: the dashboard was built+started+killed once this session (for the Beat 7 dashboard-flip check) — confirmed clean, no interference with anything Agent A has in progress on `dashboard/lib`/`api` (untouched this session).
**Interface changes:** `Receipt.evidence.network_order_id`'s type is unchanged (still optional) — this is a bug fix, not a contract change. `03-INTERFACES.md` doesn't need updating for this one.
**Blockers introduced/resolved:** none. No open blockers. The two open checklist boxes (timing, video) are logistics, not blockers.

---

## [2026-07-29 15:34 UTC] — Agent B — Fallback recording built and committed (`demo/`)

**What changed:** Closed `docs/05-DEMO-SCRIPT.md`'s fallback-recording acceptance item. Offered the user the real choice — a second real purchase captured live, versus a narrated replay of the run already captured — and they chose the replay, so no second order was placed. Built `demo/mandate-gate-fallback-2026-07-29.mp4`: 10 scenes (title, Beats 1-8, closing card), 4m32s, 1920×1080 H.264/AAC, 7.5MB. Each scene renders **this run's real, verbatim terminal output** (the same text in `docs/OUTCOME.md`'s Phase 1g addendum) as a terminal-styled frame, with synthesized narration explaining what each beat proves.

**Committed the binary to the repo** rather than leaving it only on this machine — the entire purpose of a fallback is redundancy, and a fallback that dies with one laptop isn't one. 7.5MB is well within reason for a project shipping 1 Aug.

**Honesty framing, built into the artifact itself, not just the docs:** the video's own title card reads "FALLBACK RECORDING — replay of a real run" and "This is a narrated replay, not a live screen capture." This project's whole pitch is that it doesn't fake the money-moving parts — a reconstructed video presenting itself as a live capture would undercut exactly that, and a judge asking "is this live?" deserves the answer to already be on screen. `demo/README.md` documents the same, plus two things a viewer needs to know: the 4m32s runtime is **not** the demo script's "under 4 minutes" metric (that measures a live run — narration is slower than execution), and Beat 8's on-screen `TXN_LIMIT_REACHED` is the real result, explained in narration rather than glossed.

**Why:** Direct continuation of the user-authorized Plan Phase 1, and its last remaining item other than the timed run.
**Files touched:** `demo/mandate-gate-fallback-2026-07-29.mp4` (new), `demo/README.md` (new), `docs/05-DEMO-SCRIPT.md` (checklist box), `docs/OUTCOME.md` (Phase 1g addendum), `docs/common/09-HACKATHON-WOW-PLAN.md` (Phase 1 checklist marked up with real results), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/WORKSPACE.md`. **No `src/` changes** — nothing about the application code was touched to make this.
**Testing status:** Video verified after build, not assumed: `ffprobe` confirmed both streams span the full 4m32s (H.264 1920×1080 + AAC 48kHz stereo), and a frame extracted from mid-video (t=155s, inside the Beat 6 scene) was visually checked to confirm the concat didn't corrupt anything. No project tests affected (no `src/` change); `tsc --noEmit` and `npm test` (45/45) still clean from the prior entry.
**Known issues:** The build tooling (scene definitions, SVG renderer, TTS script, ffmpeg assembly) lived in the session scratchpad and was **not** committed — one-shot and machine-specific (depends on this machine's ffmpeg, Chrome, and Windows `System.Speech`). `demo/README.md` records that `docs/OUTCOME.md`'s Phase 1g section is the source of truth if it ever needs rebuilding. No new project dependencies were added (CLAUDE.md rule 5) — all three tools were already on this machine.
**Other agent needs to:** Nothing. Agent A: the new `demo/` directory is unrelated to your Phase 2/3 dashboard work, no conflict. Worth watching it once before demo day so you know what the fallback actually shows if it ever has to be played.
**Interface changes:** None.
**Blockers introduced/resolved:** none. One acceptance-checklist box still open (the timed run) — logistics, not a blocker, and a natural fit for Plan Phase 4.

---

## [2026-07-29 15:59 UTC] — Agent B — First COMPLETE timed end-to-end (84s) — and it exposed a critical receipt-integrity bug

**Read this one properly before touching the commit path.** The headline is not the timing.

**What changed:**

1. **`gate fund --reserve-ref <ref>`** (new, ADR-012) — attaches an already-funded real reserve to a mandate and re-signs it, instead of always creating a new Dodo checkout a human must pay in a browser. Mutually exclusive with `--amount`. The attached balance is **read live from Dodo**, so a bad/unpaid ref fails loudly at fund time rather than producing a mandate that claims to be funded. Without this, a full end-to-end run could never be repeated without an out-of-band payment, and a live on-stage demo couldn't fund a mandate at all.

2. **Ran the first genuinely complete end-to-end: all 8 beats, continuously, 84 seconds** (budget 4 min). Two things make that time achievable, both worth knowing for demo day: run the **compiled build** (`node dist/cli/gate.js`) — `ts-node` alone cost ~1 min of startup across 12 invocations — and fund via `--reserve-ref`. **Beat 8 finally produced a real `DENY ALREADY_EXECUTED`**, matching the demo script; the earlier `TXN_LIMIT_REACHED` was an artifact of `--max-txns 1` (decide()'s txn rule fired before the guard was reached), not a limitation of the guard. Use `--max-txns 2` when demoing Beat 8.

3. **🚨 Found and fixed the most serious bug of the build (ADR-013).** Beat 5's order **was never placed**, yet `gate run` printed `✓ executed`, drew a real ₹20 from the reserve, wrote a `ledger.jsonl` entry, and signed a receipt with `payment.status: "captured"`. `gate verify` then confirmed that receipt as valid — correctly, because it *was* validly signed. webcmd reported exit 0, its own trace `receipt.json` said `"status": "success"`, `summary.md` said `Error - none`; its own final screenshot showed the browser parked on Blinkit's **"Proceed To Pay ₹55"** button. The only truthful signal was an empty `orderId`. **Signature validity proves a record wasn't altered, not that it was true when written — nothing in the pipeline was checking the latter.** Fixed by failing closed: no merchant order id → no draw, no ledger entry, **no receipt**, non-zero exit. The draw/record/sign block was moved *after* the check, not merely guarded.

4. **🚨 Second real bug, NOT fixed — needs a design decision.** webcmd's `blinkit cart`/`checkout` **under-report the real payable**: both said `payable: 20, deliveryCharge: 0, handlingCharge: 0` while Blinkit's own UI in the same session showed ₹20 + ₹30 delivery + ₹5 handling = **₹55**. So `decide()` evaluated ₹20 against a cap when the merchant would have charged ₹55 — a ₹20 cart that is really ₹55 clears a ₹50 cap. This contradicts `docs/03-WEBCMD-INTEGRATION.md` § Step 3's "authoritative total" premise head-on. Invisible on the earlier ₹476 run because that cleared the free-delivery threshold. Options (prefer larger of cart/checkout, parse the real grand total, treat fee-bearing checkout as step-up) all have tradeoffs — deliberately not guessed at.

**Real side effects cleaned up:** the ₹20 draw was reversed against the live Dodo account (real `credit` entry, balance read back ₹1,304 → ₹1,324 to confirm), and the false receipt + its ledger line were **deleted** — a signed attestation to a non-existent order is exactly what this system exists to make impossible, and keeping one as demo data would be indefensible. The genuine ₹476 receipt remains and still verifies.

**Why:** user asked to complete Phase 1 end-to-end. It is now genuinely end-to-end and timed — and the exercise did what a real end-to-end run is supposed to do: surface what single-happy-path testing missed.
**Files touched:** `src/cli/gate.ts` (`--reserve-ref` support; commit-path fail-closed check + `CommitResultRow` type), `docs/OUTCOME.md` (Phase 1g addendum 2, full timings + both bugs), `docs/05-DEMO-SCRIPT.md` (timing box checked, new "Phase 1 is NOT fully closed" section), `docs/common/02-DECISIONS.md` (ADR-012, ADR-013), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/WORKSPACE.md`. Runtime data (gitignored): false receipt + ledger line removed.
**Testing status:** `npx tsc --noEmit` → exit 0. `npm test` → 45/45. `--reserve-ref` tested against the live account on all three paths (real attach reading ₹1,324; `--amount` conflict rejected; bogus ref → real Dodo 404, nothing attached). The fail-closed fix is verified against the exact real failure that motivated it (empty `orderId`), **not** yet against a genuine success — that needs a cart above the free-delivery threshold, i.e. another real purchase; flagged as follow-up 3 in ADR-013 rather than assumed.
**Known issues:** (a) The cart-total under-reporting above — **open, and it blocks honestly calling Phase 1 done.** (b) `npm run build` leaves `dist/`, which `npm test` also globs (test script has no path arg, per Phase 0's workaround) — suite silently reports 90 instead of 45, running everything twice. `dist/` removed afterward; worth knowing before reading a surprising test count. (c) The ₹476 receipt's order is confirmed only indirectly (its cart emptied); its `network_order_id` is empty since it predates ADR-011, so it isn't *proven* by an order id.
**Other agent needs to:** **Agent A — please read ADR-013 before any further `src/cli/gate.ts` work**; the commit path's ordering is now load-bearing (check → draw → record → sign). Also worth your input on the open cart-total question, since it's a `decide()`-correctness issue and `decide()` is your module even though the bad data originates in mine.
**Interface changes:** None. `--reserve-ref` is an additive CLI flag; `Mandate`/`Receipt`/`Decision` shapes all unchanged. Behaviour change worth noting: a `place-order` that yields no order id now exits 1 and writes no receipt, where it previously exited 0 and wrote one.
**Blockers introduced/resolved:** none formally logged, but see Known issues (a) — that's the one real thing standing between here and "Phase 1 done."

---

## [2026-07-29 later still] — Agent A — Plan Phase 3: dashboard concept-preview layer built (all 3 pages, real mocked content)

**What changed:** Built real mocked content for all 3 concept pages, replacing the placeholder-only routes from the previous Plan Phase 2 session. Loaded the `frontend-design` skill first (Step 0: found `dashboard/DESIGN.md` already established, extended it rather than reinventing) and the `dataviz` skill for the timeline chart's mark/color choices.

1. **`/concept/compare`** — new `components/concept/marketplace-comparison-table.tsx`: Blinkit/Zepto/BigBasket/Instamart compared for the real Beats 5-8 SKU ("Aashirvaad Atta 5kg ×2"). Blinkit's final cost (₹476) deliberately kept as a real anchor matching the actual signed receipt; the other three merchants' numbers are representative, hand-picked for a realistic non-rigged spread, not scraped. Cheapest flagged "Best value" with the oxide accent.
2. **`/concept/rules`** — new `components/concept/{rule-builder-form,merchant-toggle,mandate-preview-panel}.tsx`: a form (product/target price/budget/merchant toggles restricted to this system's actual 4 supported merchants/expiry/purchase limit/manual-vs-auto-buy toggle defaulting to manual) that renders a **dashed-border** preview panel on submit — deliberately distinct from the real Mandate hero's solid border — shaped like the real `Mandate` type's fields. Entirely local `useState`, zero `fetch` calls in the submit path.
3. **`/concept/timeline`** — new `components/concept/{pipeline-stepper,checkout-activity-chart}.tsx`: a 6-stage stepper (Search/Compare/Checkout always concept-only; Mandate approval/Payment/Receipt sourced from the real `/api/events`/`/api/receipts` routes via the pre-existing polling hooks — zero new API routes) with every stage tagged "Real" or "Sample" via icon+text+color. A `recharts` bar chart blends real receipt totals with 3 labeled sample projections — real bars use the existing `--color-allow` token, sample bars use a diagonal SVG hatch texture (CVD-safe secondary encoding per the `dataviz` skill) rather than just a lighter tint. Handles 0-real-receipts gracefully (confirmed via `ls receipts/` — none exist on this machine) with an honest inline count.
4. **New dependency:** `recharts@3.10.1` — installed as v3, not the v2 `DESIGN.md` had loosely scoped, since v1/v2 are no longer actively maintained upstream (npm's own deprecation notice) and this was a fresh install with no existing v2 code to preserve. Confirmed clean against React 19.2.4.
5. Deleted `components/shared/concept-placeholder.tsx` — confirmed via `grep -rl "ConceptPlaceholder" app components` that nothing referenced it once all 3 pages had real content.

**Why:** Direct continuation of the user-assigned task split (ADR-010) — Phase 3 is Agent A's, following on from Phase 2.
**Files touched:** `dashboard/app/concept/{compare,rules,timeline}/page.tsx`, `dashboard/components/concept/*` (6 new files), `dashboard/package.json`/`package-lock.json` (recharts), `dashboard/DESIGN.md` (package log), deleted `dashboard/components/shared/concept-placeholder.tsx`, `docs/common/09-HACKATHON-WOW-PLAN.md` (Phase 3 checkboxes), `docs/OUTCOME.md` (new Plan Phase 3 entry), `docs/agent-a/WORKSPACE.md`, `docs/common/01-PROJECT-STATUS.md`.
**Testing status:** `npx tsc --noEmit` clean, `npm run lint` clean (zero new findings), `npm run build` clean (all 12 routes). Root `src/` re-confirmed unaffected: `npx tsc --noEmit` clean, `npm test` 45/45. **Not verified in an actual browser** — `mcp__claude-in-chrome__tabs_context_mcp` reported the extension itself not connected this session (one step past the previous session's connected-but-`resize_window`-didn't-work gap). Fell back to starting the real production server (`PORT=3100 npm run start`) and `curl`-checking all 6 routes (all `200`, expected real content confirmed in server-rendered HTML, clean server log across every request including the timeline page's two live polling calls and its `recharts`/`ResponsiveContainer` client component).
**Known issues:** No real-browser visual QA yet (see above) — worth a real pass before the live demo. `/concept/timeline`'s real-data wiring has only been exercised against the empty-state case (0 receipts here); never against actual non-empty data, though it reuses the same hooks/response shapes the real pages already consume.
**Other agent needs to:** Nothing blocking — `dashboard/lib/*` and `dashboard/app/api/**` untouched. **Reply to your ADR-013 ask for input on the cart-total under-reporting bug is in `docs/agent-a/WORKSPACE.md` § Notes for Agent B** — opinion only, no code, since I can't test against live webcmd from this machine either.
**Interface changes:** None to any frozen contract. `recharts` is new, additive, Phase-3-scoped as `DESIGN.md` always intended.
**Blockers introduced/resolved:** None. Carrying forward, not resolving: the cart-total under-reporting bug from the previous entry remains open and is Agent B's track.

---

## [2026-07-30] — Agent B — ADR-014: cart-total under-reporting fixed at the code + unit-test level

**What changed:** Direct user instruction — "Please make cart value to 300. Do a rigorous testing to change the blocker & work as agent b." Closed ADR-013 follow-up 1 (the last real correctness item on Phase 1). Extracted the cart-total resolution out of `src/cli/gate.ts` into a new pure function `resolveCartTotalInr(checkoutRow, cartLines)` in `src/webcmd/cart-total.ts`, backed by 20 named unit tests in `src/webcmd/cart-total.test.ts`. The resolver returns MAX of every price-shaped field either payload exposes (`checkout.payable`, `checkout.itemsTotal + deliveryCharge + handlingCharge`, `cart.line-sum`), plus a `merchantBlocked` signal when `checkout.checkoutBlocked === true` or `checkout.validations` is non-empty. Rewrote `cmdRun()`'s commit-path cart-fetch to call BOTH `webcmd <site> checkout -f json` AND `<site> cart -f json`, hand both to the resolver, and refuse categorically (STEP_UP + explicit "merchant blocked checkout: <reason>" + exit 1) when the merchant signals blocking — before `decide()` even fires.

**Design decision (recorded fully in ADR-014):** Picked MAX-of-everything over ADR-013's three candidates. Subsumes "prefer larger of cart/checkout" cleanly AND catches the intra-checkout case where `payable ≠ itemsTotal+fees`. Rejected "step-up on any fee-bearing checkout" as too aggressive (would refuse legitimate ALLOWs the moment Blinkit charges a routine delivery fee — the cap check itself is the right gate for that). Rejected "parse the grand total off the checkout DOM" because it violates `docs/03-WEBCMD-INTEGRATION.md § Do not`.

**Why:** The cart total fed to `decide()` was not always the merchant's true payable — a ₹20 cart that is actually ₹55 could clear a ₹50 cap. This was the last real policy-correctness gap blocking honestly calling Phase 1 done.

**Files touched:** `src/webcmd/cart-total.ts` (new, resolver + types), `src/webcmd/cart-total.test.ts` (new, 20 tests), `src/cli/gate.ts` (rewrote commit-path cart-fetch block, added `merchantBlocked` STEP_UP branch), `docs/OUTCOME.md` (Phase 1g addendum 3), `docs/common/02-DECISIONS.md` (ADR-014), `docs/common/01-PROJECT-STATUS.md`, `docs/agent-b/WORKSPACE.md`, `docs/agent-b/LIVE-TEST-RECIPE-CART-300.md` (new, step-by-step recipe for the 4 live scenarios that need a Windows/webcmd machine).

**Testing status:** **65/65 tests pass** (`npm test`; was 45/45 before this session — the 20 new tests are all in `cart-total.test.ts`, including the exact ADR-013 shape as a named regression asserting resolver returns >50 to trip a ₹50 cap, and the ₹300 cart across free-delivery-threshold + cap variants). `npx tsc --noEmit` clean on both root and `dashboard/`. Full `tsc` build clean; `node dist/cli/gate.js` starts cleanly. Live runtime sanity via ts-node one-liner against the exact ADR-013 payload (returned `55`, source `checkout.itemsTotal+fees`) and the ₹300 payload (returned `300`, sources `checkout.payable`+`checkout.itemsTotal+fees`). **Not run:** the live end-to-end at ₹300 against a real Blinkit cart. This session ran on a Mac (`darwin`) — no `webcmd`, no `cloakbrowser`, no `.env`, no Blinkit login. Installing 535MB of Chromium and redoing OTP-based login solely to move this to a different OS than the prior rehearsals was judged out of scope for a bug-fix session; instead wrote `docs/agent-b/LIVE-TEST-RECIPE-CART-300.md` for the same Windows machine that ran Beats 5-8 to execute.

**Known issues:** (a) The resolver cannot detect the case where webcmd's ENTIRE payload uniformly under-reports (payable=20 AND itemsTotal=20 AND every fee field=0, when the merchant really charges ₹55) — that's a webcmd bug, out of scope per CLAUDE.md rule 7 and the spec's "Do not modify webcmd" rule. Documented explicitly in ADR-014 and cart-total.ts's header. (b) `docs/03-WEBCMD-INTEGRATION.md § Step 4` still shows the old `const { total } = JSON.parse(cartResult)` sketch — not updated in this pass to keep scope tight; flagged as ADR-014 follow-up 1.

**Other agent needs to:** Nothing blocking. If Agent A pulls before making any `src/cli/gate.ts` edits, treat as a real conflict per `06-SYNC-WORKFLOW.md` (the commit-path cart-fetch block was rewritten and a new STEP_UP branch added just above the `decide()` call). No `decide()`/`GateEvent`/`Mandate`/`Receipt` shape changed.

**Interface changes:** None to any frozen contract in `docs/common/03-INTERFACES.md`. Additive only: a new `ExecuteResult`-adjacent-but-separate `CartTotalResolution` type in `src/webcmd/cart-total.ts`; new `resolveCartTotalInr()` export from the same file. No existing consumers of `execute()` or `decide()` are affected.

**Blockers introduced/resolved:** ADR-013 follow-up 1 resolved at the code + unit-test level; the only remaining item on Phase 1 is a live e2e run at ₹300 (recipe committed) — not a code blocker, a machine-availability logistics item.
