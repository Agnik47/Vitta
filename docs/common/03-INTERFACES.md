# 03 — Shared Interfaces (Contract Registry)

This is a **registry**, not a copy of the code. The actual type definitions live in the numbered specs and, once written, in `src/`. This file exists so either agent can answer "is this contract stable, who owns it, and who do I coordinate with before changing it" in one glance, without re-deriving it from `docs/01-ARCHITECTURE.md` and `docs/04-POLICY-ENGINE-SPEC.md` every time. If this file and the actual source code ever disagree, **the source code wins** — update this table to match, the same way `CLAUDE.md` already requires for `docs/OUTCOME.md`.

## Registry

| Contract | Source of truth (spec) | Defined in (code, once built) | Owner | Consumed by | Status |
|---|---|---|---|---|---|
| `Mandate` | `docs/04-POLICY-ENGINE-SPEC.md` § The Mandate schema | `src/mandate/schema.ts` | Agent A | policy, receipt, cli, dashboard | 🔒 Frozen — shipped 2026-07-29 (Phase 1a), plus an additive `isMandate()` type guard not in the original spec |
| `canonicalJSON()` / `sign()` / `verify()` | `docs/04-POLICY-ENGINE-SPEC.md` § Canonical JSON + Ed25519 signing | `src/mandate/sign.ts` | Agent A | mandate, receipt, cli | 🔒 Frozen — shipped 2026-07-29 (Phase 1a), matches the spec's code exactly |
| `Decision` / `SpendRequest` / `decide()` | `docs/04-POLICY-ENGINE-SPEC.md` § The decide() function | `src/policy/decide.ts` | Agent A | cli, (later) the Phase 3 MCP stub | 🔒 Frozen — shipped 2026-07-29 (Phase 1b). **Rule order changed from the original spec**: read-access is now Rule 0 (was Rule 3) — it fires before signature/expiry, not after. `SpendRequest.access` is `'read' \| 'write' \| undefined`, not just `'read' \| 'write'`. Both corrected in `04-POLICY-ENGINE-SPEC.md` itself, see its "Rule order note" and `docs/OUTCOME.md` Phase 1b for why. If you're consuming `SpendRequest`/`decide()` from `03-INTERFACES.md`-era memory, re-check against the current spec — this one moved. |
| `Ledger` interface | `docs/01-ARCHITECTURE.md` § Ledger interface | `src/ledger/Ledger.ts` | **Agent A** — reassigned from Agent B 2026-07-29, see `02-DECISIONS.md` ADR-005. Both the interface and `DodoCreditLedger` are Phase 1c deliverables per `docs/PROMPTS.md`, both now Agent A's. | cli, whatever calls `decide()` | ⏳ Not started — blocked on a real Dodo test-mode account (`04-BLOCKERS.md` B-001). Agent B's own real-SDK research (method names, customer-keyed balance model) carries over — see `docs/OUTCOME.md`'s open-questions table, not re-derived. |
| `GateEvent` / `DenyCode` | `docs/01-ARCHITECTURE.md` § GateEvent | `src/events/GateEvent.ts` | Joint — created for real in Phase 0 (see `docs/OUTCOME.md` Phase 0 entry for why this one file got real content instead of a comment stub) | everything, including the dashboard | 🔒 Frozen at Phase 0. CLAUDE.md itself: "never restructure it later." Treat any proposed change as a stop-the-line event. `DenyCode` gained `ALREADY_EXECUTED` during Phase 0 — see `docs/OUTCOME.md`, it was missing from the original spec despite Beat 8 requiring it. |
| `Receipt` | `docs/04-POLICY-ENGINE-SPEC.md` § The Receipt schema | `src/receipt/schema.ts` | Agent A | cli, dashboard (re-implemented reader, see `docs/06-DASHBOARD-SPEC.md`'s duplication note) | 🔒 Frozen — shipped 2026-07-29 (Phase 1e), matches the spec exactly, no deviations. `src/receipt/chain.ts` also exports `sha256Hex()` and `verifyChain()` (described in the spec's prose, not shown as code) — the dashboard's re-implemented reader should produce identical chain-verify results against these, since both derive from the same "sha256 of canonicalJSON(x)" rule. |
| webcmd manifest access-map shape (`Map<string, 'read'\|'write'>`) | `docs/03-WEBCMD-INTEGRATION.md` § Step 1 | `src/webcmd/manifest.ts` | Agent B | cli, whatever calls `decide()` | 🔒 Frozen — shipped 2026-07-29, real-tested (805 commands, 228 write; live-fetch and disk-cache-fallback paths both confirmed) |
| `ExecuteResult` / `execute()` | `docs/03-WEBCMD-INTEGRATION.md` § Step 5 | `src/webcmd/executor.ts` | Agent B | cli (Phase 1f) | ⚠️ Implemented, NOT yet verified against a live command — see `04-BLOCKERS.md` B-002. Shape matches spec exactly; treat as provisional until a real run confirms it. |
| `LedgerEntry` / `hasAlreadyDrawn()` / `recordDraw()` (the `ledger.jsonl` idempotency guard) | Not in the numbered specs — placement was specified (`docs/PROMPTS.md` Phase 1c prompt), exact shape was not | `src/webcmd/executor.ts` | Agent B | cli (Phase 1f, before/after `Ledger.draw()`) | 🔒 Frozen — shipped 2026-07-29, real-tested (pure fs round-trip). See `02-DECISIONS.md` ADR-004 for the shape rationale. |
| Key persistence (`keys/{issuer,gate}.{private,public}.pem`) / `getOrCreateKeyPair()` | Not in the numbered specs at all — a real gap Agent B found while building Phase 1h (`docs/agent-b/WORKSPACE.md` § Notes for Agent A), resolved in Phase 1f | `src/cli/keys.ts` | Agent A | cli (all mandate/receipt signing), dashboard (needs `keys/gate.public.pem` to verify `signature_valid`) | 🔒 Frozen — shipped 2026-07-29 (Phase 1f). Both PEM files are gitignored, generated on first `gate` CLI use. Dashboard reads `keys/gate.public.pem` via the same `MANDATE_GATE_DATA_DIR` env var already used for `mandates/`/`receipts/`/`events.jsonl` — no new configuration surface. |
| Dashboard API response shapes (`/api/mandate`, `/api/events`, `/api/receipts`) | `docs/06-DASHBOARD-SPEC.md` | `dashboard/app/api/*/route.ts` | Agent B | dashboard frontend only | Shipped 2026-07-29 — owned entirely within Phase 1h, no cross-agent risk, Agent A never imports these. `/api/receipts`' `signature_valid` now real (wired to `keys/gate.public.pem` same day Phase 1f shipped it — real-tested against a bootstrapped keypair via the actual `getOrCreateKeyPair()`, including a live tamper test distinguishing signature vs. chain-link failure). `/api/mandate`'s Dodo balance lookup remains unverified live (B-001). |

## Change protocol

Everything marked 🔒 above is meant to be written once and not touched again — that's what makes the parallel split safe. If real API behavior or a real bug forces a change to a frozen contract (this **will** happen per `CLAUDE.md` rule 6 — Dodo's real response shapes are explicitly unverified until you call the API):

1. Before editing, add an entry to `04-BLOCKERS.md` if the change would block the other agent, or just a heads-up in `08-CHANGELOG.md` if it's purely additive (e.g. adding an optional field).
2. `git pull --rebase` immediately before making the change, to minimize the window where the other agent could be building against the stale shape.
3. Make the change, update this table's `Status` cell (e.g. "🔒 Frozen once Phase 1a ships" → "⚠️ Amended 2026-07-29, see ADR-00X"), and update the owning spec file too if the spec's sketch was simply wrong (this is `CLAUDE.md` rule 6, already in force).
4. **If this is a change to an already-frozen contract** (not the first time it's being defined), write an ADR entry in `02-DECISIONS.md` — what changed, why, what alternatives existed, and what it impacts. Changing something both agents already built against is exactly the kind of decision that log exists to preserve; a one-line changelog entry isn't enough context for the other agent to trust the change without re-deriving your reasoning.
5. Push immediately — do not batch a frozen-contract change with unrelated work.
6. Log it in `08-CHANGELOG.md` with `Interface changes:` filled in explicitly, not left as "none," and link the ADR number.
7. The other agent pulls and re-runs their own tests against the new shape before continuing any work that touches it.

Additive changes (new optional field, new `DenyCode` value) are lower-risk than changes that alter an existing field's type or remove one — still go through steps 2, 3, 5-7, but step 4 (the ADR) is only mandatory when altering or removing something, not for a purely additive field that can't break anything already written. Use judgment; when in doubt, write the ADR.
